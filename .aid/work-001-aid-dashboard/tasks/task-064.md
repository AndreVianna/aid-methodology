# task-064: Reader KB-status extension + cross-runtime git read (Python ∥ Node byte-parity) — KbStateRef{status,summary_present,kb_baseline} + 5-state waterfall + FR35 freshness + docstrings

**Type:** IMPLEMENT

**Source:** feature-007-kb-dashboard → delivery-009

**Depends on:** task-059

**Scope:**
- Extend the feature-002 reader (Python `dashboard/reader/{models.py,parsers.py,derivation.py}` ∥ Node `dashboard/server/reader.mjs`) to derive the 5-state KB status + the FR35 git-freshness read. Both runtimes change in **lockstep**; every parse rule / git invocation / normalization is a literal Python↔Node twin guarded by PT-1-H byte-parity (task-066, R12). This is the **single writer** of the reader files in d009 (no same-file race).
- **DM-A1 extended `KbStateRef`** (`models.py` + the Node task/kb shape): add `status: KbStatus` (NEW — the FR32 5-state enum `pending|generating|preparing|approved|outdated`, derived, never persisted), `summary_present: bool` (NEW — a `stat` of `<repo>/.aid/dashboard/kb.html`), `kb_baseline: KbBaseline | null` (NEW — `{branch, tip_date}` parsed from `.aid/settings.yml` per DM-A4; null if unset/unparseable). All read-derived; **nothing new is persisted** (NFR2).
- **`kb_baseline` parse** (`parsers.py`): a tolerant settings line-scan of the `kb_baseline:` nested block (`branch:` + `tip_date:`), reusing the `parse_project_name` posture (`parsers.py:148`); absent/unparseable → `null`. Matches the task-059 schema shape.
- **FF-A3 5-state derivation waterfall** (`derivation.py`, outermost-first, disk-only): `.aid/knowledge/` absent/empty → `pending`; discovery in progress (KB present, not yet `**User Approved:** yes`) → `generating`; KB approved but `kb.html` absent OR summary not yet V1-approved (`## Knowledge Summary Status` not ok) → `preparing`; else KB+kb.html ready & approved → `outdated` if `freshness_check`==outdated else `approved`. Signals reuse `_parse_kb_summary_approval` (the existing `## Knowledge Summary Status` parse). **Residual #1 — already decided in the SPEC, applied (not pinned) here:** SPEC FF-A3 residual #1 (`feature-007-kb-dashboard/SPEC.md:507-510`) already settled the `generating` predicate's safe default; this IMPLEMENT task makes **no** design decision — it applies that default verbatim: "KB present but not yet `**User Approved:** yes`" → `generating`.
- **FF-A2 cross-runtime git read** (LC-A2, the reader's FIRST subprocess): one bounded read-only `git -C <root> log -1 --format=%cI <branch>` per registered repo. Branch resolution = prefer `baseline.branch`, else `git symbolic-ref --short refs/remotes/origin/HEAD` basename, else first of `{main, master}` (DD-A2). Python `subprocess.run` and Node `child_process.execFile` invoke the **identical argv, no shell**. **Graceful-degradation matrix (DD-A2):** every failure mode (not a git repo / no resolvable branch / detached HEAD / git absent ENOENT / nonzero or unparseable / `kb_baseline` absent / timeout ~2s) → SKIP → stay `approved`; never error, never block. Only `rev-parse`/`symbolic-ref`/`log` verbs — **never** `fetch`/`pull`/`commit`/`checkout`; no file written.
- **UTC-instant normalization before compare (residual #5, R12):** normalize both `current_tip` (committer-local `%cI` offset) and `baseline.tip_date` (possibly `Z`) to a common UTC instant, THEN compare — never a raw lexicographic string compare. Python `datetime.fromisoformat(...).astimezone(timezone.utc)`; Node `Date.parse(...)`/`getTime()`. Pin the shared helper's shape/location here; the `Z`-vs-`±HH:MM` same-instant boundary unit case is task-066.
- **SEC-A1 — update the 4 "no subprocess" docstrings** (the reader's self-documented NFR2 invariant is now false): change `dashboard/reader/__init__.py:7`, `dashboard/reader/reader.py:15`, `dashboard/reader/reader.py:54`, `dashboard/reader/derivation.py:49` from "...no subprocess..." → "**no write / no LLM / one read-only `git log` subprocess for KB freshness (FR35)**". Do **NOT** introduce a "no subprocess" docstring into `reader.mjs` (it has none today — verified). The no-write self-check (`test_no_write_primitives_in_reader_modules`, `test_reader.py:833`) still passes (no write-mode `open()` added).
- **SEC-A1 (cont.) — reconcile the TWO delivered "no subprocess" TEST assertions** that this single sanctioned `git log` subprocess would turn RED on execute (this is the IMPLEMENT task that *introduces* the subprocess and already owns the 4 docstring updates, so it owns keeping the suite green). Both sites must be tightened from "no subprocess at all" to "**no write/mutation subprocess; the ONE read-only `git log -1` for KB freshness (FR35) is allowed**" — i.e. still forbid write-mode subprocess / `Popen`-for-mutation and any non-`git-log` subprocess, but permit the single sanctioned read-only `git log`:
  1. `dashboard/reader/tests/test_integration.py` — `test_reader_modules_still_clean_after_integration` (`:542`): the `assertNotIn("import subprocess", source)` (`:562-566`) + `assertNotIn("Popen", source)` (`:568-572`) sweep over `{locator,parsers,reader,models,derivation}.py` (`:551-557`) must be relaxed to allow the single read-only `git log -1 --format=%cI` invocation while still asserting no mutating-git verb (`fetch`/`pull`/`commit`/`checkout`/`reset`) and no `Popen`/write-mode subprocess appears.
  2. `dashboard/reader/tests/test_feature009.py:24` — the module-docstring claim "no subprocess except for Node" is now false for the reader; update it (and any assertion it backs) to "no subprocess except for Node and the one read-only `git log` KB-freshness read (FR35)".
  This is the legacy-assertion relaxation; it is **distinct** from task-066's NEW git-state-determinism parity case (which adds coverage, not relaxation) — no duplication.
- **DM-A3 — serialize `kb_state` at the SAME `/api/model` path, NO `schema_version` bump.** `kb_state` stays a strict superset of the existing fields; the per-`<id>` `/api/model` envelope grows the 3 new keys in deterministic DM-3 key order (compact, `ensure_ascii=False`, U+2028/U+2029 escape preserved — R7), no `schema_version` change.
- Reader stays **read-only / no-LLM** throughout (NFR2/NFR7): only parse rules + bounded reads + the ONE read-only git subprocess; no write/lock/agent call.

**Acceptance Criteria:**
- [ ] `KbStateRef` carries `status` (5-state `KbStatus`), `summary_present` (bool, `kb.html` stat), `kb_baseline` (`{branch,tip_date}` or null) in both runtimes; all read-derived, nothing newly persisted (NFR2); an absent/unparseable `kb_baseline` → null (skip freshness, stay `approved`).
- [ ] The FF-A3 waterfall derives `pending → generating → preparing → approved → outdated` outermost-first from disk-only signals, byte-identically Python↔Node; the `generating` predicate applies the SPEC's already-decided safe default (residual #1, `SPEC.md:507-510` — no design decision in this task); `outdated` is checked **last and only over `approved`**.
- [ ] The git read invokes the **identical** read-only `git log -1 --format=%cI <branch>` argv (no shell) in both runtimes; the full DD-A2 degradation matrix (7 failure modes) each → SKIP → `approved`; only `rev-parse`/`symbolic-ref`/`log` verbs appear — a self-check (grep, mirroring the no-write check) asserts **no** mutating git verb and no file write.
- [ ] Both runtimes normalize `current_tip` and `baseline.tip_date` to a common UTC instant before comparing (never a raw lexicographic compare); the shared helper's shape/location is pinned (the `Z`-vs-`±HH:MM` boundary unit case is task-066, R12).
- [ ] The **4 "no subprocess" docstrings** (`__init__.py:7`, `reader.py:15`, `reader.py:54`, `derivation.py:49`) are updated to "no write / no LLM / one read-only git subprocess for KB freshness (FR35)"; `reader.mjs` gains **no** such docstring; `test_no_write_primitives_in_reader_modules` (`test_reader.py:833`) still passes.
- [ ] **BOTH delivered "no subprocess" TEST assertions are reconciled** so the suite stays green with the single sanctioned `git log` subprocess: (1) `test_integration.py::test_reader_modules_still_clean_after_integration` (`:542`, the `import subprocess`/`Popen` sweep at `:562-572`) and (2) `test_feature009.py:24` (the "no subprocess except for Node" docstring/claim) are tightened to permit only the read-only `git log -1` while still forbidding write/mutation subprocess, `Popen`-for-mutation, and any mutating-git verb (`fetch`/`pull`/`commit`/`checkout`/`reset`).
- [ ] After the change, the FULL `python3 -m pytest dashboard/reader/tests/ dashboard/server/tests/` runs **green** (no test left RED by the introduced subprocess).
- [ ] `kb_state` serializes at the same `/api/model` path with the 3 new keys in deterministic DM-3 order, U+2028/U+2029 escaped, with **NO** `schema_version` bump (DM-A3); structural self-checks confirm read-only/no-LLM (NFR2/NFR7) — exactly one read-only git subprocess, no write/lock/agent call.
- [ ] All §6 quality gates pass; IMPLEMENT default — unit tests for the waterfall + each degradation branch + the normalization added in both runtimes; cross-runtime byte-parity is task-066.
