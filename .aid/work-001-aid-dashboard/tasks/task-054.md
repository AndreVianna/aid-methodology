# task-054: feature-006 revision — rename `index.html` → per-repo `home.html`, DELETE the Level-0 `.card.plugin` panel (KB card stays 2-state)

**Type:** REFACTOR

**Source:** feature-006-project-main-page → delivery-008

**Depends on:** task-048

**Scope:**
- Land feature-006's two-level revision (R-1 + R-2) as a bounded front-end delta on the delivered d004 page. This task **renames away** the old `dashboard/index.html` (→ `home.html`) **before** task-053 creates the NEW CLI-home `index.html` at that same path; the two front-end writers therefore touch **different files** (this task owns `home.html`; task-053 owns the new `index.html`) and never contend. task-053 encodes a `Depends on: task-054` edge so this rename-away is enforced/auditable, not merely wave-incidental (see finding #2).
- **R-1 re-home + client poll-URL edit:** rename/relocate the delivered `dashboard/index.html` (d004 per-repo main page) to **`<repo>/.aid/dashboard/home.html`** — co-located with `kb.html` (NFR11), served by feature-010's server at `/r/<id>/home.html`. The page stays the same polling SPA, but its hardcoded **absolute** client poll URL must change: edit the `fetch('/api/model')` call in the `home.html` body (currently `dashboard/index.html:1010`, an absolute `/api/model` that would hit the CLI-home root route when the page is served at `/r/<id>/home.html`) to the **location-relative** `fetch('./api/model')` form, so when the document is served at `/r/<id>/home.html` the poll resolves to **`/r/<id>/api/model`** (the per-repo route). This mirrors feature-007's location-relative `./kb.html` pattern (which resolves to `/r/<id>/kb.html`) and consumes feature-010's per-`<id>` route (FF-2). The **`<id>` URL grammar / per-`<id>` route shape** that justifies the relative resolution is task-048's contract (DD-1) — this task depends on task-048 for that; it does **not** need the server *implementations* (task-050/051), since a relative `./api/model` resolves purely client-side against the document's served path. Keep the per-work hash router (`#/work/<id>/...`) unchanged — it composes under the `/r/<id>/home.html` document.
- **R-2 DELETE the Level-0 panel:** remove the d004 `LC-L0` `.card.plugin` panel (kicker "AID CLI (this machine)", version/install-location/tools + its "unavailable" degraded state, `index.html:1589-1645`) and its dedicated `.card.plugin` CSS (`index.html:516-523`) from `home.html`. The machine/CLI info now lives ONLY on feature-010's CLI home (FR33). The d004 two-section layout ("Pipelines" + "Knowledge & Tooling") collapses to a **Pipelines section + a single KB card**. The server may still carry the L0 field in the `/api/model` envelope for compatibility, but `home.html` ignores it (no reader/server change owned here).
- **KB card stays the carried-over d004 2-state form (NOT the 5-state repoint).** Per the d008 "KB-card intermediate state" plan note: the KB card keeps its delivered d004 2-state behavior (Approved/Draft/`null`→"no KB"); the feature-007 **5-state** repoint + the `/r/<id>/kb.html` target are a **d009 step (feature-006 R-3)**, NOT this task. d008 ships the slot in its valid 2-state form so there is no broken intermediate.
- **R-4 carry-over UNCHANGED:** the work-card grid, FR11 attention sort + amber/red left-border + "attention cards first" two-pass sort, `_fmtLocalDateTime` local date formatting, the FR18 empty-state, and the read-only/no-LLM contract (NFR2/NFR7) are re-homed verbatim. No new model/endpoint/schema bump owned here (R-6).
- No reader/server/canonical edit; pure front-end file rename + delete + (KB-card untouched). The Playwright R5 validation of `home.html` (via the click-through from the CLI home) is task-058.

**Acceptance Criteria:**
- [ ] The delivered `dashboard/index.html` is renamed/relocated to `<repo>/.aid/dashboard/home.html`; it remains the live polling SPA (work-card grid + KB card) and the per-work hash router is preserved.
- [ ] The client poll URL is changed from the absolute `fetch('/api/model')` (`dashboard/index.html:1010`) to the **location-relative** `fetch('./api/model')`, so a `home.html` served at `/r/<id>/home.html` polls `/r/<id>/api/model` (the per-repo route, per task-048's DD-1 `<id>` grammar) — consistent with feature-007's `./kb.html` pattern; no absolute `/api/model` string remains in `home.html`.
- [ ] The Level-0 `.card.plugin` panel (render path + its dedicated CSS) is **deleted** from `home.html`; the page shows only project-scoped content (Pipelines section + a single KB card); the machine/CLI info appears nowhere on `home.html` (FR33 removal half).
- [ ] The KB card keeps its **d004 2-state** behavior (Approved/Draft/`null`→"no KB") — it is **not** repointed to feature-007's 5-state model or `/r/<id>/kb.html` (that is a d009 step); the card always renders a valid state (no broken intermediate).
- [ ] Everything else from d004 (FR11 attention sort + amber/red border, local date formatting, FR18 empty-state, read-only/no-LLM) carries over unchanged; no new model field / endpoint / schema bump is introduced here (R-6).
- [ ] Static self-checks: `home.html` writes nothing to `.aid/`, no agent/LLM import, same-origin fetch only.
- [ ] All §6 quality gates pass; existing front-end tests updated for the rename pass (visual R5 gate is task-058).
