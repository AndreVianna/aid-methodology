# Specification State

**Status:** Ready
**Started:** 2026-05-22

## Activated Sections

| Section | Status | Activation |
|---------|--------|------------|
| Data Model | Written | core — no new persisted artifact; defines the static per-skill state-map descriptor (state id + label + description + order, optionally branch); feature-007 reads no persisted artifact either (standalone, pure text) |
| State Machines | Written | warranted — the "you are here" map *is* a rendered projection of every skill's state machine; this section pins what feature-007 visualizes vs. what it must not alter (§7) |
| Feature Flow | Written | core — three flows: state-entry print, per-transition state-map render, and bracket-pair around every long operation; 3-tier NFR4 degradation; the bracket-pair floor is the always-available "is it stuck?" answer |
| Layers & Components | Written | core — state-entry print + map render + bracket-pair invocation points in skill bodies, per-skill state-map descriptors, static per-operation-class rough-time hints table; pure skill-body text (no shipped scripts, no helpers, no hooks); FR5 delivery seam |
| ~~Events & Messaging~~ | Removed | dropped in scope reshape — feature-007 no longer emits or consumes events; user-facing output is purely printed text in the chat |
| Migration Plan | Written | auto — additive near-greenfield: no existing map/bracket-pair; no backward-compat surface of its own; uniform rollout across all 10 skills |

## Pending Q&A

(none)

## Loopbacks

(none)

## Change Log

| Date | Change | Source |
|------|--------|--------|
| 2026-05-22 | Specification started — 6 sections activated (3 core + State Machines + Events & Messaging + Migration Plan); Status → In Discussion | /aid-specify |
| 2026-05-22 | Technical Specification written — Data Model, State Machines, Feature Flow, Layers & Components, Events & Messaging, Migration Plan. Key decisions: feature-007 persists nothing of its own and adds no `data-model.md` artifact; it is a read-only consumer of feature-006's `progress.jsonl` + `.progress-context.json` and emits `heartbeat`/`error` *through* feature-006's `emit-progress-event` (single-writer discipline); helpers are shell, shipped payload (NFR5); the state map is a projection of the FR3 dispatch table, never a redefinition of any state machine (§7); 3-tier NFR4 degradation guarantees at minimum a "starting … / finished" bracket pair on every tool. **OQ6 (carried from feature-006) resolved:** feature-006 owns writing `.progress-context.json`, feature-007 only reads it. 3 new open questions surfaced (OQ-A descriptor carrier, OQ-B heartbeat cadence + mid-operation tick mechanism, OQ-C descriptor/dispatch-table single-source-of-truth). | /aid-specify |
| 2026-05-22 | Reviewer-identified fixes — cross-cutting CR2/CR5/CR6 + 5 spec-local corrections. **CR2 applied:** every `state.enter` / `phase.enter` reference renamed to `state.start` / `phase.start` (preamble, Data Model row, State Machines projection bullet, Flow A box label + step, Events & Messaging consumed-events paragraph + parenthetical note, OQ6 resolution body). **CR6 applied:** illustrative "you are here" projection now uses feature-002's `SCREAMING_CASE` ids (`LOAD_TASKS`, `EXECUTE_WAVE`, `QUICK_CHECK`, `DELIVERY_GATE`), with a sentence stating the inheritance. **CR5 applied:** OQ6 resolution body updated to note feature-006's spec now also marks its OQ6 RESOLVED, so the two specs agree end-to-end (feature-006 writes; 007/008 read-only). **CR8 honored:** `(graded A)` references in the preamble retained. **Spec-local fix 1:** preamble FR4-P1 vs FR4-P2 miscite repaired — the P2 bullet states the cross-mechanism "stream is the data source" rule that feature-007 (P1) honors by consumption, no longer phrased as if P2 governs feature-007. **Spec-local fix 2:** present-tense over-claim that the FR3 thin-router refactor "already enumerates every state" / "already makes each skill's state set explicit" softened to future/conditional tense in both the Data Model "Form" cell and the State Machines "What feature-007 does NOT do" paragraph, consistent with feature-002 being a soft dependency not yet shipped. **Spec-local fix 3:** Events & Messaging `error` row tightened to drop implied exclusivity — feature-007 is *a* producer for watched-operation failures, and feature-006's emitter independently emits `error` on its own append failures; the surrounding paragraph also softened from "the producer" to "a producer". **Spec-local fix 4:** OQ-A ↔ OQ-C one-line cross-reference added to both questions, noting they are decided together as two facets of the same single-source-of-truth question. | /aid-specify (Wave-2 correction) |
| 2026-05-22 | Fresh-eyes scope reshape — redesigned standalone (no dependency on feature-006); pure skill-body text design (state-entry print + bracket-pair floor + ASCII state-map); all hook/JSONL/schema/context-file references removed; OQ-B (mid-wait tick) dropped; OQ6 (run-context ownership) dropped; (graded A) preamble references removed (prior grade reflects a different design). Activated-Sections table also edited as part of this reshape: the **Events & Messaging** row was struck through and re-labelled "Removed" (with "dropped in scope reshape — feature-007 no longer emits or consumes events" as the activation note), reflecting that the section was removed from SPEC.md. Noted retroactively for audit-trail completeness. | /aid-specify (scope reshape) |
| 2026-05-22 | Reviewer fixes applied to SPEC.md (1 LOW + 3 MINOR): **LOW (cross-reference)** — every bare `feature-001` / `decision F` cross-work reference qualified as `work-002's feature-001-profile-driven-generator` / `work-002 decision F` (parallel to feature-002's SPEC cleanup); touched the Data Model "canonical content" line, the Layers & Components "Skill body" row, the "Per-tool delivery via FR5" paragraph, Migration Plan step 4, OQ-A, and OQ-C. **MINOR #1 (AC split)** — old AC1 split into AC1 (state-entry print) + AC3 (ASCII "you are here" map on state transition); old AC2 (bracket-pair) retained verbatim as AC2 — each of the three components now has its own testable criterion. **MINOR #2 (threshold)** — Bracket-pair coverage paragraph clarified: threshold is qualitative per-skill judgment, **rough-time-hints table is the source of truth for what gets bracketed**, with a multi-second calibration aid (sub-second tool calls not bracketed). **MINOR #3 (audit-trail)** — this STATE.md change-log entry, plus the back-fill on the reshape entry above, explicitly capture the Activated-Sections "Events & Messaging" row edit. No section status transitions in STATE.md (no new sections activated, no Written-status changes). | /aid-specify (reviewer fixes) |
| 2026-05-22 | Absorbed feature-002's OQ-A/OQ-C resolutions — descriptor carrier is the first-line of `references/state-{name}.md`; SSoT is the dispatch table; all 6 SPEC touch-points updated; Open Questions section closed for OQ-A/OQ-C. Specifically: (1) Data Model "Form" cell rewritten to state the resolved carrier and point at feature-002 SPEC's *"State descriptors and single source of truth"* subsection (no candidate-carrier language); (2) Data Model "Source of truth" cell updated to state the dispatch table as SSoT (no "See OQ-C" deferral); (3) Data Model `data-model.md` reference paragraph rewritten to drop the OQ-A conditional; (4) State Machines "Methodology preservation" paragraph updated to reference feature-002's resolution rather than naming OQ-C as an open item; (5) Layers & Components "State topology" row rewritten — descriptor derived from feature-002's dispatch table + `references/state-{name}.md` first-lines, no separate descriptor file; (6) "Per-tool delivery via FR5" paragraph rewritten to drop the "if OQ-A chooses that carrier" conditional; (7) Dependencies bullet rewritten — feature-002 owns the on-disk carriers, feature-007 reads them or degrades cleanly; (8) Migration Plan steps 1, 3, 4, 5 updated to drop "descriptor lives inline in SKILL.md" / "descriptors added by hand" / "If OQ-A selects a standalone references/state-map.md carrier" conditionals; (9) Open Questions section restructured — OQ-A and OQ-C moved to a new "Resolved Questions" subsection, with "(none open)" stated at the top of Open Questions; (10) one remaining bare `FR5` reference at line 77 qualified as `FR5 (work-002's feature-001-profile-driven-generator)`, parallel to the other cross-work references in this SPEC. **Pending Q&A** verified `(none)` — no change. No section status transitions. | /aid-specify (cross-feature absorption) |
