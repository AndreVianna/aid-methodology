---
kb-category: primary
source: hand-authored
objective: Defined and prioritized work items for AID that have not shipped — the slice committed to the next release and the prioritized remainder.
summary: Read this to see what is accepted into the plan but not yet shipped; raw unscheduled observations live in tech-debt.md and shipped work in release-tracking.md.
sources: []
tags: [C7, backlog, prioritization, items, planning]
see_also: [tech-debt.md, release-tracking.md, roadmap.md]
owner: architect
audience: [developer, architect, pm]
---

# Backlog

This document holds the work items that have been explicitly accepted into the plan but not
yet shipped. An item moves here when a human makes a deliberate planning decision to commit
to it — the per-item confirm gate at `/aid-create-backlog` or `/aid-update-backlog`. Raw,
unscheduled debt observations stay in `tech-debt.md` until that decision is made; shipped
items move to `release-tracking.md`.

## Contents

- [Next Release](#next-release)
- [Prioritized](#prioritized)
- [Gotchas](#gotchas)

## Next Release

The committed slice for the current tag. Every item here is drained by `release-aid` at tag
time — it becomes a tagged release-note bullet in `release-tracking.md` and is deleted from
this document in the same run.

| ID | Tag | Title | Definition & done-condition | Location | Risk if not done | Priority |
|----|-----|-------|-----------------------------|----------|-----------------|----------|
| **W5-24** | `[CHANGE]` | **Breaking — the skill-alias model is retired** | **Breaking.** The skill-alias model is removed. An alias was never a synonym entry: each one was a full shortcut-catalog row *plus* its own skill directory, marked by a non-null `alias_of`. All 36 are deleted — the `/aid-add-*` and `/aid-change-*` families plus `/aid-audit`, `/aid-delete`, `/aid-investigate`, `/aid-query-kb`, `/aid-show-dashboard` and `/aid-spike`. Those names no longer resolve to anything; use the surviving canonical name instead (`/aid-add-api` → `/aid-create-api`, `/aid-change-ui` → `/aid-update-ui`, `/aid-query-kb` → `/aid-ask`). The `alias_of` field itself is deliberately retained as a required-but-always-`null` key, with the catalog header recording why its removal is still blocked. **Done when** the next tag ships it — **shipped, pending tag** (PR #176) | `canonical/aid/templates/shortcut-catalog.yml`; `canonical/skills/`; `README.md` § retired-names note | Adopters upgrade into 36 silently-missing skill names with no release note explaining the rename | P1 |
| **W5-25** | `[NEW]` | Design-phase skill family | **Design-phase skill family** — 36 new skills implementing a uniform `design → create → update` lifecycle across nine artifact types: roadmap, MVP, backlog, architecture, technology stack, testing strategy, CI/CD, document and brainstorm. A `design` run develops a subject as a **design seed** under `.aid/design/` and writes no production code and no Knowledge Base document; a `create` run realizes a ready seed into its destination document and registers it; an `update` run revises content already committed, and needs no seed. They share one contract (`canonical/aid/templates/design-lifecycle.md`) and the seed format (`design-seed.md`), and `roadmap.md` and `backlog.md` are admitted as conditional Knowledge Base documents with conditional registrations in `.aid/settings.yml`. All 36 rows carry `repurpose: true`, so the shortcut catalog returns to 94 rows and the corpus to 111 skill directories, with the generated-doorway count unchanged at 34. **Done when** the next tag ships it — **shipped, pending tag** (PR #184) | `canonical/skills/aid-{design,create,update}-{roadmap,mvp,backlog,architecture,stack,testing-strategy,cicd,document}`; `canonical/skills/aid-brainstorm`; `canonical/aid/templates/design-lifecycle.md`; `canonical/aid/templates/design-seed.md`; `canonical/aid/templates/design-folder-readme.md`; `canonical/aid/templates/shortcut-catalog.yml` | The release's largest feature ships with no release note, and adopters never learn the `.aid/design/` seed layer exists | P1 |
| **W5-26** | `[CHANGE]` | **Breaking — Node floor raised to >=22, CI pinned to 24** | **Breaking.** Every adopter-facing Node requirement is raised to **>=22** and CI pins **24**. Node 20 reached end-of-life on 2026-04-30 while the npm wrapper still declared `>=18`, the repository root `>=20`, and five CI jobs pinned `20` — so for three months every merge was gated on a runtime receiving no security patches. The two numbers are deliberately different and now documented as a policy: the floor is the oldest supported LTS (22, supported through 2027-04-30), the CI pin is the newest Active LTS (24, through 2028-04-30), and the pin is re-checked each LTS cycle rather than left to expire silently. `site/` keeps its own higher Astro floor of `>=22.12.0`, which 22 satisfies. **Done when** the next tag ships it — **shipped, pending tag** (PR #181) | `packages/npm/package.json`; root `package.json`; `.github/workflows/{test,coverage-parity,installer-tests,release}.yml`; `.aid/knowledge/technology-stack.md` § *Node version policy* | Adopters on Node 20 upgrade into an unexplained install failure | P1 |
| **W5-27** | `[NEW]` | Skill Explorer — a generated `/skills/` section on the product site | **Skill Explorer** — the product site gains a generated `/skills/` section pairing every skill's frontmatter with a flow chart derived from that skill's own instructions, so a reader can follow what a skill does step by step without opening its source. Ships 111 skill detail pages plus an index, generated at build time by `site/scripts/gen-skills.mjs`, which throws by name on corpus drift — an on-disk skill that is neither a catalog row nor a curated-roster member fails the build. Every page carries a derived flow chart (ordered steps, loops, decision branches, exit points), verbatim prompt fragments with `canonical/` deep links behind a provenance verifier, and a click-to-open node panel reachable by pointer and keyboard. `reference/skills.md` sheds the roster it duplicated and keeps only the shortcut-engine narrative. `docs.yml` now runs the site test suite on every PR touching `site/`, `docs/`, `canonical/` or `VERSION`. **Done when** the next tag ships it — **shipped, pending tag** (PR #174) | `site/scripts/gen-skills.mjs`; `site/scripts/lib/flow-graph/`; `site/src/content/docs/reference/skills.md`; `.github/workflows/docs.yml` | A user-visible site feature ships unannounced | P1 |
| **W5-28** | `[NEW]` | Declared review criteria | **Declared review criteria** — every authored file now declares what it must be true against, and every review finding cites the criterion it violates. Criteria resolve through three levels — global, per-document-type, per-file — each written once at the highest level where it holds, so most files declare nothing and are covered by their type. The former `contracts:` frontmatter key is renamed and widened to `review-criteria:` across all four trees, severity is reconciled to a single authority, and the reader surfaces resolve, cite, and surface overrides. Criteria are a *writer's* contract resolved before an edit, with the reviewer as backstop rather than enforcer. The 462-line repo-wide count guard is retired in favour of `tests/canonical/test-doc-counts.sh`, which derives the same counts over the public-facing surface. **Done when** the next tag ships it — **shipped, pending tag** (PR #190) | `.aid/knowledge/authoring-conventions.md`; `canonical/aid/templates/reviewer-ledger-schema.md`; `canonical/aid/templates/review-rubric.md`; `tests/canonical/test-doc-counts.sh` | The review model changes shape with no note telling adopters their files can now declare criteria | P1 |
| **W5-29** | `[CHANGE]` | Scoped review cycles and criterion oracles | Review cycles get cheaper without losing what they catch. From cycle 2 onward only the *hunt* is scoped while verification stays FULL, which is what protects a `Recurred` finding from being missed; a paired within-task control over three real subjects, holding task, artifact and reviewer fixed, measured a **61.5% mean reduction** in declared read surface at cycle 2. A new optional per-file `oracle:` key gives a criterion a machine verdict contract — one 151-line script decides 241 of 317 files in under half a second, removing roughly 186 KB of hand re-derivation per cycle. The per-task requirements slice is 83% smaller per specify-gate load. `grade.sh` is byte-identical and the ledger is still 7 columns. **Done when** the next tag ships it — **shipped, pending tag** (PR #194) | `canonical/aid/templates/reviewer-ledger-schema.md`; `canonical/agents/aid-reviewer/`; `canonical/aid/scripts/review/` | A measurable cost reduction in every review cycle goes unannounced | P2 |
| **W5-30** | `[CHANGE]` | **Breaking — the pipeline artifact set is folded** | **Breaking.** The per-feature `SPEC.md` and per-delivery `BLUEPRINT.md` are retired. A feature is now a `### Feature NNN` section under `REQUIREMENTS.md § 11`, and a delivery a `### delivery-NNN` stanza in `PLAN.md`; the Lite path writes only `REQUIREMENTS.md` plus one `DETAIL.md` per task, since one feature and one delivery leave no sequencing decision to record. The execution graph is **derived** from each task's `**Depends on:**` field (`derive-waves.sh --from-tasks`) instead of stored, so a stored copy can no longer disagree with the tasks it came from; the derivation is ported in-process to both dashboard readers with cross-runtime parity tests. Acceptance criteria carry stable `AC-N` ids that a task's `**Source:**` cites, making a per-task requirements slice derivable. Layout is read from `pipeline.path` in `STATE.yml` rather than inferred from `BLUEPRINT.md` presence. `spec-template.md` is split rather than deleted: its body becomes `technical-specification-template.md`. **Done when** the next tag ships it — **shipped, pending tag** (PR #191) | `canonical/aid/templates/technical-specification-template.md`; `canonical/aid/scripts/execute/derive-waves.sh`; `canonical/aid/scripts/works/slice-requirements.sh`; `canonical/skills/aid-{specify,plan,detail,execute}/SKILL.md`; `examples/` | Adopters with in-flight works find `SPEC.md` and `BLUEPRINT.md` gone with no migration note | P1 |
| **W5-31** | `[CHANGE]` | Review stack completion — one path, arguable severity, measured recall | The review stack is completed along three tracks (Align, Gaps, Measure). There is now **one review path** — the rival rubric-catalog framing is gone and `review-path-audit.sh` checks that across four layers. Every finding carries a *why-line* naming the consequence and a provenance token saying where its severity band came from, so a severity asserted without a consequence leaves something on the row to argue with. A new cycle can no longer reach the previous cycle's ledger: the dispatch **refuses**, where before it was merely told not to. Recall is measured against 20 seeded defects and reported per criterion scope as raw counts, replacing a re-derivation that was wrong by default — the naive row count and the status-aware count disagree on 17 of 21 real ledgers. A fix must close its class, recorded as commit trailers a verifier re-runs. Six checks that shared one failure mode — a check examining nothing reports clean — now fail loudly, each with a test that fails when its guard is removed. **Done when** the next tag ships it — **shipped, pending tag** (PR #196) | `canonical/aid/templates/reviewer-ledger-schema.md`; `canonical/aid/scripts/review/review-path-audit.sh`; `canonical/agents/aid-reviewer/` | Reviewer behaviour changes materially with no note | P2 |
| **W5-32** | `[CHANGE]` | Knowledge Base index restructured | The generated Knowledge Base index is restructured, behaviour-preserving in the sense that matters: no row is added, dropped or re-sourced and no cell recomputed. The routing table now emits `Document \| Audience \| Tags \| See-instead \| Objective \| Summary` — the short, near-fixed-width columns a reader actually scans down come first, and the two long prose columns sit at the end. The separate `## Extension — project-specific` section is folded into `## Primary`, so the index has one document list rather than two. **Done when** the next tag ships it — **shipped, pending tag** (PR #188) | `canonical/aid/scripts/kb/build-kb-index.sh`; `.aid/knowledge/INDEX.md` | Adopters see their regenerated `INDEX.md` change shape with no explanation | P2 |
| **W5-33** | `[CHANGE]` | The Knowledge Base history apparatus is removed | The Knowledge Base carried 231 work-folder citations across 18 documents, most pointing at folders already pruned — sanctioned by the authoring model rather than drift, since three channels (`changelog:` frontmatter, a `## Change Log` section, and a "semantic anchor" exception) were all marked exempt from review. The governing rule is now explicit: **the Knowledge Base states only the current state of the project's sources**, and git records per-document history at higher fidelity. Removed: 231 references, 19 `## Change Log` sections, a `## Revision History`, and 20 `changelog:` frontmatter blocks — together with the generators, seed templates and skill prompts that would have written them back. Process records in `STATE.md` are kept in full, so `/aid-housekeep` resume is unaffected. **Done when** the next tag ships it — **shipped, pending tag** (PR #183) | `canonical/aid/templates/kb/{principles,tier-model,frontmatter-schema,review-rubric}.md`; `canonical/aid/scripts/kb/build-kb-index.sh`; `canonical/aid/scripts/kb/migrate-kb-frontmatter.sh`; the 14 KB seed templates | Adopters' next `/aid-discover` or `/aid-housekeep` run silently drops sections they expect, with no note saying why | P2 |
| **W5-34** | `[CHANGE]` | Test-suite performance and the coverage-parity gate | The canonical test suite is made materially faster while preserving coverage, and gains a gate that proves it. A new coverage-parity gate (`tests/coverage-parity.sh`) diffs the executed-assertion multiset against a committed baseline with a re-home allowlist, so a suite refactor cannot quietly drop an assertion; it is bootstrapped from CI and wired into the pipeline. Seven peer suites that redundantly re-ran each other are de-nested, `run-all.sh` runs suites in parallel with a single-threaded non-interleaved replay and an `AID_TEST_JOBS=1` serial lane, PowerShell parity collapses ~158 cold `pwsh` starts into one warm session, and the byte-identity suite goes from ~1765 forks to ~10. The CI job's wall clock falls from ~690s to a ~183s floor against a committed contract of ≤3 minutes. **Done when** the next tag ships it — **shipped, pending tag** (PRs #170, #171, #172) | `tests/coverage-parity.sh`; `tests/run-all.sh`; `tests/lib/{pwsh,sandbox,net}.sh`; `.github/workflows/coverage-parity.yml` | Contributors never learn the suite is parallel now, or that a dropped assertion is gated | P3 |
| **W5-35** | `[FIX]` | Two flat-layout defects in the execute state helpers | `writeback-state.sh --findings` was dead on the flat (single-feature) work layout: `mode_findings` resolved a per-task `STATE.md` path unconditionally and died, because a flat work has no per-task state file by design — so the quick-check findings write failed on **every** flat work, which is every shortcut-engine work. `mode_field` already carried the correct guard; `mode_findings` never got one. Fixed by routing the flat case to a dedicated writer rather than copying the full-layout logic. **Done when** the next tag ships it — **shipped, pending tag** (PR #189) | `canonical/aid/scripts/execute/writeback-state.sh` | Every shortcut-engine run keeps losing its quick-check findings silently | P2 |
| **W5-36** | `[FIX]` | Windows version-sync false failure, manifest timestamp ratchet, and test tag leak | `check-version-sync.sh` embedded a POSIX path inside a code string handed to a Windows-native interpreter at four sites. MSYS translates paths in `argv` but not inside a quoted code string, so the read failed, the substitution yielded the empty string, and both the npm and PyPI carriers were reported as diverging — a false FAIL of the very gate `release.yml` depends on, for any Windows user running it from Git Bash. It had been mis-filed for two release cycles as a Windows-local test artifact. Fixed by converting paths with `cygpath -m` before embedding, and reproduced against the host before fixing. Ships alongside a manifest timestamp ratchet fix and a test tag leak fix. **Done when** the next tag ships it — **shipped, pending tag** (PR #177) | `scripts/check-version-sync.sh` | A Windows contributor's release gate keeps failing for a reason the tech-debt row got wrong twice | P2 |

## Prioritized

Accepted items not yet committed to the current tag, ordered by priority.

| ID | Tag | Title | Definition & done-condition | Location | Risk if not done | Priority |
|----|-----|-------|-----------------------------|----------|-----------------|----------|
| **W5-20** | `[CHANGE]` | A refused or routed design-lifecycle run still leaves an allocated work folder and git worktree behind | The design-lifecycle skills allocate through the Work Initiation Gate at their `INTAKE` state — before the `GUARD` that refuses a seed whose `## Open questions` is non-empty, and before the `REALIZE` that routes to a prerequisite skill when the destination is absent. An invocation that writes nothing therefore still creates a `work-NNN` folder **and**, because allocation runs `worktree-lifecycle.sh create`, a git worktree the user must remove by hand. Decide whether that is the intended shape. Two coherent resolutions: move allocation to after both exits, or have each exit tear down what it allocated. **Done when** the allocation contract states the chosen rule explicitly and every skill body matches it, with the refusal and routing-exit paths asserted either way. Note the counter-argument on record: allocation is currently unconditional *by design*, so that a graded, reviewable work folder exists for every invocation — changing it is a contract change, not a bug fix | `canonical/aid/templates/design-lifecycle.md` § *Skill shape — Allocation*, and its binding-table row `Allocation via the Work Initiation Gate`; the `INTAKE` state of each `canonical/skills/aid-{design,create,update}-*/SKILL.md` body; `canonical/aid/scripts/works/worktree-lifecycle.sh` | Refused and routed runs accumulate empty work folders and stray git worktrees in every adopter's project, with no skill that cleans them up; and verification keeps re-deriving the same surprise, having already produced one unsatisfiable acceptance criterion from the assumption that a non-realizing run allocates nothing | P3 |

## Gotchas

- **ID is inherited on promotion, never re-minted.** The `comm -12` duplicate-item oracle
  (V18) keys on the id; re-minting a promoted id makes the check compare unlike things and
  breaks the move audit entirely.
- **Parking an item in `## Next Release` is a commitment.** That section is drained at
  tag time by `release-aid`; every row there becomes a tagged release-note bullet in
  `release-tracking.md` and is deleted from `backlog.md` in the same run. Do not park
  an item there unless the current tag will include it.
- **An item is moved, never copied.** The promoted row is deleted from `tech-debt.md` in
  the same run that adds it to `backlog.md`. An item present in both documents is a
  checkable defect.
