---
kb-category: primary
source: generated
generator: build-project-index.sh
intent: |
  Repository inventory for the AID-methodology repo. Covers the top-level folder layout
  (canonical/, profiles/, .claude/, .aid/, methodology/, tests/), entry points (run_generator.py,
  setup.sh, setup.ps1), and key config files (settings.yml, EMISSION-MANIFEST.md, profile
  TOML files). Generated by discovery-scout from .aid/generated/project-index.md; counts
  are authoritative as of the snapshot date. Read this for a navigational map before diving
  into any subtree.
contracts:
  - "1,077 files / 148,068 lines as of 2026-05-27 snapshot"
changelog:
  - 2026-05-27: Initial frontmatter added during cycle-1 FIX Phase B
---
# Project Structure

> Repository inventory for AID (AI Integrated Development) — the methodology
> specification and multi-tool distribution that ships skills/agents/templates/recipes
> into 3 install trees (Claude Code, Codex CLI, Cursor) from a single `canonical/` source.
>
> Generated by `discovery-scout` from `.aid/generated/project-index.md` (1077 files,
> 148,068 lines, snapshot 2026-05-27). All counts are authoritative as of that snapshot.

## Repository Overview

| Aspect | Value | Evidence |
|--------|-------|----------|
| Repo root | `C:\Projects\Personal\AID` | working directory |
| VCS | Git, branch `kb-overhaul` | `.git/` directory, git status |
| Total files | 1,077 | `.aid/generated/project-index.md:10` |
| Total lines | 148,068 | `.aid/generated/project-index.md:11` |
| Primary purpose | Methodology spec + multi-tool install bundles | `README.md:5-7` |
| Application code | **None** — ships docs, skills, agents, templates, scripts | `README.md §Overview`; `architecture.md §Project Type` |

## Top-Level Directory Tree (depth 3)

```
aid-methodology/                       (repo root)
├── .aid/                              ← runtime KB scaffold (gitignored except .heartbeat)
│   ├── .heartbeat/                    ← ephemeral subagent heartbeat files (gitignored, .gitignore:47)
│   ├── .temp/                         ← scratch
│   ├── generated/
│   │   └── project-index.md           ← pre-built file inventory, 1148 lines (built by build-project-index.sh)
│   ├── knowledge/                     ← KB output dir (this scout target)
│   │   └── STATE.md                   ← discovery state hub (FR2 area-state)
│   ├── templates/                     ← runtime template copies
│   └── settings.yml                   ← runtime AID settings
├── .claude/                           ← dogfood Claude Code install tree (this repo uses AID on itself)
│   ├── agents/                        ← 22 generated agent definitions
│   ├── recipes/                       ← 5 generated lite-path recipes + README
│   ├── scripts/                       ← generated helper scripts (config/, execute/, interview/, kb/, summarize/)
│   ├── skills/                        ← 10 AID skills + maintainer-only aid-generate
│   ├── templates/                     ← generated templates (knowledge-base/, knowledge-summary/, kb-authoring/, ...)
│   └── settings.json                  ← Claude Code permission config
├── canonical/                         ← SINGLE SOURCE OF TRUTH for all install-tree content
│   ├── EMISSION-MANIFEST.md           ← deletion-safety boundary spec (152 lines)
│   ├── agents/                        ← 22 agent dirs (AGENT.md + README.md per agent)
│   ├── recipes/                       ← 5 recipes + README (478 lines) + .gitkeep
│   ├── rules/                         ← cursor .mdc rule files
│   ├── scripts/                       ← helper scripts (mirror of .claude/scripts/)
│   ├── skills/                        ← 10 skill dirs (SKILL.md + references/ + README.md)
│   └── templates/                     ← knowledge-base, knowledge-summary, kb-authoring, recipe-template, etc.
├── profiles/                          ← generator output + per-tool TOML profiles
│   ├── claude-code.toml               ← profile config (64 lines)
│   ├── claude-code/                   ← generated tree mirroring .claude/ + CLAUDE.md + README.md + emission-manifest.jsonl
│   ├── codex.toml                     ← profile config (78 lines)
│   ├── codex/                         ← split layout: .codex/agents/ (TOML) + .agents/ (skills, scripts, recipes, templates)
│   ├── cursor.toml                    ← profile config (75 lines)
│   └── cursor/                        ← generated .cursor/ tree + AGENTS.md + README.md + emission-manifest.jsonl
├── methodology/
│   ├── aid-methodology.md             ← complete methodology spec (1,070 lines) — the load-bearing artifact
│   └── images/                        ← diagrams (2-comparison.png, 3-ironman.png)
├── examples/                          ← case studies (brownfield-enterprise, data-pipeline, desktop-app)
├── docs/                              ← FAQ + glossary
├── tests/
│   └── canonical/                     ← 5 helper-script test suites (parse-recipe, writeback-task-status, ...)
├── README.md                          ← project pitch (388 lines)
├── CLAUDE.md                          ← Claude Code project-context (25 lines, dogfood)
├── CONTRIBUTING.md                    ← contribution guide (129 lines)
├── LICENSE                            ← MIT (21 lines)
├── run_generator.py                   ← live generator entrypoint (87 lines)
├── setup.sh                           ← end-user installer (Bash, 162 lines)
└── setup.ps1                          ← end-user installer (PowerShell, 157 lines)
```

## Detected Languages

| Language | Files | Lines | Where |
|----------|-------|-------|-------|
| Markdown | 872 | 97,689 | All docs, skills, agents, templates, recipes |
| Shell | 109 | 30,887 | `canonical/scripts/` + 3 install-tree mirrors + `tests/canonical/` + `setup.sh` |
| JavaScript | 20 | 5,685 | `canonical/templates/knowledge-summary/lightbox.js`, `mermaid-init.js`, `canonical/scripts/summarize/{validate-diagrams,contrast-check}.mjs` + 3 install-tree mirrors |
| Python | 11 | 4,232 | `.claude/skills/aid-generate/scripts/{harness,profile,render_*,verify_*,test_manifest_safety}.py` + `run_generator.py` |
| CSS | 5 | 3,285 | `canonical/templates/knowledge-summary/component-css.css` (657 lines) + 4 mirrors |
| TOML | 25 | 2,888 | 3 profile `*.toml` files + 22 Codex agent `.toml` files (`profiles/codex/.codex/agents/*.toml`) |
| HTML | 5 | 505 | `canonical/templates/knowledge-summary/html-skeleton.html` (101 lines) + 4 mirrors |
| YAML | 5 | 405 | `canonical/templates/settings.yml` (81 lines) + 4 mirrors |
| PowerShell | 6 | 337 | `setup.ps1` + `canonical/scripts/summarize/concatenate.ps1` + 4 install-tree mirrors |
| JSON | 2 | 28 | `.claude/settings.json` (15 lines), `.claude/settings.local.json` (13 lines) |
| Other | 17 | 2,127 | `LICENSE`, `.gitignore`, `*.mdc` cursor rules, `generated-files.txt`, `emission-manifest.jsonl`, `methodology/images/*.png` |

Source: `.aid/generated/project-index.md:17-29` (Language Breakdown table).

## Languages NOT Present

- No Java, C#, Go, Rust, C++, Ruby, PHP, Swift, Kotlin source files.
- No SQL files.
- No XML configuration files (no Maven, no .NET .csproj).
- No package manifests for typical application languages (no `package.json`, no `pyproject.toml`, no `Cargo.toml`, no `go.mod`, no `requirements.txt`, no `pom.xml`).
- Inferred from code — needs confirmation: The lack of language manifests confirms there is no shipped application — Python is used only by the maintainer-only generator harness.

## Key Files and Their Purpose

### Repo-root entrypoints

| File | Lines | Purpose | Evidence |
|------|-------|---------|----------|
| `README.md` | 388 | Project pitch, pipeline diagram, KB description, install instructions | `README.md:1-388` |
| `CLAUDE.md` | 25 | Dogfood Claude Code project context — minimal pointer to KB | `CLAUDE.md:1-25` |
| `CONTRIBUTING.md` | 129 | Repo structure table, contribution rules, style guide | `CONTRIBUTING.md:1-129` |
| `LICENSE` | 21 | MIT license | `LICENSE` |
| `run_generator.py` | 87 | Top-level generator entrypoint — loads every `profiles/*.toml`, calls renderers, runs VERIFY-4a (strict) + VERIFY-4b (advisory) | `run_generator.py:1-87` |
| `setup.sh` | 162 | Bash end-user installer (target: real projects, not maintainer) | `setup.sh` |
| `setup.ps1` | 157 | PowerShell end-user installer | `setup.ps1` |
| `.gitignore` | 47 | Excludes Python/Node caches, IDE, `.aid/knowledge/.cache/`, `.claude/worktrees/`, `.aid/.heartbeat/` | `.gitignore:1-47` |

### Methodology spec

| File | Lines | Purpose |
|------|-------|---------|
| `methodology/aid-methodology.md` | **1,070** | The methodology document. The load-bearing artifact — every skill/agent is derived from it. |
| `methodology/images/2-comparison.png` | (binary) | SDD vs AID comparison diagram |
| `methodology/images/3-ironman.png` | (binary) | Human-AI collaboration model |

### Generator (maintainer-only, lives in `.claude/skills/aid-generate/scripts/`)

| File | Lines | Purpose |
|------|-------|---------|
| `harness.py` | 756 | Largest source file. Emission-manifest implementation + pure-mirror deletion logic |
| `profile.py` | 550 | Parses `profiles/*.toml`, validates against schema |
| `render_agents.py` | 522 | Renders `canonical/agents/` per profile |
| `render_skills.py` | 469 | Renders `canonical/skills/` per profile (Thin-Router + references/) |
| `render_recipes.py` | 261 | Renders `canonical/recipes/` (passthrough) |
| `render_scripts.py` | 224 | Renders `canonical/scripts/` per profile |
| `render_templates.py` | 252 | Renders `canonical/templates/` per profile |
| `verify_deterministic.py` | 515 | VERIFY-4a — byte-identical re-run guarantee |
| `verify_advisory.py` | 343 | VERIFY-4b — advisory checks |
| `test_manifest_safety.py` | 254 | Generator self-tests |

### Largest helper scripts (canonical, with 4 mirror copies each)

| Canonical path | Lines | Purpose |
|----------------|-------|---------|
| `canonical/scripts/execute/writeback-task-status.sh` | 627 | Updates STATE.md after task completion |
| `canonical/scripts/summarize/validate-diagrams.mjs` | 574 | Mermaid diagram validation |
| `canonical/scripts/summarize/run-validators.sh` | 518 | Aggregates summarize-phase validators |
| `canonical/scripts/interview/parse-recipe.sh` | 540 | Parses recipe `{{slot}}` placeholders + emits lite-path artifacts |
| `canonical/scripts/summarize/validate-html-output.sh` | 321 | HTML output validation |
| `canonical/scripts/execute/compute-block-radius.sh` | 293 | BFS over task dependency graph (failure block radius) |
| `canonical/scripts/summarize/manual-checklist.sh` | 269 | Manual verification prompts |
| `canonical/scripts/config/read-setting.sh` | 263 | Reads `.aid/settings.yml` keys |
| `canonical/scripts/execute/complexity-score.sh` | 209 | Task complexity scoring |
| `canonical/scripts/kb/build-metrics.sh` | 213 | KB doc-size metrics |
| `canonical/scripts/kb/build-index.sh` | 203 | Builds `.aid/knowledge/INDEX.md` |
| `canonical/scripts/kb/build-project-index.sh` | 368 | Builds `.aid/generated/project-index.md` (this scout input) |
| `canonical/scripts/grade.sh` | 141 | Deterministic grading function (severity → letter grade) |
| `canonical/scripts/summarize/spot-check-facts.sh` | 176 | Spot-checks KB facts against source |
| `canonical/scripts/summarize/writeback-state.sh` | 173 | Writes summarize-phase state back |
| `canonical/scripts/summarize/contrast-check.mjs` | 151 | Color-contrast accessibility checker |
| `canonical/scripts/summarize/stale-check.sh` | 107 | Detects stale KB sections |
| `canonical/scripts/summarize/preflight.sh` | 101 | Summarize preflight check |
| `canonical/scripts/summarize/fetch-mermaid.sh` | 77 | Fetches Mermaid CLI assets |
| `canonical/scripts/kb/preflight.sh` | 46 | KB pipeline preflight |

Each script above has 4 byte-identical copies on disk: the canonical source, the dogfood `.claude/scripts/` copy, plus copies in each of the 3 profile install trees.

### Test files

| Path | Lines | Purpose |
|------|-------|---------|
| `tests/canonical/parse-recipe.sh` | 1,002 | Largest test file. 113 tests for `parse-recipe.sh` (runtime ~150s — needs timeout ≥180s) |
| `tests/canonical/writeback-task-status.sh` | 535 | 69 tests for `writeback-task-status.sh` |
| `tests/canonical/delivery-gate-aggregate.sh` | 535 | 18 tests for the delivery-gate aggregator |
| `tests/canonical/compute-block-radius.sh` | 345 | 17 tests for BFS block-radius |
| `tests/canonical/read-setting.sh` | 360 | `read-setting.sh` test suite |
| `tests/README.md` | — | Suite inventory + run instructions |

Tests `tests/skills/` (skill-level e2e) and `tests/canonical/pool-dispatch.sh` were deleted in cycle-1 per Q6 resolution. See `tests/README.md` for the current suite list.

### Examples

| Path | Purpose |
|------|---------|
| `examples/brownfield-enterprise/` | Brownfield case (README + discovery-report + knowledge-base/architecture.md) |
| `examples/data-pipeline/` | Multi-agent data pipeline case (README + pipeline-architecture.md) |
| `examples/desktop-app/` | Greenfield desktop app case (README + delivery-plan + task-spec) |

### Documentation

| Path | Lines | Purpose |
|------|-------|---------|
| `docs/faq.md` | 61 | FAQ |
| `docs/glossary.md` | 76 | Term definitions |

## Configuration Files

| File | Type | Purpose | Evidence |
|------|------|---------|----------|
| `profiles/claude-code.toml` | TOML profile | Generator config: output_root, frontmatter schema, model tiers, filename_map, capabilities | `profiles/claude-code.toml:1-65` |
| `profiles/codex.toml` | TOML profile | Same — Codex CLI (split-root: `.codex/agents/` for TOML, `.agents/{skills,scripts,recipes,templates}` for markdown) | `profiles/codex.toml`, `CONTRIBUTING.md:14-18` |
| `profiles/cursor.toml` | TOML profile | Same — Cursor IDE (`.cursor/`, `.mdc` rule files) | `profiles/cursor.toml` |
| `.claude/settings.json` | JSON | Claude Code permission config (15 lines) | `.claude/settings.json` |
| `.claude/settings.local.json` | JSON | Personal Claude Code overrides (13 lines, gitignored per `.gitignore:44`) | `.claude/settings.local.json` |
| `.aid/settings.yml` | YAML | Runtime AID settings (mirrors `canonical/templates/settings.yml`) | `.aid/settings.yml` |
| `canonical/templates/settings.yml` | YAML | Settings template (81 lines) shipped via render | `canonical/templates/settings.yml` |

## Build & Test System

This repo has NO application build (see `architecture.md §Project Type`):
> Build = render canonical/ → 3 profile trees. Test = verify the render is correct +
> run the canonical helper test suites.

The build commands:

| Command | Purpose |
|---------|---------|
| `python run_generator.py` | Re-generate all 3 install trees from `canonical/` (maintainer-only) |
| `python .claude/skills/aid-generate/scripts/verify_deterministic.py` | Verify render is byte-correct + complete |
| `bash canonical/scripts/kb/build-project-index.sh --root . --output .aid/generated/project-index.md` | Rebuild file inventory |
| `bash tests/canonical/*.sh` | Run canonical helper test suites (5 suites — see `tests/README.md`) |

**KB claim validation** is performed by the `discovery-reviewer` sub-agent in `/aid-discover REVIEW`.

Quality gates are the AID skills themselves — `aid-discover` adversarial review, `aid-execute` two-tier review per task + per delivery, `aid-deploy` verification step.

## Skills (10)

Installed at `.claude/skills/`. Each has a `SKILL.md` Thin-Router (≤~360 lines) plus a `references/state-*.md` per state.

| Skill | SKILL.md lines | Reference files | Purpose |
|-------|----------------|-----------------|---------|
| `aid-config` | 176 | 0 | Setup — once per project |
| `aid-discover` | 308 | 8 | Brownfield KB scan (agent-prompts, document-expectations, reviewer-{brief,prompt}, state-{approval,done,fix,generate,q-and-a,review}) |
| `aid-interview` | 357 | 19 | Requirements gathering + triage to lite-path (largest reference set) |
| `aid-specify` | 207 | 9 | Technical spec per feature |
| `aid-plan` | 208 | 3 | Sequence features into shippable deliveries |
| `aid-detail` | 77 | 5 | Decompose into PR-sized typed tasks |
| `aid-execute` | 279 | 8 | Implement + two-tier review |
| `aid-deploy` | 147 | 5 | Ship delivery + PR |
| `aid-monitor` | 224 | 3 | Production-finding classification |
| `aid-summarize` | 233 | 10 | Optional offline HTML KB viewer |
| `aid-generate` (maintainer-only) | 261 | n/a — uses `scripts/*.py` | Render canonical → 3 profile trees |

Total skill body lines: 2,242 across 10 skills (per `metrics.md`).

## Agents (22)

Installed at `.claude/agents/`. Three tiers (per `README.md:178-198`):

| Tier | Count | Names |
|------|-------|-------|
| Large (Opus / GPT-5.5 high) | 10 | architect, reviewer, interviewer, security, discovery-{scout, architect, analyst, integrator, quality, reviewer} |
| Medium (Sonnet / GPT-5.4 medium) | 9 | orchestrator, researcher, developer, operator, data-engineer, performance, devops, tech-writer, ux-designer |
| Small (Haiku / GPT-5.4-mini) | 3 | simple-extractor, simple-formatter, simple-glob |

Largest agent definitions: `discovery-reviewer.md` (387), `discovery-architect.md` (224), `discovery-quality.md` (197), `discovery-scout.md` (162) — discovery sub-agents have the densest contracts.

## Recipes (5)

Lite-path templates at `canonical/recipes/`, replicated into all 3 install trees (`.claude/recipes/`, `profiles/codex/.agents/recipes/`, `profiles/cursor/.cursor/recipes/`):

| Recipe | Lines | Purpose |
|--------|-------|---------|
| `add-crud-endpoint.md` | 109 | Add a new CRUD HTTP endpoint |
| `add-unit-test.md` | 75 | Add a single unit test |
| `bug-fix.md` | 65 | Minimal bug-fix lite path |
| `method-refactor.md` | 74 | Refactor a single method |
| `write-release-note.md` | 71 | Author a release note |
| `README.md` | 478 | Recipe catalog documentation |

## Templates (categories under `canonical/templates/`)

| Category | Files | Notable items |
|----------|-------|---------------|
| `delivery-plans/` | 1 | `task-template.md` |
| `feedback-artifacts/` | 1 | `IMPEDIMENT.md` (116 lines) — escalation contract |
| `kb-authoring/` | 5 | README, frontmatter-schema, principles, review-rubric, tier-model |
| `knowledge-base/` | 15 | Templates for the 14 standard-set KB docs (pipeline-contracts, architecture, coding-standards, schemas, domain-glossary, external-sources, feature-inventory, infrastructure, integration-map, module-map, project-structure, tech-debt, technology-stack, test-landscape) + README. The repo's 15th active doc — custom `repo-presentation.md` (replaces ui-architecture per Q3) — has no template here; it's a per-project customization, not a standard template (per Q16 methodology-flex tech-debt H5) |
| `knowledge-summary/` | 19+ | HTML/CSS/JS for offline KB viewer (component-css.css 657 lines, lightbox.js 359 lines, html-skeleton.html, mermaid-init.js, design-tokens, mermaid-examples, prompt, plus 6 section-templates: auto-detect, cli, data-pipeline, library, microservices, web-app + accessibility-checklist + grading-rubric) |
| `requirements/` | 1 | `requirements-template.md` |
| `specs/` | 2 | `lite-spec-template.md`, `spec-template.md` |
| Top-level templates | 18 | discovery-state-template.md, dispatch-protocol-checklist.md, feature.md, feature-inventory.md, generated-files.txt, grading-rubric.md, known-issues.md, long-wait-protocol.md, package.md, recipe-template.md, requirements.md, reviewer-dispatch.md (296 lines), rough-time-hints.md, self-review-protocol.md, settings.yml, subagent-heartbeat-protocol.md (176 lines), work-state-template.md, delivery-issues.md |

## Entry Points

| Audience | Entry point | Where it goes |
|----------|-------------|---------------|
| Maintainer build | `python run_generator.py` | Renders all 3 install trees + runs VERIFY-4a/4b |
| End-user install | `./setup.sh` (Unix) / `.\setup.ps1` (Windows) | Installs selected profile(s) into a target project |
| Methodology reader | `methodology/aid-methodology.md` (1,070 lines) | The complete spec |
| First-time AI agent reader | `CLAUDE.md` (dogfood) or `AGENTS.md` (per profile) | Project context, KB pointers |
| End-user runtime | Slash commands `/aid-config`, `/aid-discover`, ... | One per skill |

## Unusual Structure Notes

1. **Quadruple mirror.** The 109 shell scripts include only ~21 unique canonical scripts — each is duplicated 4x (canonical + .claude + 3 profiles). Similarly for markdown skill/agent/template files. The renderers enforce byte-identical output. This is by design (per `coding-standards.md §7a`) but inflates file counts and makes the project look larger than it is.

2. **Dogfood `.claude/` tree.** Unique among AID-using projects: this repo applies AID to itself. The top-level `.claude/` is functionally identical to `profiles/claude-code/.claude/` (the install bundle) but is the one the dev environment actually uses.

3. **Split Codex layout.** `profiles/codex/.codex/agents/` holds TOML agent definitions; `profiles/codex/.agents/{skills,scripts,recipes,templates}/` holds the markdown bodies. A single `profiles/codex/emission-manifest.jsonl` covers both roots (per `canonical/EMISSION-MANIFEST.md:20-27`).

4. **Generator lives in `.claude/skills/aid-generate/`, not in repo root.** It is a skill that ships with itself; `run_generator.py` is a tiny wrapper that imports its scripts (`run_generator.py:7-15`).

5. **`canonical/scripts/` was newly reorganized.** Git status shows many recent renames from `canonical/templates/scripts/` and `canonical/skills/*/scripts/` into the consolidated `canonical/scripts/{config,execute,interview,kb,summarize}/` hierarchy.

6. **`tests/canonical/` is the official test directory.** No `pytest.ini`, no `package.json`-style test config; tests are pure bash scripts; 5 suites (see `tests/README.md`). The `tests/skills/` directory and `tests/canonical/pool-dispatch.sh` were deleted in cycle-1 per Q6 resolution.

7. **No CI.** See `tech-debt.md H2`. No `.github/`, no `.gitlab-ci.yml`, no `Jenkinsfile`, no `azure-pipelines.yml` in the project index.

8. **`.aid/work-001-aid-lite/` and `.aid/work-002-canonical-generator/`** are correctly absent — per Q1 and Q2 resolutions (cycle-1): those directories were never correct canonical homes. See `tech-debt.md H1` and `M1` for history.

10. **`canonical/rules/` (2 .mdc files)** appears to exist only as a staging area; the rendered copies land in `profiles/cursor/.cursor/rules/`. There is no `.claude/rules/` equivalent (Claude Code uses inline `CLAUDE.md` for rules — see `profiles/claude-code.toml:55-57`).

11. **Knowledge-summary asset bundle is the heaviest static-resource cluster.** A single template subdirectory (`canonical/templates/knowledge-summary/`) holds 657-line CSS, 359-line JS, 101-line HTML, 6 section-template variants, and an accessibility checklist — atypical for a methodology repo and reflects the optional `aid-summarize` offline viewer feature.

12. **No `node_modules/` despite Node 18+ being a runtime requirement.** Per `README.md:326`, Node is only used by `aid-summarize` for diagram validation; presumably end users install Mermaid CLI ad-hoc. No `package.json` exists at any level.
