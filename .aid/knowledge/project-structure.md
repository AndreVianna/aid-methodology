---
kb-category: primary
source: generated
generator: build-project-index.sh
intent: |
  Repository inventory for the AID-methodology repo. Covers the top-level folder layout
  (canonical/, profiles/, .claude/, .aid/, methodology/, tests/), entry points (run_generator.py,
  setup.sh, setup.ps1), and key config files (settings.yml, EMISSION-MANIFEST.md, profile
  TOML files). T1/T2 facts only — file/line counts (T3) live in .aid/generated/project-index.md.
  Read this for a navigational map before diving into any subtree.
contracts:
  - "File/line counts are NOT maintained here (T3 metrics, banned per coding-standards.md §9a). Current counts live in `.aid/generated/project-index.md`, regenerated by `build-project-index.sh`."
changelog:
  - 2026-05-27: Initial frontmatter added during cycle-1 FIX Phase B
  - 2026-06-01: Post-merge update for work-001-add-providers (PRs #42/#43/#44) — 3 install trees → 5 (added copilot-cli, antigravity); generator scripts 10→12 .py; reconciled canonical-test-suite count to 18; mirror multiplier 4→6 byte-identity trees (7 physical copies incl. dogfood `.claude/`).
  - 2026-06-03: Targeted re-discovery for work-001-aid-housekeep (PR #49) — added optional `aid-housekeep` skill (canonical skill count 10→11), new `canonical/scripts/housekeep/` dir (branch-commit.sh, cleanup-classify.sh, housekeep-state.sh), and 5 new `tests/canonical/test-housekeep-*.sh` suites (canonical-suite count 18→24). aid-generate remains the maintainer-only `.claude/`-only skill (not counted in the canonical 11).
  - 2026-06-03: §9a T3-count strip — removed all hardcoded file/line counts (global snapshot, language breakdown numbers, per-skill SKILL.md line counts, per-script/per-suite line counts, individual file line counts). Replaced global snapshot with pointer to `.aid/generated/project-index.md`. Replaced stale Windows repo-root path and hardcoded branch name with durable references.
---
# Project Structure

> Repository inventory for AID (AI Integrated Development) — the methodology
> specification and multi-tool distribution that ships skills/agents/templates/recipes
> into 5 install trees (Claude Code, Codex CLI, Cursor, GitHub Copilot CLI, Antigravity) from a single `canonical/` source.
>
> Generated by `discovery-scout` from `.aid/generated/project-index.md`. For current
> file/line counts see that file (regenerated by `bash canonical/scripts/kb/build-project-index.sh`);
> counts are not maintained inline here per `coding-standards.md §9a`.

## Repository Overview

| Aspect | Value | Evidence |
|--------|-------|----------|
| Repo root | environment-specific (check `git rev-parse --show-toplevel`) | working directory |
| VCS | Git, current working branch (`git branch --show-current`) | `.git/` directory, git status |
| Total files | see `.aid/generated/project-index.md` `Total files` row | regenerated by `build-project-index.sh` |
| Total lines | see `.aid/generated/project-index.md` `Total lines` row | regenerated by `build-project-index.sh` |
| Primary purpose | Methodology spec + multi-tool install bundles | `README.md` `the methodology for the rest of the lifecycle` |
| Application code | **None** — ships docs, skills, agents, templates, scripts | `README.md §Overview`; `architecture.md §Project Type` |

## Top-Level Directory Tree (depth 3)

```
aid-methodology/                       (repo root)
├── .aid/                              ← runtime KB scaffold (gitignored except .heartbeat)
│   ├── .heartbeat/                    ← ephemeral subagent heartbeat files (gitignored, `.gitignore` `.aid/.heartbeat/`)
│   ├── .temp/                         ← scratch
│   ├── generated/
│   │   └── project-index.md           ← pre-built file inventory (built by build-project-index.sh)
│   ├── knowledge/                     ← KB output dir (this scout target)
│   │   └── STATE.md                   ← discovery state hub (FR2 area-state)
│   ├── templates/                     ← runtime template copies
│   └── settings.yml                   ← runtime AID settings
├── .claude/                           ← dogfood Claude Code install tree (this repo uses AID on itself)
│   ├── agents/                        ← 22 generated agent definitions
│   ├── recipes/                       ← 5 generated lite-path recipes + README
│   ├── scripts/                       ← generated helper scripts (config/, execute/, housekeep/, interview/, kb/, summarize/)
│   ├── skills/                        ← 11 AID skills + maintainer-only aid-generate
│   ├── templates/                     ← generated templates (knowledge-base/, knowledge-summary/, kb-authoring/, ...)
│   └── settings.json                  ← Claude Code permission config
├── canonical/                         ← SINGLE SOURCE OF TRUTH for all install-tree content
│   ├── EMISSION-MANIFEST.md           ← deletion-safety boundary spec
│   ├── agents/                        ← 22 agent dirs (AGENT.md + README.md per agent)
│   ├── recipes/                       ← 5 recipes + README + .gitkeep
│   ├── rules/                         ← cursor .mdc rule files
│   ├── scripts/                       ← helper scripts (mirror of .claude/scripts/): config/, execute/, housekeep/, interview/, kb/, summarize/
│   │   └── housekeep/                 ← branch-commit.sh, cleanup-classify.sh, housekeep-state.sh (added work-001-aid-housekeep, PR #49)
│   ├── skills/                        ← 11 skill dirs (SKILL.md + references/ + README.md)
│   │   └── aid-housekeep/             ← optional skill: SKILL.md + references/ (5 state-*.md) (added PR #49)
│   └── templates/                     ← knowledge-base, knowledge-summary, kb-authoring, recipe-template, etc.
├── profiles/                          ← generator output + per-tool TOML profiles (5 profiles)
│   ├── antigravity.toml               ← profile config — Google Antigravity (Windsurf lineage)
│   ├── antigravity/                   ← generated .agent/ tree (output_root `.agent`): rules/ (sub-agents reshaped to trigger:-frontmatter rules) + skills/ + scripts/ + recipes/ + templates/ + AGENTS.md
│   ├── claude-code.toml               ← profile config
│   ├── claude-code/                   ← generated tree mirroring .claude/ + CLAUDE.md + README.md + emission-manifest.jsonl
│   ├── codex.toml                     ← profile config
│   ├── codex/                         ← split layout: .codex/agents/ (TOML) + .agents/ (skills, scripts, recipes, templates)
│   ├── copilot-cli.toml               ← profile config — GitHub Copilot CLI
│   ├── copilot-cli/                   ← generated .github/ tree (output_root `.github`): agents/*.agent.md (copilot-agent format) + skills/ + scripts/ + recipes/ + templates/ + AGENTS.md
│   ├── cursor.toml                    ← profile config
│   └── cursor/                        ← generated .cursor/ tree + AGENTS.md + README.md + emission-manifest.jsonl
├── methodology/
│   ├── aid-methodology.md             ← complete methodology spec — the load-bearing artifact
│   └── images/                        ← diagrams (2-comparison.png, 3-ironman.png)
├── examples/                          ← case studies (brownfield-enterprise, data-pipeline, desktop-app)
├── docs/                              ← FAQ + glossary
├── tests/
│   ├── canonical/                     ← currently 24 helper-script test suites (parse-recipe, writeback-state, housekeep-*, ...)
│   │   ├── test-housekeep-branch-commit.sh      ← housekeep branch/commit suite (added PR #49)
│   │   ├── test-housekeep-classify.sh           ← housekeep cleanup-classify suite (added PR #49)
│   │   ├── test-housekeep-deletion-split.sh     ← housekeep deletion-split suite (added PR #49)
│   │   ├── test-housekeep-state.sh              ← housekeep-state.sh suite (added PR #49)
│   │   └── test-housekeep-workfolder-safety.sh  ← work-folder safety suite (added PR #49)
│   ├── lib/                            ← shared test helpers (assert.sh)
│   └── run-all.sh                      ← runs every canonical suite
├── README.md                          ← project pitch
├── CLAUDE.md                          ← Claude Code project-context (dogfood)
├── CONTRIBUTING.md                    ← contribution guide
├── LICENSE                            ← MIT
├── run_generator.py                   ← live generator entrypoint
├── setup.sh                           ← end-user installer (Bash)
└── setup.ps1                          ← end-user installer (PowerShell)
```

## Detected Languages

File/line counts by language are T3 metrics and are not maintained inline here (per `coding-standards.md §9a`). Current breakdown lives in `.aid/generated/project-index.md` `## Language Breakdown`, regenerated by `bash canonical/scripts/kb/build-project-index.sh`.

Languages present (where, not counts):

| Language | Where |
|----------|-------|
| Markdown | All docs, skills, agents, templates, recipes |
| Shell | `canonical/scripts/` + 5 install-tree mirrors + `tests/canonical/` + `setup.sh` |
| JavaScript | `canonical/templates/knowledge-summary/lightbox.js`, `mermaid-init.js`, `canonical/scripts/summarize/{validate-diagrams,contrast-check}.mjs` + 5 install-tree mirrors |
| Python | `.claude/skills/aid-generate/scripts/*.py` (12 files incl. the two generator self-tests `test_copilot_emitter.py` + `test_antigravity_emitter.py`) + `run_generator.py` |
| CSS | `canonical/templates/knowledge-summary/component-css.css` + 6 mirrors (.claude + 5 profile trees) |
| TOML | 5 profile `*.toml` files (`profiles/*.toml`) + 22 Codex agent `.toml` files (`profiles/codex/.codex/agents/*.toml`) |
| HTML | `canonical/templates/knowledge-summary/html-skeleton.html` + 6 mirrors |
| YAML | `canonical/templates/settings.yml` + 6 mirrors |
| PowerShell | `setup.ps1` + `canonical/scripts/summarize/assemble-3part.ps1` + 6 install-tree mirrors |
| JSON | `.claude/settings.json`, `.claude/settings.local.json` |
| Other | `LICENSE`, `.gitignore`, `*.mdc` cursor rules, `generated-files.txt`, `emission-manifest.jsonl`, `methodology/images/*.png` |

## Languages NOT Present

- No Java, C#, Go, Rust, C++, Ruby, PHP, Swift, Kotlin source files.
- No SQL files.
- No XML configuration files (no Maven, no .NET .csproj).
- No package manifests for typical application languages (no `package.json`, no `pyproject.toml`, no `Cargo.toml`, no `go.mod`, no `requirements.txt`, no `pom.xml`).
- Inferred from code — needs confirmation: The lack of language manifests confirms there is no shipped application — Python is used only by the maintainer-only generator harness.

## Key Files and Their Purpose

### Repo-root entrypoints

| File | Purpose | Evidence |
|------|---------|----------|
| `README.md` | Project pitch, pipeline diagram, KB description, install instructions | `README.md` `# AID — AI Integrated Development` |
| `CLAUDE.md` | Dogfood Claude Code project context — minimal pointer to KB | `CLAUDE.md` `# CLAUDE.md` |
| `CONTRIBUTING.md` | Repo structure table, contribution rules, style guide | `CONTRIBUTING.md` `# Contributing to AID` |
| `LICENSE` | MIT license | `LICENSE` |
| `run_generator.py` | Top-level generator entrypoint — loads every `profiles/*.toml` (5 profiles), calls renderers, runs VERIFY (deterministic, strict) + VERIFY (advisory) | `run_generator.py` `Running VERIFY (deterministic)...` |
| `setup.sh` | Bash end-user installer (target: real projects, not maintainer); interactive 5-tool menu (`setup.sh` `tool_name()`) + Option-A AGENTS.md collision handler (`setup.sh` `AGENTS.md collision pre-copy block`) | `setup.sh` |
| `setup.ps1` | PowerShell end-user installer (same 5-tool menu + collision behavior) | `setup.ps1` |
| `.gitignore` | Excludes Python/Node caches, IDE, `.aid/knowledge/.cache/`, `.claude/worktrees/`, `.aid/.heartbeat/` | `.gitignore` `.aid/.heartbeat/` |

### Methodology spec

| File | Purpose |
|------|---------|
| `methodology/aid-methodology.md` | The methodology document. The load-bearing artifact — every skill/agent is derived from it. |
| `methodology/images/2-comparison.png` | SDD vs AID comparison diagram |
| `methodology/images/3-ironman.png` | Human-AI collaboration model |

### Generator (maintainer-only, lives in `.claude/skills/aid-generate/scripts/`)

12 Python files under `.claude/skills/aid-generate/scripts/` (10 renderers/verifiers + 2 emitter self-tests added by work-001); with the top-level `run_generator.py` the project has 13 Python files total. The supported `[agent].format` values are now four — `markdown`, `toml`, `copilot-agent`, `antigravity-rule` (`aid_profile.py` `_KNOWN_AGENT_FORMATS`).

| File | Purpose |
|------|---------|
| `render_lib.py` | Emission-manifest implementation + pure-mirror deletion logic |
| `aid_profile.py` | Parses `profiles/*.toml`, validates against schema |
| `render_agents.py` | Renders `canonical/agents/` per profile |
| `render_skills.py` | Renders `canonical/skills/` per profile (Thin-Router + references/) |
| `render_recipes.py` | Renders `canonical/recipes/` (passthrough) |
| `render_canonical_scripts.py` | Renders `canonical/scripts/` per profile |
| `render_templates.py` | Renders `canonical/templates/` per profile |
| `verify_deterministic.py` | VERIFY (deterministic) — byte-identical re-run guarantee |
| `verify_advisory.py` | VERIFY (advisory) — advisory checks |
| `test_manifest_safety.py` | Generator self-tests (manifest deletion-safety) |
| `test_copilot_emitter.py` | Generator self-test for the `copilot-agent` agent-format emitter (CI-wired, `.github/workflows/test.yml`; added work-001) |
| `test_antigravity_emitter.py` | Generator self-test for the `antigravity-rule` agent-format emitter (CI-wired, `.github/workflows/test.yml`; added work-001) |

### Helper scripts (canonical, with 6 additional mirror copies each — 7 on-disk total)

Each canonical script has **7** byte-identical copies on disk: the canonical source, the dogfood `.claude/scripts/` copy, plus a copy in each of the **5** profile install trees (claude-code, codex, cursor, copilot-cli, antigravity). Verified: `find . -name writeback-state.sh -path '*/execute/*'` returns 7 paths (canonical + .claude + 5 profiles). The three `canonical/scripts/housekeep/*.sh` scripts (branch-commit.sh, cleanup-classify.sh, housekeep-state.sh) follow the same 7-copy mirror discipline (added PR #49).

For per-file line counts see `.aid/generated/project-index.md` (T3 metrics, not maintained inline per `coding-standards.md §9a`).

| Canonical path | Purpose |
|----------------|---------|
| `canonical/scripts/config/read-setting.sh` | Reads `.aid/settings.yml` keys |
| `canonical/scripts/execute/writeback-state.sh` | Updates STATE.md after task completion |
| `canonical/scripts/execute/compute-block-radius.sh` | BFS over task dependency graph (failure block radius) |
| `canonical/scripts/execute/complexity-score.sh` | Task complexity scoring |
| `canonical/scripts/housekeep/branch-commit.sh` | Branch + commit helper for `aid-housekeep` (added PR #49) |
| `canonical/scripts/housekeep/cleanup-classify.sh` | Classifies work-folder artifacts for keep/delete during `aid-housekeep` cleanup (added PR #49) |
| `canonical/scripts/housekeep/housekeep-state.sh` | Reads/writes `aid-housekeep` state hub (added PR #49) |
| `canonical/scripts/interview/parse-recipe.sh` | Parses recipe `{{slot}}` placeholders + emits lite-path artifacts |
| `canonical/scripts/kb/build-kb-index.sh` | Builds `.aid/knowledge/INDEX.md` |
| `canonical/scripts/kb/build-metrics.sh` | KB doc-size metrics |
| `canonical/scripts/kb/build-project-index.sh` | Builds `.aid/generated/project-index.md` (this scout input) |
| `canonical/scripts/kb/discover-preflight.sh` | KB pipeline preflight |
| `canonical/scripts/grade.sh` | Deterministic grading function (severity → letter grade) |
| `canonical/scripts/summarize/contrast-check.mjs` | Color-contrast accessibility checker |
| `canonical/scripts/summarize/fetch-mermaid.sh` | Fetches Mermaid CLI assets |
| `canonical/scripts/summarize/grade-summary.sh` | Aggregates summarize-phase validators |
| `canonical/scripts/summarize/manual-checklist.sh` | Manual verification prompts |
| `canonical/scripts/summarize/spot-check-facts.sh` | Spot-checks KB facts against source |
| `canonical/scripts/summarize/stale-check.sh` | Detects stale KB sections |
| `canonical/scripts/summarize/summarize-preflight.sh` | Summarize preflight check |
| `canonical/scripts/summarize/validate-diagrams.mjs` | Mermaid diagram validation |
| `canonical/scripts/summarize/validate-html-output.sh` | HTML output validation |
| `canonical/scripts/summarize/writeback-state.sh` | Writes summarize-phase state back |

### Test files

There are **24** canonical test suites under `tests/canonical/` (see `tests/README.md` for the authoritative list), each a self-contained bash script with its own assertions, plus the aggregator `tests/run-all.sh` (discovers suites by glob `tests/canonical/test-*.sh`, `tests/run-all.sh` `suites=( tests/canonical/test-*.sh )`) and shared helpers in `tests/lib/assert.sh`. Per-suite line counts are T3 metrics — see `.aid/generated/project-index.md`.

| Path | Purpose |
|------|---------|
| `tests/canonical/test-parse-recipe.sh` | Covers `parse-recipe.sh` operating modes + error paths (runtime is the longest of the suites — give it a generous timeout) |
| `tests/canonical/test-delivery-gate-aggregate.sh` | Delivery-gate aggregator (`aid-execute` gate: deferred rows, SCORE, `grade.sh` determinism) |
| `tests/canonical/test-housekeep-workfolder-safety.sh` | `aid-housekeep` work-folder safety guarantees (added PR #49) |
| `tests/canonical/test-writeback-state.sh` | `writeback-state.sh` arg modes + lock-contention safety |
| `tests/canonical/test-read-setting.sh` | `read-setting.sh` 3-tier resolution + `--path` lookups |
| `tests/canonical/test-housekeep-branch-commit.sh` | `housekeep/branch-commit.sh` branch + commit flow (added PR #49) |
| `tests/canonical/test-housekeep-classify.sh` | `housekeep/cleanup-classify.sh` keep/delete classification (added PR #49) |
| `tests/canonical/test-housekeep-deletion-split.sh` | `aid-housekeep` deletion-split safety (added PR #49) |
| `tests/canonical/test-compute-block-radius.sh` | BFS transitive-descendant block-radius |
| `tests/canonical/test-housekeep-state.sh` | `housekeep/housekeep-state.sh` state read/write (added PR #49) |
| `tests/README.md` | Suite inventory + run instructions |

Remaining suites: `test-complexity-score.sh`, `test-grade.sh`, `test-fetch-mermaid.sh`, `test-validate-diagrams.sh` (needs `node`), `test-contrast-check.sh` (needs `node`), `test-assemble-3part.sh`, `test-assemble-3part-ps1.sh` (needs `pwsh`), `test-setup.sh`, `test-setup-ps1.sh` (needs `pwsh`), `test-discovery-doc-ownership.sh`, `test-doc-set-mapping.sh`, `test-doc-set-propose-confirm.sh`, `test-doc-set-read.sh`, `test-expectations-single-source.sh`. The full glob (`ls tests/canonical/test-*.sh`) currently lists 24 files = 19 non-housekeep + 5 new `test-housekeep-*.sh` from PR #49. `test-setup.sh` and `test-setup-ps1.sh` were extended by work-001 to exercise the new menu options 4–6 (Copilot CLI, Antigravity, Done).

Tests `tests/skills/` (skill-level e2e) and `tests/canonical/pool-dispatch.sh` were deleted in cycle-1 per Q6 resolution. See `tests/README.md` for the current suite list.

### Examples

| Path | Purpose |
|------|---------|
| `examples/brownfield-enterprise/` | Brownfield case (README + discovery-report + knowledge-base/architecture.md) |
| `examples/data-pipeline/` | Multi-agent data pipeline case (README + pipeline-architecture.md) |
| `examples/desktop-app/` | Greenfield desktop app case (README + delivery-plan + task-spec) |

### Documentation

| Path | Purpose |
|------|---------|
| `docs/faq.md` | FAQ |
| `docs/glossary.md` | Term definitions |

## Configuration Files

| File | Type | Purpose | Evidence |
|------|------|---------|----------|
| `profiles/claude-code.toml` | TOML profile | Generator config: output_root, frontmatter schema, model tiers, filename_map, capabilities | `profiles/claude-code.toml` `[layout]` |
| `profiles/codex.toml` | TOML profile | Same — Codex CLI (split-root: `.codex/agents/` for TOML, `.agents/{skills,scripts,recipes,templates}` for markdown) | `profiles/codex.toml`, `CONTRIBUTING.md` `## Repository Structure` |
| `profiles/cursor.toml` | TOML profile | Same — Cursor IDE (`.cursor/`, `.mdc` rule files) | `profiles/cursor.toml` |
| `profiles/copilot-cli.toml` | TOML profile | Same — GitHub Copilot CLI (output_root `.github`; `[agent].format = copilot-agent` → `.agent.md` sub-agents) | `profiles/copilot-cli.toml` `[agent]` |
| `profiles/antigravity.toml` | TOML profile | Same — Google Antigravity (output_root `.agent`; `[agent].format = antigravity-rule` → sub-agents reshaped to `rules/*.md`) | `profiles/antigravity.toml` `[agent]` |
| `.claude/settings.json` | JSON | Claude Code permission config | `.claude/settings.json` |
| `.claude/settings.local.json` | JSON | Personal Claude Code overrides (gitignored per `.gitignore` `.claude/settings.local.json`) | `.claude/settings.local.json` |
| `.aid/settings.yml` | YAML | Runtime AID settings (mirrors `canonical/templates/settings.yml`) | `.aid/settings.yml` |
| `canonical/templates/settings.yml` | YAML | Settings template shipped via render | `canonical/templates/settings.yml` |

## Build & Test System

This repo has NO application build (see `architecture.md §Project Type`):
> Build = render canonical/ → 5 profile trees. Test = verify the render is correct +
> run the canonical helper test suites.

The build commands:

| Command | Purpose |
|---------|---------|
| `python run_generator.py` | Re-generate all 5 install trees from `canonical/` (claude-code, codex, cursor, copilot-cli, antigravity; maintainer-only) |
| `python .claude/skills/aid-generate/scripts/verify_deterministic.py` | Verify render is byte-correct + complete |
| `bash canonical/scripts/kb/build-project-index.sh --root . --output .aid/generated/project-index.md` | Rebuild file inventory |
| `bash tests/run-all.sh` | Run all canonical helper test suites (currently 24 suites — aggregator globs `tests/canonical/test-*.sh`; see `tests/README.md`) |

**KB claim validation** is performed by the `discovery-reviewer` sub-agent in `/aid-discover REVIEW`.

Quality gates are the AID skills themselves — `aid-discover` adversarial review, `aid-execute` two-tier review per task + per delivery, `aid-deploy` verification step.

## Skills (11)

Installed at `.claude/skills/`. Each has a `SKILL.md` Thin-Router (≤~360 lines) plus a `references/state-*.md` per state. Per-file line counts are T3 metrics — see `.aid/generated/metrics.md`.

> Reference-file counts (T3) are not pinned here — list them with `ls canonical/skills/<skill>/references/`.

| Skill | In mandatory pipeline | Purpose |
|-------|----------------------|---------|
| `aid-config` | Yes | Setup — once per project (no `references/`) |
| `aid-discover` | Yes | Brownfield KB scan (discovery sub-agent prompts, document-expectations, reviewer brief/prompt, doc-set-resolve, per-state references) |
| `aid-interview` | Yes | Requirements gathering + triage to lite-path (largest reference set) |
| `aid-specify` | Yes | Technical spec per feature |
| `aid-plan` | Yes | Sequence features into shippable deliveries |
| `aid-detail` | Yes | Decompose into PR-sized typed tasks |
| `aid-execute` | Yes | Implement + two-tier review |
| `aid-deploy` | Yes | Ship delivery + PR |
| `aid-monitor` | Yes | Production-finding classification |
| `aid-summarize` | No (optional) | Optional offline HTML KB viewer |
| `aid-housekeep` | No (optional) | Optional work-folder cleanup + KB/summary reconciliation (per-state references) — added PR #49 |
| `aid-generate` (maintainer-only) | No (maintainer-only) | Render canonical → 5 profile trees (uses `scripts/*.py`, no `references/`) |

Total: **11** shipped AID skills (`ls -d canonical/skills/*/` = 11) + the maintainer-only `aid-generate` (which lives only in `.claude/skills/`, not in `canonical/skills/`, and is excluded from the 11).

## Agents (22)

Installed at `.claude/agents/`. Three tiers (per `README.md` `## The Agent Model — three tiers`):

| Tier | Count | Names |
|------|-------|-------|
| Large (Opus / GPT-5.5 high) | 10 | architect, reviewer, interviewer, security, discovery-{scout, architect, analyst, integrator, quality, reviewer} |
| Medium (Sonnet / GPT-5.4 medium) | 9 | orchestrator, researcher, developer, operator, data-engineer, performance, devops, tech-writer, ux-designer |
| Small (Haiku / GPT-5.4-mini) | 3 | simple-extractor, simple-formatter, simple-glob |

Densest agent definitions by purpose: `discovery-reviewer.md`, `discovery-architect.md`, `discovery-quality.md`, `discovery-scout.md` — discovery sub-agents have the densest contracts. Per-file line counts are in `.aid/generated/project-index.md`.

## Recipes (5)

Lite-path templates at `canonical/recipes/`, replicated into all 5 install trees (`.claude/recipes/`, `profiles/codex/.agents/recipes/`, `profiles/cursor/.cursor/recipes/`, `profiles/copilot-cli/.github/recipes/`, `profiles/antigravity/.agent/recipes/`):

| Recipe | Purpose |
|--------|---------|
| `add-crud-endpoint.md` | Add a new CRUD HTTP endpoint |
| `add-unit-test.md` | Add a single unit test |
| `bug-fix.md` | Minimal bug-fix lite path |
| `method-refactor.md` | Refactor a single method |
| `write-release-note.md` | Author a release note |
| `README.md` | Recipe catalog documentation |

## Templates (categories under `canonical/templates/`)

| Category | Files | Notable items |
|----------|-------|---------------|
| `delivery-plans/` | 1 | `task-template.md` |
| `feedback-artifacts/` | 1 | `IMPEDIMENT.md` — escalation contract |
| `kb-authoring/` | 5 | README, frontmatter-schema, principles, review-rubric, tier-model |
| `knowledge-base/` | 15 | Templates for the 14 standard-set KB docs (pipeline-contracts, architecture, coding-standards, schemas, domain-glossary, external-sources, feature-inventory, infrastructure, integration-map, module-map, project-structure, tech-debt, technology-stack, test-landscape) + README. These templates form the default seed synthesized by `synth_default_seed` (delivery-002 resolved H5: doc-set is now project-configurable). This repo's custom `repo-presentation.md` has no template here — it is declared via `discovery.doc_set` as a per-project addition. |
| `knowledge-summary/` | 19+ | HTML/CSS/JS for offline KB viewer (component-css.css, lightbox.js, html-skeleton.html, mermaid-init.js, design-tokens, mermaid-examples, prompt, plus 6 section-templates: auto-detect, cli, data-pipeline, library, microservices, web-app + accessibility-checklist + grading-rubric) |
| `requirements/` | 1 | `requirements-template.md` |
| `specs/` | 2 | `lite-spec-template.md`, `spec-template.md` |
| Top-level templates | 18 | discovery-state-template.md, dispatch-protocol-checklist.md, feature.md, feature-inventory.md, generated-files.txt, grading-rubric.md, known-issues.md, long-wait-protocol.md, package.md, recipe-template.md, requirements.md, reviewer-dispatch.md, rough-time-hints.md, self-review-protocol.md, settings.yml, subagent-heartbeat-protocol.md, work-state-template.md, delivery-issues.md |

## Entry Points

| Audience | Entry point | Where it goes |
|----------|-------------|---------------|
| Maintainer build | `python run_generator.py` | Renders all 5 install trees + runs VERIFY (deterministic) / VERIFY (advisory) |
| End-user install | `./setup.sh` (Unix) / `.\setup.ps1` (Windows) | Installs selected profile(s) into a target project |
| Methodology reader | `methodology/aid-methodology.md` | The complete spec |
| First-time AI agent reader | `CLAUDE.md` (dogfood) or `AGENTS.md` (per profile) | Project context, KB pointers |
| End-user runtime | Slash commands `/aid-config`, `/aid-discover`, ... | One per skill |

## Unusual Structure Notes

1. **Multi-tree mirror (now 7 copies).** The shell scripts include only ~20–23 unique canonical scripts (`ls canonical/scripts/*/*.sh canonical/scripts/*.sh`; now includes the 3 `canonical/scripts/housekeep/*.sh`) — each exists in **7** byte-identical copies (canonical + dogfood `.claude/` + the 5 profile trees: claude-code, codex, cursor, copilot-cli, antigravity). Before the work-001-add-providers merge this was a quadruple mirror (canonical + .claude + 3 profiles); the two new profiles raised the multiplier to 6 extra copies. Similarly for markdown skill/agent/template files. The renderers enforce byte-identical output. This is by design (per `coding-standards.md §7a`) but inflates file counts and makes the project look larger than it is.

2. **Dogfood `.claude/` tree.** Unique among AID-using projects: this repo applies AID to itself. The top-level `.claude/` is functionally identical to `profiles/claude-code/.claude/` (the install bundle) but is the one the dev environment actually uses.

3. **Split Codex layout.** `profiles/codex/.codex/agents/` holds TOML agent definitions; `profiles/codex/.agents/{skills,scripts,recipes,templates}/` holds the markdown bodies. A single `profiles/codex/emission-manifest.jsonl` covers both roots (per `canonical/EMISSION-MANIFEST.md` `## Filename and Location`).

4. **Generator lives in `.claude/skills/aid-generate/`, not in repo root.** It is a skill that ships with itself; `run_generator.py` is a tiny wrapper that imports its scripts (`run_generator.py` `sys.path.insert(0, '.claude/skills/aid-generate/scripts')`).

5. **`canonical/scripts/` was newly reorganized.** Git status shows many recent renames from `canonical/templates/scripts/` and `canonical/skills/*/scripts/` into the consolidated `canonical/scripts/{config,execute,interview,kb,summarize}/` hierarchy; PR #49 added a sixth subdir, `canonical/scripts/housekeep/`.

6. **`tests/canonical/` is the official test directory.** No `pytest.ini`, no `package.json`-style test config; tests are pure bash scripts; currently 24 suites run via the `tests/run-all.sh` aggregator (shared helpers in `tests/lib/assert.sh`; recount with `ls tests/canonical/test-*.sh | wc -l`; see `tests/README.md`). PR #49 added the 5 `test-housekeep-*.sh` suites. The `tests/skills/` directory and `tests/canonical/pool-dispatch.sh` were deleted in cycle-1 per Q6 resolution.

7. **CI is enforced.** `.github/workflows/test.yml` (added 2026-05-29) runs on PR/push and is a required status check on `master`; no other CI system (`.gitlab-ci.yml`, `Jenkinsfile`, `azure-pipelines.yml`).

8. **`.aid/work-001-aid-lite/`** is correctly absent — per Q1/Q2 resolutions (cycle-1): never a correct canonical home (see `tech-debt.md H1`/`M1`). ⚠️ Note: a *different* effort, **work-001-add-providers** (PRs #42/#43/#44, merged 2026-06-01), added the copilot-cli + antigravity profiles and is the source of the 5-profile reality reflected throughout this doc; its work dir is not committed to the repo. A further effort, **work-001-aid-housekeep** (PR #49, merged 2026-06-03), added the optional `aid-housekeep` skill, `canonical/scripts/housekeep/`, and the 5 housekeep test suites; its work dir (`.aid/work-001-aid-housekeep/`) is in-flight during this re-discovery (git status shows `.aid/work-001-aid-housekeep/STATE.md` modified).

9. **Two non-dot-tool profile roots.** Unlike the `.claude`/`.codex`/`.cursor` roots, the work-001 profiles use distinct output roots: `profiles/copilot-cli/` emits under `.github/` (`profiles/copilot-cli.toml` `output_root`) with `.agent.md`-suffixed sub-agents (`copilot-agent` format), and `profiles/antigravity/` emits under `.agent/` (`profiles/antigravity.toml` `output_root`) with sub-agents reshaped into `rules/*.md` carrying `trigger:`-style frontmatter (`antigravity-rule` format). Both share `AGENTS.md` as their project-context file — see setup.sh Option-A collision handling (note 12).

10. **`canonical/rules/` (2 .mdc files)** appears to exist only as a staging area; the rendered copies land in `profiles/cursor/.cursor/rules/`. There is no `.claude/rules/` equivalent (Claude Code uses inline `CLAUDE.md` for rules — see `profiles/claude-code.toml` `[extras]`).

11. **Knowledge-summary asset bundle is the heaviest static-resource cluster.** A single template subdirectory (`canonical/templates/knowledge-summary/`) holds CSS, JS, HTML, 6 section-template variants, and an accessibility checklist — atypical for a methodology repo and reflects the optional `aid-summarize` offline viewer feature.

12. **Option-A AGENTS.md collision handler in setup.sh/ps1.** Codex (2), Cursor (3), Copilot CLI (4) and Antigravity (5) all write a root `AGENTS.md`. When ≥2 of these are selected, the installer warns once and the highest-numbered selected writer wins (fixed block order, no interactive prompt) — `setup.sh` `AGENTS.md collision pre-copy block (Option A)`.

13. **No `node_modules/` despite Node 18+ being a runtime requirement.** Per `README.md` `**Node 18+** is optional`, Node is only used by `aid-summarize` for diagram validation; presumably end users install Mermaid CLI ad-hoc. No `package.json` exists at any level.
