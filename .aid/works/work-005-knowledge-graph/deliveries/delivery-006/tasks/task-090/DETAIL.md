# task-090: Roster entries across the discoverability surfaces and the diagram contract

> **Execution protocol (binding on whoever executes this task -- no
> exceptions):** the moment this task's `State` changes, write it --
> `In Progress` before starting work, `In Review` before dispatching the
> reviewer, and the terminal value (`Done` / `Failed`) at the end -- via
> `writeback-state.sh --field State --value ...`. This applies equally
> whether the main/orchestrator agent executes this task directly or
> dispatches it to a sub-agent; neither may skip, batch, or defer these
> writes. (`Blocked` is a distinct, orchestrator-assigned value for a
> DIFFERENT, downstream task that depends on a failed one -- it is never
> self-written by the task being executed.) Full mandate:
> `aid-execute/references/state-execute.md § MANDATORY: State-Write
> Protocol`.

**Type:** DOCUMENT

**Source:** work-005-knowledge-graph -> delivery-006

**Depends on:** task-069, task-086

**Scope:**
- Add the **roster** entries for `/aid-graph` -- the prose-and-name half of feature-013 § L2's
  012/013 seam. A documentation edit whose reason is **a number** belongs to feature-012 and
  landed in delivery-002 (the eleven `${SKILLS}` count needles, task-012); this task owns the
  edits whose reason is **a roster entry or prose**, and must not touch a count.
- **The four `docs/` reference surfaces:** `docs/repository-structure.md`,
  `docs/aid-methodology.md`, `docs/glossary.md` and `docs/install.md`.
- **`docs/aid-methodology.md` needs a decision made, not guessed.** Its `### Skill Inventory`
  section partitions the toolkit its own way -- **A** the curated-skills table, **B** `/aid-triage`,
  **C** `/aid-ask`, and **D** the shortcut-catalog families -- and feature-013 § "Deliberately left
  open" states outright that which of them `/aid-graph` lands in "should be settled when the entry
  is written rather than guessed here". The site generator places it in **Knowledge Base
  Maintenance** beside `aid-summarize` (feature-012's `SKILL_GROUPS`), which is evidence but is a
  different partition from the document's own. **Read the tables, write a short proposal naming
  the table and the reason, and obtain the owner's confirmation before the entry is written.** The
  § 4 phase-mapping table is a fifth roster surface in the same document and takes the same
  decision.
- The same document's `G1 " Knowledge Base Maintenance "` Mermaid subgraph gains an `/aid-graph`
  node beside `aid-summarize`.
- **`README.md`:** the on-demand skill list beside `/aid-summarize`, and the node in the
  hand-authored `R1` Mermaid diagram. `R1` has no sync step -- `docs/diagram-content-reference.md`
  § R1 says "edit at the source".
- **`docs/diagram-content-reference.md` is a contract, not just a document.** Its maintenance rule
  is "if you add, remove, or relabel a diagram, update this reference in the same change", and its
  update-triggers index already names a skill add/remove as a trigger touching the kb.html
  four-plane module map, the site methodology skill diagrams, README `R1`, and the
  `gen-reference` roster test. Update that entry **in the same change** as the roster edits above.
- **Sync the site copy -- never hand-edit it.** `site/src/content/docs/concepts/methodology.md` is
  regenerated per `site/scripts/.synced-manifest.json`:
  ```
  node site/scripts/sync-docs.mjs
  ```
  This step is inseparable from the source edits: a source edit without the sync leaves the site
  showing the previous toolkit.
- **Out of scope:** `site/src/content/docs/reference/skills.md` -- generated by
  `gen-reference.mjs`; its read-back is task-092 and editing it would be reverted by the next
  build. Also out of scope: `.aid/knowledge/kb.html`'s four-plane module map (generated and
  hand-edit-forbidden; corrected by a `/aid-housekeep` SUMMARY-DELTA); the `SKILL_GROUPS` roster
  entry and its `CURATED_SKILL_NAMES` mirror (feature-012, task-010); every `${SKILLS}` count
  needle (feature-012, task-012); and the Knowledge Base entries (tasks 094 and 095).

**Acceptance Criteria:**
- [ ] Accuracy verified against the current codebase: every added entry describes `/aid-graph` as
      it actually shipped, read back against `canonical/skills/aid-graph/SKILL.md`.
- [ ] A written proposal names which of `docs/aid-methodology.md`'s inventory tables carries
      `/aid-graph`, with its reason and the alternatives considered, and **the owner has confirmed
      it before any table entry is written**. No entry is written on a guess.
- [ ] `/aid-graph` appears in `docs/repository-structure.md`, `docs/glossary.md` and
      `docs/install.md`.
- [ ] `docs/aid-methodology.md` carries it in the confirmed inventory table, in the § 4
      phase-mapping table, and as a node in the `G1 " Knowledge Base Maintenance "` Mermaid
      subgraph.
- [ ] `README.md`'s on-demand skill list beside `/aid-summarize` and its hand-authored `R1`
      diagram both carry the skill.
- [ ] `docs/diagram-content-reference.md`'s update-triggers entry is amended in the **same
      change** as the roster edits, per its own maintenance rule.
- [ ] `node site/scripts/sync-docs.mjs` has been run, and `git diff` shows
      `site/src/content/docs/concepts/methodology.md` moved with its source and nothing else.
- [ ] No count needle is edited by this task, and `bash tests/canonical/test-doc-counts.sh` still
      passes.
- [ ] No generated file is hand-edited: `site/src/content/docs/reference/skills.md` and
      `.aid/knowledge/kb.html` are untouched.
- [ ] The reviewer ledger for this task carries no finding with Status `Pending` or `Recurred`, so
      the delivery gate's `grade.sh` run over `.aid/.temp/review-pending/` reaches this
      repository's resolved `A+` (`review.minimum_grade`; `.aid/knowledge/quality-gates.md`
      § Minimum-Grade Thresholds).
