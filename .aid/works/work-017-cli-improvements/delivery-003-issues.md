# Delivery Issue Log -- delivery-003

> Deferred findings from per-task quick checks. Consumed by the per-delivery
> quality gate as prior context. Not graded -- grade.sh runs only on the
> gate reviewer's own issue list.

| Source task | Severity | Description | Status |
|-------------|----------|-------------|--------|
| task-018 | [LOW] | Structural guard drift: `test-connector-skills-structural.sh` ST26 (a work-004 "no new scripts in connectors/" closed-set assertion) had to be updated to allowlist the new `write-connector.sh`. Legitimate + spec-authorized (this task adds exactly that script), but the gate should confirm the allowlist edit is minimal (only `write-connector.sh` added) and the closed-set intent is otherwise intact. | Deferred to gate (developer flagged; found via regression sweep, not called out by the task DETAIL). |
| task-018 | [MINOR] | `write-connector.sh` `set` runs the `mcp`/`auth_method: none` orphan-secret purge UNCONDITIONALLY (simpler than the skill's ADD-vs-UPDATE old-auth comparison). Developer confirms idempotent + confirmed to actually delete a pre-existing `.secrets/<stem>` (test U38). Gate: confirm this matches the DETAIL's explicit ask and can't purge a still-referenced secret. | Deferred to gate (developer-noted design choice, per DETAIL). |
| task-018 | [MINOR] | `connector-secret.sh`'s 2/3 exit-code normalization branch inside `purge_secret()` is defensively unreachable through the CLI surface (stems pre-validated). Developer states it is intentional defense-in-depth per DETAIL, not dead code. Gate: confirm it's not misleading cruft. | Deferred to gate (developer-noted). |
| task-018 | [INFO] | CI-deferred locally: `tests/run-all.sh` full suite and `test-dogfood-byte-identity.sh` (~700 sequential sha256sum forks -> impractically slow on this Git-Bash host). Developer verified the specific new-file risk surface via `diff -q` + manual `sha256sum` against `profiles/claude-code/`. Gate/CI must run the full byte-identity check. | Deferred to CI. |
| task-019 | [LOW] | Golden-fixture key-order assertions in `test_server_py.py::TestSerializationDm3` + `test_server_node.mjs` were updated to include the additive `connectors` DM-1 key. Intended change (not a masked regression — dev introduced then fixed one mid-task). Gate: confirm the fixture edits are the minimal additive `connectors`-after-`kb_state` insertion and DM-1 `schema_version` stayed 3. | Deferred to gate (dev-flagged intended change). |
| task-019 | [INFO] | Twin-parity for `parse_connectors`/`parseConnectors` + `connector.set/remove` ops verified by a direct Python↔Node data-parity test + mirrored validator/argv/OP_TABLE matrices + real `write-connector.sh` round-trips on both runtimes. Full cross-runtime OP status+body-byte parity is reserved for task-023 (TEST). Gate/CI: run the deferred live-socket classes + full parity. | Deferred to task-023 + CI. |
| task-020 | [LOW] | Render-model asymmetry to confirm at gate: `write-external-source.sh` is co-vendor-ONLY (`dashboard/scripts/`, no render — mirrors `write-setting.sh`/`write-requirement.sh`), whereas task-018's `write-connector.sh` lives in the run_generator-rendered `connectors/` area (canonical + profiles + dogfood + co-vendored). Both are server-dispatched writers; the placement differs by feature area. Gate: confirm each placement matches its feature precedent and BOTH appear in `dashboard/MANIFEST` (verified: lines 33-34). | Deferred to gate (dev precedent-matched; verified both in MANIFEST). |
| task-020 | [INFO] | AC2 reader-visibility verified via the real `parse_doc_frontmatter` twin against a written file. Dev noted the UNMODIFIED real `.aid/knowledge/external-sources.md` currently parses to `[]` because of an interstitial `# EXTERNAL...` comment before `- (none)` (the writer's add re-emits a contiguous block, fixing visibility). Lint stayed green through add/remove (tested via a `kb-category: primary` fixture, since the real file is `meta` and skipped by the linter). | Deferred to gate/task-023. |
| task-019 (found in task-021) | [LOW] | task-019 added `connector.set`/`connector.remove` to `OP_TABLE` but did NOT update the EXACT-SET `OP_TABLE.keys()` assertions in `test_task004_op_dispatch.py` + `test_task008_display_rename.py`, leaving them red (would fail CI) until task-021 found + fixed them to the full current 10-row table. Now green. PROCESS LESSON: any OP_TABLE row addition must update those exact-set assertions; task-019's local verification didn't run those cross-task tests. Gate: confirm all OP_TABLE exact-set assertions match the final 10-row table. | Fixed in task-021 (a05e7309). |
| task-021 | [INFO] | `test_task011_dispatch_round_trip.mjs`/`test_task013_project_registry_ops.mjs` `ECONNRESET` failures confirmed pre-existing environmental flakiness (reproduce on the unmodified baseline via git stash) — socket-timing, not code. Deferred to CI. | Pre-existing / out of scope. |
| task-022 | [HIGH-watch] | RUNTIME UI behavior (Add form opens/submits, Remove confirm() blocks/proceeds, busy-state redraws, re-render from fresh model) is NOT verifiable by the 65 static tests — this is EXACTLY the delivery-001 regression class (4 edit surfaces shipped broken past an A+ static gate). MUST be confirmed by the delivery-003 browser-dogfood before delivery-003 Done. Dev engineered against it (always-visible Add forms; `_consume*ExplicitRender()` bypass on the saving-state render; UI-state field tracking; self-caught + fixed a Remove double-submit race in e14701de). | **RESOLVED — browser-dogfood PASS (2026-07-19)**: isolated `--allow-writes` server + Playwright against a throwaway project. Connectors: Add (mcp, name-only) → `connector.set` dispatched → descriptor written with correct mcp normalize (auth none, no secret) + INDEX rebuilt + table re-rendered; Remove (confirm-accepted) → descriptor deleted + INDEX emptied + "No connectors registered." External Sources: Add URL → `external-source.add` → persisted to `sources:` frontmatter + rendered as `<a rel="noopener noreferrer">` (URL-regex twin) + re-render; Remove → `sources: []` + "No external sources registered." All four surfaces OPEN + SUBMIT + persist + re-render — the delivery-001 no-op class is ABSENT. NON-BUG observation: the `## Sources` body-mirror insert no-op'd because the throwaway fixture used a TRUNCATED placeholder (writer matches the full canonical `PLACEHOLDER_TEXT`, L97) — authoritative `sources:` frontmatter (reader/UI source of truth) updated correctly regardless; would update the body too against a real canonical file. |
| task-022 | [INFO] | 15 pre-existing `test_server_py.py`/`test_reader.py`/`test_task014_fixtures.py` failures confirmed unrelated (git stash reproduces on baseline) — the known `$HOME`-isolation/Windows-8.3-path class. home.html is co-vendored (no render); confirmed `git diff --stat -- canonical/ profiles/ .claude/` empty. | Pre-existing / out of scope. |
| task-023 | [LOW] | Fixture placement: the DETAIL says golden fixtures "placed under the existing `dashboard/reader/tests/` AND `dashboard/server/tests/` fixture locations" (both); the dev CENTRALIZED them under `dashboard/server/tests/fixtures/pt023-*` and had the reader-tests file cross-reference that one location (avoids fixture duplication/drift, mirrors the `reader.mjs` cross-ref precedent). Gate: accept the single-location + cross-ref (recommended — no duplication) or require the strict "both locations" duplication (one-file follow-up). | Deferred to gate (dev-flagged design decision). |
| task-023 | [INFO] | No production defects found — every real-writer output matched golden fixtures byte-for-byte; exit→HTTP matrix + parser/serializer twin parity all green. Live-socket `write_enabled` 403-gate class formally CI-deferred (ran opportunistically in-session, ~5s, no hang, but CI is authoritative). | Clean. |
| delivery-wide | [INFO] | Confirmed pre-existing on this Windows host (reproduce on the parent commit, unrelated to work-017 code): ~11 `test_server_py.py` + ~12 `dashboard/reader/tests/` failures from the `$HOME`/`USERPROFILE` test-isolation gap (tests read the real `~/.aid`) + Windows 8.3-path/separator + one `ERR_UNSUPPORTED_ESM_URL_SCHEME`. NOT introduced by any delivery-003 task. Privacy angle (real ~/.aid data leak) tracked separately as a follow-up (see memory `reference_windows-pwsh-test-isolation`). | Pre-existing / out of scope. |
