# Delivery Issue Log -- delivery-005

> Deferred findings from per-task quick checks. Consumed by the per-delivery
> quality gate as prior context. Not graded -- grade.sh runs only on the
> gate reviewer's own issue list.

| Source task | Severity | Description | Status |
|-------------|----------|-------------|--------|
| task-028 | [INFO] | GOOD CATCH: the `.gitignore` "AID managed" block is GENERATOR-produced (`lib/aid-install-core.sh` `_aid_gitignore_block` + `lib/AidInstallCore.psm1` `Get-AidGitignoreBlock`, both "rewritten on every add/update"), so a static `.gitignore` edit alone would be silently reverted by the next `aid update`. Dev fixed BOTH generators too (added `.aid/.control/` alongside `.aid/.heartbeat/`). install-provisioning tests 44/44 + 33/33 pass; no test hardcodes a line-count. Gate: confirm the twin generators (bash + ps1) stay in sync. | Deferred to gate (dev-flagged, verified). |
| task-028 | [INFO] | RENDER handled correctly: `write-control-signal.sh` authored in `canonical/aid/scripts/execute/`, `run_generator.py` rendered to all 5 profiles + dogfood `.claude/` resynced + co-vendored to `dashboard/scripts/` + MANIFEST; byte-identity confirmed across canonical↔5 profiles↔.claude↔dashboard via `diff -q`; render diff scope is ONLY the new file × 5 + 5 emission-manifest lines (no drift). 50/50 writer tests. `test-writeback-state.sh` local-hang (untouched, CI-deferred); full byte-identity → CI. | Clean. |
| task-028 | [LOW] | KB-delta follow-up (out of task-028's IMPLEMENT scope, dev-flagged): `.aid/knowledge/module-map.md` line ~157 lists the `execute/` script set and doesn't yet mention `write-control-signal.sh`. Housekeeping, not blocking. | Deferred (KB-delta follow-up). |
| task-029 | [LOW] | Two dev judgment calls to confirm at gate: (1) `stop_requested` appended LAST on TaskModel (after `display_name`), not after `lane` as the DETAIL/SPEC line-citation literally reads — the citation predates task-008's `display_name` merge; append-only additive convention (matches `RepoInfo.connectors`/`external_sources`). (2) `task.stop`/`task.resume` use the GENERIC loose `^work-[0-9]+` work_id validation (→400 on malformed, like `pipeline.finish`/`rename`), NOT `pipeline.delete`'s strict `work_id_re`→422 override — following the DETAIL's literal "validated per feature-001" over an imprecise paraphrase. Gate: confirm both are the intended readings. | Deferred to gate (dev-flagged, spec-followed). |
| task-030 | [HIGH-watch] | RUNTIME UI behavior (Finish confirm fires + marks Completed; Stop/Resume toggle fires + label/op flips on stop_requested; paused pill shows; controls disable during in-flight; a poll tick doesn't clobber the in-flight disabled state; chip button's stopPropagation prevents drill-nav) NOT verifiable by the 55 static tests — delivery-001 render-guard class. Dev verified pipeline.finish is a FUNCTIONAL op (server.py ~2119 / server.mjs ~1859), used the `_consume*ExplicitRender()` bypass + guard-bypass test classes. | Browser-dogfood REQUIRED (orchestrator, pre-gate): Finish on a Running pipeline → Completed; Stop→paused pill+Resume flip→resume; AC6 (control absent for In Review + non-active). |
| task-030 | [INFO] | `makeTaskChip` gained a 3rd arg (writeEnabled) threaded renderTasks→renderTasksFull→renderLanes + renderTaskView; rippled to `test_task009_rename_ui.py` (2 assertions updated for the new signature/search-window). No render (home.html co-vendored). A stray `_test_task023_real_slice_*.mjs` temp fixture leaked from an interrupted full-suite run (dev-noted) — cleaned by orchestrator, not committed. | Clean. |
| task-031 | [INFO] | Executor-poll baseline (prose-only) added to canonical `state-execute.md` at 5 tick points (new Cooperative-Poll section + EXECUTE-entry Finish check + pre-Step-2 Stop/Resume + PD-2 Finish guard + PD-3 heartbeat tick); rendered to all 5 profiles + dogfood resync; byte-identity confirmed (sha256/diff); render scope = state-execute.md ×5 + 5 manifest lines + dogfood copy (no drift). Sharp judgment: Finish check placed BEFORE the EXECUTE state's unconditional `Lifecycle=Running` write, so a dashboard-set Completed/Blocked can't be clobbered first. Prose-only (no unit-testable code — verified by canonical-render/twin-parity per the execution-graph note, not task-033). Full test-dogfood-byte-identity → CI. Gate: confirm the poll prose is correct + present identically across all 5 renders. | Clean (prose + render verified). |
| task-033 (found) / ops (fixed) | [HIGH] | task-033's REAL-writer round-trip caught a Windows-only production defect: `_op_task_stop_argv`/`_op_task_resume_argv` (both twins) passed `write-control-signal.sh` a NATIVE backslash `AID_WORK_DIR`, but the bash writer does forward-slash path arithmetic (`${WORK_DIR##*/}` + `/../../.control/...`) → the `.stop` signal landed at a garbled path while the writer still exited 0 (HTTP 200), so `stop_requested` silently never flipped — FR-T3 (Task Stop/Resume) BROKEN on Windows (the user's platform). `pipeline.finish` unaffected. | **FIXED (orchestrator, commit 95e67ae8):** `work_dir.as_posix()` (server.py) / `toPosixArg(String(workDir))` (server.mjs) on the AID_WORK_DIR env value (mirrors `_posix_argv_path` argv convention; ops-only, no writer re-render). Un-gated task-033's skipIf'd round-trips + removed the obsolete defect canary. VERIFIED on this Windows host: task-033 **14 passed / 20 subtests, NO skips** — real `task.stop` now lands the signal correctly + `stop_requested` re-derives true/false + twin parity. IMPEDIMENT-task-033.md resolved + removed. |
| task-033 | [INFO] | Consolidated round-trip coverage (vs task-029/030's stubbed/hand-written suites): pipeline.finish real writeback round-trip (Completed persists, body byte-preserved, /api/model shows it); task.stop/resume real write-control-signal.sh round-trip + idempotence; WT-1 with a REAL `git worktree add` fixture; AC6 gate model-flags matrix (all 7 statuses × write_enabled, In Review excluded); real-writer twin dispatch-response parity. No render. | Clean. |
| task-032 | [INFO] | Opt-in STOP_FILE sub-agent mid-task poll added to `subagent-heartbeat-protocol.md` + `agent-boilerplate.md` (mirrors HEARTBEAT_FILE; additive, no behavior change where STOP_FILE absent; aid-clerk correctly inherits the exemption via the include). Render blast radius = 73 files (2 canonical templates × 5 profiles × the `{{include:agent-boilerplate}}` cascade into 9 agents + the 2 templates + 5 manifests + 11 dogfood) — mechanically correct, no drift outside the expected set. Verified: run_generator clean+deterministic (2×); in-process sha256 sweep of all 347 `.claude` manifest entries (dogfood ≡ profiles/claude-code); dir-listing diffs (no orphans). Full test-dogfood-byte-identity → CI. NO dispatch call site wired (contract only; a specific dispatch passing STOP_FILE is an explicit follow-on per DETAIL/SPEC §Migration). Gate: confirm the render cascade is exactly the include set + the follow-on wiring boundary is intended. | Clean (prose + render verified; wiring = documented follow-on). |
| task-029 | [INFO] | Recurring closed-set OP_TABLE.keys() assertions updated to the now-13-row table (task.stop + task.resume added); also fixed a SECOND pre-existing break — `test_task008_display_rename.py::TestSerTaskDisplayName` used a bare mock lacking `stop_requested` → AttributeError in `_ser_task`; added the attribute + updated its last-key assertion. Twin-parity + golden fixtures regenerated; no `schema_version` bump; no render (git diff on canonical/profiles/.claude empty). 19+29+75 tests pass. Real write-control-signal round-trips + AC6 UI gate deferred to task-033/030. | Clean / pre-existing fixes. |
