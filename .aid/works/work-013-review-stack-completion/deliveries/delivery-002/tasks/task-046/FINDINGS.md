| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [MINOR] | Fixed | .aid/works/work-013-review-stack-completion/deliveries/delivery-002/tasks/task-030/NFR1-VERIFICATION.md | — | task-030 AC-4 — Section 3 records "severity enum occurrences\|4" and "status enum occurrences\|2" with no commands shown, so a reader cannot independently re-run those checks as AC-4 requires | cycle-2 FIX: comparison loop now shown in section 3; the first two iterations reproduce base=4/now=4 and base=2/now=2 exactly; `severity: judged` |
| 2 | [MINOR] | Fixed | .aid/works/work-013-review-stack-completion/deliveries/delivery-002/tasks/task-033/CLASSIFICATION.md | — | task-033 AC-1 — CLASSIFICATION.md stated "9 instruction sites" for state-review.md and "13 instruction sites" total, but the diff showed only 7 instruction sites in state-review.md and 10 across all files, so the record's counts were inaccurate | cycle-2 FIX: count changed to 12 with note that the wrong figure came from reading the summary table rather than measuring; `grep -c '{{LEDGER}}'` on the four files → 1+1+1+9=12 (at task-033 commit 0371f80e6, verified `git show 0371f80e6:canonical/skills/aid-update-kb/references/state-review.md \| grep -c '{{LEDGER}}'` → 9); `severity: judged` |
| 3 | [MINOR] | Fixed | tests/review-cost-meter.sh | 174 | task-046 AC-1 — the header documented the double-count's measured ratio (84934 vs 42467 = 2×) but not the "extractor's before-output recorded showing the same path emitted twice" as AC-1 literally specifies, so the raw path-list output (two identical lines) was absent | cycle-2 FIX: RECORD.md now contains a `### Cost-meter extractor — the before-output` section showing `brief_artifacts "$brief"` → `README.md\nREADME.md` with README.md size 14895 bytes (`wc -c /workspace/README.md` → 14895); `severity: judged` |
| 4 | [MINOR] | Pending | .aid/works/work-013-review-stack-completion/deliveries/delivery-002/tasks/task-030/NFR1-VERIFICATION.md | — | task-030 AC-4 — the third loop iteration in section 3 uses pattern `'^| \`(Pending\|...\|Invalid)\`'` where `^\|` acts as regex alternation (empty-string-at-start OR pipe), producing base=327/now=340 rather than the 6 claimed in the "status-table rows\|6\|6" table row, so a reader cannot reproduce that count by running the shown command | `cd /workspace; S=canonical/aid/templates/reviewer-ledger-schema.md; B=1412e63d1b8cf5d039778efe2f9c3ad23a9fdbd3; grep -cE '^| \`(Pending\|Fixed\|Recurred\|Accepted\|OOS\|Invalid)\`' $S` → 340 (not 6); the table entry "status-table rows \| 6 \| 6" requires escaping `|` as `\|` in the pattern to match only lines starting with a literal pipe; `severity: judged` |
| 5 | [MINOR] | Pending | .aid/works/work-013-review-stack-completion/deliveries/delivery-002/tasks/task-033/CLASSIFICATION.md | — | task-033 AC-1 — the cycle-2 cross-reference fix added 2 more `{{LEDGER}}` tokens to state-review.md documentation sections (lines 368, 370), so the grep command shown now produces 1+1+1+11=14, not the 1+1+1+9=12 displayed; a reader running the command gets the wrong count | `grep -c '{{LEDGER}}' canonical/skills/aid-discover/references/state-fix.md canonical/skills/aid-discover/references/state-done.md canonical/skills/aid-update-kb/references/state-done.md canonical/skills/aid-update-kb/references/state-review.md` → 1/1/1/11=14 (not 12 as shown); the pre-cycle-2 count was 9 in state-review.md (`git show 0371f80e6:…state-review.md \| grep -c '{{LEDGER}}'` → 9); `severity: judged` |
