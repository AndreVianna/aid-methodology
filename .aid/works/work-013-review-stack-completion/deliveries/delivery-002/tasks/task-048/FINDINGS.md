| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [LOW] | Fixed | .aid/works/work-013-review-stack-completion/deliveries/delivery-002/RECORD.md | 383 | task-048 AC-2 — criterion 8 asserts "the header row still yields exactly one match" and "4/2/6 at base and now" in narrative text without runnable commands, so AC-2's "re-running task-030's three checks" is only partially demonstrated (check 1 shown with command; checks 2 and 3 by assertion only) and gate criterion 9's "every count carries its command" is violated for these two inline counts | cycle-2 FIX: header-row grep and three-pattern enum loop now shown in criterion 8; all three reproduce: header grep → 1; loop gives 4/4, 2/2, 6/6; third pattern uses escaped `^\\\|` and gives 6 while the previously unescaped variant gives 340 — contrast verified; severity: judged |
| 2 | [MINOR] | Fixed | .aid/works/work-013-review-stack-completion/deliveries/delivery-002/RECORD.md | 388 | task-048 AC-6 — criterion 9 states "The one deliberate exception is git rev-parse HEAD" but the historical baselines section has at least 4 more command blocks that produce different numbers now (suite, files, paths, COVERS counts), so the criterion 9 statement understates the non-reproducing command set by at least 4 | cycle-2 FIX: "one deliberate exception" replaced with a five-row table naming git rev-parse HEAD, suite count, file count, literal ledger paths, and COVERS headers, each with its current value and the task or cause that moved it; count is 5 (verified by reading table rows); severity: judged |
| 3 | [MINOR] | Pending | .aid/works/work-013-review-stack-completion/deliveries/delivery-002/RECORD.md | 412 | task-048 AC-6 — criterion 9's exception table attributes the COVERS header count change (6 to 9) to task-027, but task-027 only added one COVERS entry to an existing file without changing the file count; the count rose because tasks 028, 035, and 036 each created a new suite file with COVERS headers, so the attribution names the wrong task and a reader auditing the table would misattribute the cause | `git grep -l 'COVERS' 1412e63d1b8cf5d039778efe2f9c3ad23a9fdbd3 -- 'tests/canonical/*.sh' \| wc -l` → 6 (base); test-scoped-review-cycles.sh had COVERS at base and gained one entry (grade.sh) from task-027, leaving file count unchanged at 6; `grep -rln 'COVERS' tests/canonical/*.sh \| wc -l` → 9 now; the 3 additions are test-severity-why-line.sh (task-028), test-ledger-isolation.sh (task-035), test-review-recall.sh (task-036); severity: judged |
