| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [MINOR] | Fixed | tests/canonical/fixtures/recall-corpus/CATALOGUE.md | 8 | task-037 AC-1 — `ZQ7-STALECOUNT-ALPHA` appears in `seed-ledger.md` (outside `defects/`), so the claim is false for D01 and a future recall harness reading it literally would need to compensate | Fixed: claim now reads "nowhere else in the reviewed tree"; assertion confirmed true — `rg "ZQ7-" canonical/` returns 0 for all 20 signatures; both corpus-internal exceptions named and necessity stated; `severity: judged` |
| 2 | [MINOR] | Fixed | canonical/aid/templates/reviewer-ledger-schema.md | 94 | F-schema-gap — the id-citation system had no defined resolution path for task DETAIL.md acceptance criteria, so every task-gate reviewer was forced to invent a non-resolving id or omit it, producing the defect the schema itself forbids, so id traceability was broken by design for the most common review invocation | Fixed: schema now documents `task-NNN AC-N` resolved by counting top-level checkboxes under `**Acceptance Criteria:**`; form works for this ledger; edge case (nested sub-items) addressed by row 4 fix; `severity: judged` |
| 3 | [LOW] | Fixed | canonical/agents/aid-reviewer/AGENT.md | 133 | F-schema-gap — AGENT.md and authoring-conventions.md still said "the winning level is named" for `severity: override <level>`, inconsistent with the schema's updated reading that `<level>` names the cascade source (`file`, `file-class`, or `type`), so a reviewer following the system-prompt wrote an incompatible token to one following the schema, undermining provenance traceability | Fixed: `grep "severity: override" canonical/agents/aid-reviewer/AGENT.md` → line 133: "`<level>` names where the winning band came from — `file`, `file-class` or `type` — not the band"; `grep "severity: override" .aid/knowledge/authoring-conventions.md` → line 402: same meaning; all five profile renders and both root install trees verified consistent; `rg "severity: override\|why-line" canonical/ .aid/knowledge/ -l` returns exactly the three canonical sites, no fourth; `severity: judged` |
| 4 | [MINOR] | Fixed | canonical/aid/templates/reviewer-ledger-schema.md | 101 | F-schema-gap — counting checkboxes under `**Acceptance Criteria:**` was ambiguous for a DETAIL.md with nested `- [ ]` sub-items, so a citation against any subsequent AC would silently resolve to the wrong criterion without a diagnostic | Fixed: schema now reads "counting the **top-level** checkboxes" with the explicit reason "a nested `- [ ]` sub-item is part of its parent and is not counted, or the ordinal would shift and a citation would silently resolve to the wrong criterion"; `grep "top-level" canonical/aid/templates/reviewer-ledger-schema.md` confirms new wording at line 101; all 23 delivery-002 DETAIL.md files have 0 nested checkboxes (no live defect); `severity: judged` |
| 5 | [MINOR] | OOS | — | — | Corrupted commit message in `39e6dfdfd` — `task-NNN AC-N` was shell-substituted away; message reads "The schema now documents , resolved by…"; the change itself is intact | Routing: git history only; `34537579d` records the corruption; no artifact fix required; `severity: judged` |
