| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [MEDIUM] | Fixed | scripts/checks/settings-schema-check.sh | — | AC-1 unmet — script exits 1 on .aid/settings.yml: format_version is 3 but AID_SUPPORTED_FORMAT is 4, so the claimed "exits 0 on the corrected real settings file" is false | cycle-2 FIX: script now mirrors bin/aid's _aid_format_gate three ways; `bash scripts/checks/settings-schema-check.sh --path .aid/settings.yml` → PASS (exit 0) with [NOTE]; stamp 99 → `[FAIL] format_version is 99, newer than this CLI supports`, exit 1 |
| 2 | [LOW] | Fixed | scripts/checks/settings-schema-check.sh | 81 | AC-2 partial gap — undocumented CamelCase top-level keys pass silently; the key-extractor regex `^[a-z_]+:` does not match them | cycle-2 FIX: extractor widened to `^[A-Za-z_][A-Za-z0-9_-]*:`; `printf 'format_version: 4\nForbiddenKey: value\n' > /tmp/t.yml; bash scripts/checks/settings-schema-check.sh --path /tmp/t.yml` → `[FAIL] top-level key 'ForbiddenKey' is not documented`, exit 1 |
| 3 | [MEDIUM] | Fixed | tests/canonical/test-kb-html-claims-check.sh | — | task-017 coverage gap — claim class 3 (KB document paths) is entirely untested; no fixture HTML contains a `.aid/knowledge/*.md` path, so a broken class-3 extractor passes all 14 tests | cycle-2 FIX: KH06 added; class-3 mutation (grep replaced with `'MUTATION_NOTHING_MATCHES_THIS'`) now fails 3 assertions: `KH06 -- names the document that does not exist`, `KH06 -- says why it failed`, `KH06 cited-and-present KB doc -- exit 0`; `git diff` on the script is clean after restore |
| 4 | [MINOR] | Fixed | scripts/checks/settings-schema-check.sh | 33 | WHAT IT CHECKS item 1 still says "equal to bin/aid's AID_SUPPORTED_FORMAT" — the behavior is now a three-way gate (newer=violation, older=[NOTE]+pass, equal=pass); "equal to" is wrong | cycle-3 FIX: header updated to state all three ways explicitly; `sed -n '32,39p' scripts/checks/settings-schema-check.sh` shows newer=violation, older=note, equal=pass; text now reads "compared to bin/aid's AID_SUPPORTED_FORMAT the same three ways _aid_format_gate compares it"; confirmed in place |
| 5 | [MINOR] | Fixed | scripts/checks/settings-schema-check.sh | 87 | extractor regex `^[A-Za-z_][A-Za-z0-9_-]*:` is blind to YAML quoted keys — `"quoted-key": value` slips through counted as absent, so an undocumented quoted key passes silently | cycle-3 FIX: extractor replaced with `^[^[:space:]#][^:]*:` with trailing-colon strip and quote-strip sed; all four shapes (CamelCase, quoted, dotted, single-quoted) now detected and reported by name; `printf 'format_version: 4\nCamelCaseKey: v\n"quoted-key": v\ndotted.key: v\n'"'"'single-quoted'"'"': v\n' \| bash scripts/checks/settings-schema-check.sh --path /dev/stdin` → 4 violations |
| 6 | [MINOR] | Fixed | scripts/checks/settings-schema-check.sh | 87 | extractor regex `^[A-Za-z_][A-Za-z0-9_-]*:` is blind to dotted keys — `dotted.key: value` slips through because `.` is not in the character class and the match stops before `:` | cycle-3 FIX: same class fix as row 5; extractor now `^[^[:space:]#][^:]*:` admits anything at column 0; dotted.key confirmed detected; both real settings files exit 0; baseline 13/141 unchanged; render parity clean |
