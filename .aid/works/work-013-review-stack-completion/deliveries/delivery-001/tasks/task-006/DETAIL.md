# task-006: Migrated catalog checks written as declared criteria rows

> **Execution protocol (binding on whoever executes this task -- no
> exceptions):** the moment this task's `State` changes, write it --
> `In Progress` before starting work, `In Review` before dispatching the
> reviewer, and the terminal value (`Done` / `Failed`) at the end -- via
> `writeback-state.sh --field State --value ...`. This applies equally
> whether the main/orchestrator agent executes this task directly or
> dispatches it to a sub-agent; neither may skip, batch, or defer these
> writes. (`Blocked` is a distinct, orchestrator-assigned value for a
> DIFFERENT, downstream task that depends on a failed one -- it is never
> self-written by the task being executed.) Full mandate:
> `aid-execute/references/state-execute.md § MANDATORY: State-Write
> Protocol`.

**Type:** IMPLEMENT

**Source:** work-013-review-stack-completion -> delivery-001

**Depends on:** task-003, task-004, task-005

**Scope:**
- Write each admitted row into `.aid/knowledge/authoring-conventions.md` § Review Criteria at a NEW id in its existing scope prefix.
- Record the allocation in the ledger from task-001.
- Discharge the `oracle:` half of FR-A3 as *not applicable, stated* if no admitted row is mechanically decidable — the criteria table has no oracle column and widening it is out of scope.

**Acceptance Criteria:**
- [ ] Each new id greps back to exactly one row carrying Applies-to, Kind, Criterion, Severity and Why.
- [ ] No new id collides with an existing one; the resulting row count equals the previous count plus the number admitted, with the command that measured it.
- [ ] `grep -rn 'review-rubrics' canonical tests scripts docs .aid/knowledge` returns `0`.
- [ ] `lint-frontmatter.sh --root .aid/knowledge` passes, and `INDEX.md` is regenerated by its script rather than hand-edited if any routing field changed.
- [ ] If both screens admitted zero rows, the 85-row screening table is landed instead and the `oracle:` discharge is recorded — neither is skipped silently.
- [ ] All section-6 quality gates pass
