| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [MINOR] | Fixed | canonical/aid/scripts/kb/kb-citation-lint.sh | 23 | G-01 — load-bearing count "106" was wrong: actual file count is 107 | cycle-2 FIX replaced "opens 3 files out of 106" with "opens a small handful of its markdown"; no count survives: `grep -n "3 of 106\|out of 106" canonical/aid/scripts/kb/kb-citation-lint.sh` = empty |
| 2 | [MINOR] | Fixed | .github/workflows/test.yml | 167 | G-01 — load-bearing count "106" was wrong: actual file count is 107 | cycle-2 FIX replaced "opens 3 of 106 files" with "opens a handful of a work root's markdown" and added explicit note: "Exact counts are deliberately not quoted here: they move with every task, and a stale number in a comment is worse than none." |
| 3 | [MEDIUM] | Fixed | canonical/aid/scripts/kb/kb-citation-lint.sh | — | No declared criterion — symlink root silently produced false clean: find without -L opened 0 files and lint exited clean | cycle-2 FIX: (a) added -L to both find calls so symlinked roots are followed; (b) opening zero files is now exit 2 not clean. CL12 asserts symlink opens same count and finds same violations as real path; CL11 asserts zero-file routes exit 2 with "refusing to report clean". Mutation tests: drop -L fails 2 assertions; remove zero-file guard fails 4. |
