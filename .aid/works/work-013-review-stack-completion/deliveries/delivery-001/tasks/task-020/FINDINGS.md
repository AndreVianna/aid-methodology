| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [MINOR] | Fixed | .aid/works/work-013-review-stack-completion/deliveries/delivery-001/tasks/task-020/RESEARCH.md | 119-129 | No declared criterion id (file is out-of-corpus) — reproducibility recipe said "insert the four rows" but omitted the row text; using `**/*` glob syntax causes `glob_match` to treat the inner `*` as literal, leaving SPEC.md and BLUEPRINT.md unresolved (110 violations vs ~103); correct single-`*` syntax gives 105 violations; exit=1 reproduces regardless; count discrepancy was partly doc-predicted drift and partly a silent glob-syntax trap not covered by the recipe comment | cycle-2 FIX: recipe now carries full row text as Python heredoc; verbatim recipe ran exit=1, 106 violations (exceeds 103 as document now warns), zero candidates in violations — all four rows fired; new section "A second grammar limit" documents the `"$post"` quoting mechanism; `sed -n '88,92p'` reproduces excerpt exactly; mechanism verified correct |
