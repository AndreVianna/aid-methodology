# task-005: Writer surfaces — agent-boilerplate + the 5 profile context files

> **Execution protocol:** whoever executes this task writes its `State` at every
> transition (`In Progress` at start, `In Review` before the reviewer, terminal
> `Done`/`Failed` at end) in this task's `STATE.md`. Binds the main agent executing
> directly, not only a dispatched sub-agent.

**Type:** IMPLEMENT

**Source:** work-004-frontmatter-review-criteria -> delivery-001

**Depends on:** task-001

**Scope:**
- `canonical/aid/templates/agent-boilerplate.md` (`## Self-review discipline`): add FR-9 (resolve the
  target file's `review-criteria` global→type→file and comply **before** writing/editing) and FR-10
  (a work that introduces a document type adds its registry row; removing the last file of a type
  removes it). Reaches every dispatched sub-agent via the 9 `AGENT.md` includes.
- The **5 hand-authored** `profiles/<tool>/{CLAUDE,AGENTS}.md` (`claude-code/CLAUDE.md`,
  `antigravity/AGENTS.md`, `codex/AGENTS.md`, `copilot-cli/AGENTS.md`, `cursor/AGENTS.md`),
  `AID:BEGIN`/`AID:END` region: the same two instructions as pointers to `authoring-conventions.md`.
  The repo-root `CLAUDE.md`/`AGENTS.md` is install output — **not** an edit target.

**Acceptance Criteria:**
- [ ] `agent-boilerplate.md` carries FR-9 and FR-10 as global instructions in `## Self-review discipline`.
- [ ] All 5 profile context files carry the same two instructions in their AID-managed region.
- [ ] The repo-root context file is not edited directly (it is regenerated by `_copy_root_agent_file`).
- [ ] FR-10 backstop criterion (every in-scope file resolves to one registry row) is present in
      `authoring-conventions.md` (added in task-001 or here, not duplicated).
- [ ] Additive/localized (NFR-3); no new mechanism (C-1). All §6 quality gates pass.
