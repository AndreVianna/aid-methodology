# Delivery Issue Log -- delivery-003

> Deferred findings from per-task quick checks. Consumed by the per-delivery
> quality gate as prior context. Not graded -- grade.sh runs only on the
> gate reviewer's own issue list.

| Source task | Severity | Description | Status |
|-------------|----------|-------------|--------|
| task-016 | [HIGH] | **AC-4 FAILS as measured.** 462 removed guard lines (379 checker + 83 wrapper) against 519 added mechanism lines on the strict count -- and AC-4 names 379 as the figure it is tested against, so it fails more clearly there. Two candidate reclassifications are reported, not applied; even granting both it is 458 vs 462, a 4-line margin. No script/check/validator was added (C-1 held), so the enforcement surface MOVED rather than shrank. Owner decision: accept the criterion as failed and record why, or revise NFR-2 to count mechanism as executable surface only. Not the executor's call. | Open |
| gate cycle-1 | [LOW] | relationships.md still carries 6 rows for the deleted count guard. NOT resolvable here: it is generated by build-relationships.sh, whose inputs (.aid/.temp/graph/ candidates + node TSVs) are produced by the /aid-graph extraction passes, which are a separate on-demand skill and were never in task-015's scope (profile chain + site chain only). task-013's DETAIL wrongly expected task-015 to regenerate it. Hand-off: run /aid-graph. Hand-editing it is forbidden (F2, generated artifact). | Accepted |
| gate cycle-1 | [LOW] | .cursor/rules/output-style.mdc is an unmanifested file in the cursor dogfood tree and would fail the orphan gate. NOT this work's: it arrives from master commit 75039593, is the repo owner's own Cursor config, and deleting it here would remove a deliberate personal file that master would restore. Correcting the related task-015 claim instead: my orphan check used my own definition of AID-owned (paths containing /aid/, /agents/aid-, /skills/aid-, /rules/aid-) rather than the suite's dbi_cursor_allowlisted() allowlist, so '0 orphans' was true of my check and not of the repo's. | Accepted |
