| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [HIGH] | Fixed | feature-003/SPEC.md | 155-172 | §2 restated AC-3's "name the replacement or state the drop and why" as a to-do without fulfilling it for the two uncovered corpus parts. | §2 now carries an explicit disposition table covering all four `INCLUDE_TREES` entries (`docs`, `.aid/knowledge`, `canonical`, `site/src/content/docs`) plus `INCLUDE_FILES` (`README.md`) plus `REPO_LOCAL_SKILLS`: typed markdown -> replaced by G-01-class criterion (confirmed present in REQUIREMENTS.md L569/932); non-markdown -> dropped; docs+README front face -> reduced to FR-8 review; site -> already OOS (confirmed REQUIREMENTS.md L245); repo-local skills -> dropped. Table is exhaustive against actual `INCLUDE_TREES`/`INCLUDE_FILES`/`REPO_LOCAL_SKILLS` read from `tests/canonical/check-skill-counts.mjs` L145-168. Full-delete justified; narrow alternative flagged as owner judgment call. Holds on re-check. |
| 2 | [HIGH] | Fixed | feature-003/SPEC.md | 197-199 | §4.2 called `aid-execute.flow.json` a *feeder* of the site chain; it is one of the 76 regenerated *outputs*. Feeder is `state-execute.md`. | §4.2 now correctly names `docs/*.md` and `canonical/skills/aid-execute/references/state-execute.md` as feeders, `aid-execute.flow.json` as regenerated output. Verified: `grep -o "aid-clerk[^\"]*" site/src/data/skill-flows/aid-execute.flow.json` returns text matching `state-execute.md` L166 ("Mechanical sub-tasks... aid-clerk... See agents/aid-clerk/README.md for the caller contract") verbatim. `site/scripts/sync-docs.mjs` confirms `docs/*.md` -> `site/src/content/docs` feed path. `find site/src/data/skill-flows -name "*.flow.json" \| wc -l` = 76, matching the stated count. Holds on re-check. |
| 3 | [MEDIUM] | Fixed | feature-003/SPEC.md | 162 | §2 extension filter omitted `.py`; actual regex includes it and `.py` files (render.py etc.) are in corpus. | §2 now cites `/\.(md\|mdx\|sh\|mjs\|js\|ts\|py\|yml\|yaml)$/` verbatim, confirmed via `grep -n "const EXT" tests/canonical/check-skill-counts.mjs` (L170) and `wc -l` = 379 (confirmed). `.claude/skills/generate-profile/scripts/` holds 8 `.py` files (`render.py`, `run_generator.py`, `aid_profile.py`, `build-shortcut-skills.py`, `render_lib.py`, `test_manifest_safety.py`, `verify_advisory.py`, `verify_deterministic.py`) matching the "render.py, run_generator.py and 6 siblings" count. Holds on re-check. |
| 4 | [HIGH] | Fixed | feature-003/SPEC.md | 161-162 | The #3 fix put `.py` in SPEC's filter (matching code) but left REQUIREMENTS L1167 stating the filter without `.py` — a cross-document divergence the fix exposed. | Fixed at root: REQUIREMENTS L1167 now cites the same regex `/\.(md|mdx|sh|mjs|js|ts|py|yml|yaml)$/` as SPEC §2 and the code. Verified: all three identical (`grep "mdx|sh|mjs"` in both docs + `grep "const EXT"` in the script). |
