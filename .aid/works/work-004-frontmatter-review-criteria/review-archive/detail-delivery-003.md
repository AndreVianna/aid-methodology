| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [CRITICAL] | Fixed | task-004/DETAIL.md | 15-26 | FR-6 override-surfacing (reviewer records resolved severity + overriding file's `why` in the finding's `Evidence` cell) absent from every task. | Added to task-004 scope (c) + AC; SPEC mandates it at lines 28/142. task-004 now names override + Evidence recording. |
| 2 | [HIGH] | Fixed | task-003, task-004 | task-004 depends | task-003 and task-004 both edit `aid-reviewer/AGENT.md` + `reviewer-ledger-schema.md` with no edge → parallel-wave clobber risk. | task-004 `Depends on` now includes task-003, serializing the shared-file edits; a note explains why. |
| 3 | [HIGH] | Fixed | task-001/DETAIL.md | 14-27 | FR-10 backstop criterion (every in-scope file resolves to one registry row) had no authoring task. | Added to task-001 scope + AC; SPEC lines 139-140/299. task-001 (which authors the tables) now owns it. |
| 4 | [MEDIUM] | Fixed | task-001/DETAIL.md | 14-27 | The two-limb `rendered` exclusion (+ `agent-context`) not named in task-001's criteria-authoring scope. | Added both global exclusions with the two-limb provenance keying to task-001 scope + AC (SPEC lines 123-129 / §2). |
| 5 | [MEDIUM] | Fixed | task-002, task-004 | task-004 | The "a finding citing no id / an unresolvable id is itself a defect" corollary stated in no task. | Added to task-004 scope (d) + AC; SPEC lines 154-155. |
| 6 | [CRITICAL] | Fixed | task-011/DETAIL.md | 14-30 | Cross-feature gap: feature-001 SPEC L330 assigns the rename of the 18 canonical "carry-as-data" files to feature-002, but task-011 scoped only "18 KB docs + 4 fixtures", dropping all 18 (14 knowledge-base templates + feature-inventory + state-machine-chaining + reviewer-ledger-schema + relationship-schema.yml). | task-011 scope + AC now include all 18 canonical files, verified by `grep -rl "contracts:" canonical --include=*.md --include=*.yml | grep -vE "build-|migrate-kb|kb-authoring/"` = exactly those 18. task-011→task-010 edge serializes the reviewer-ledger-schema content-vs-rename touch. |
| 7 | [HIGH] | Fixed | task-010/DETAIL.md | AC | No task's AC confirmed the aggregate 159+123+8=290 bucket-sum (BLUEPRINT Gate 2 / SPEC AC-1). | Added a delivery-wide 290-reconciliation AC to task-010 (now the last populating task); task-010 depends on task-009 so it has both slices' counts; task-009 reports its slice counts to feed it. |
| 8 | [MEDIUM] | Fixed | task-009, task-010 | scope | Ownership ambiguity on `reviewer-ledger-schema.md` (both tasks, no edge). | task-009 now explicitly **excludes** it; task-010 owns its content (flagged as a non-KB template); task-011 owns its rename; 009→010→011 edges serialize all three touches. |
| 9 | [MEDIUM] | Fixed | task-009/DETAIL.md | scope | ~273-file walk in one task with no stated session-fit basis (Rule 1). | Added a session-fit note: mechanical pass (registry selector exhaustive by construction; most files get no block; spot-check), with a split-by-tree fallback if the exception set is large. |
| 10 | [HIGH] | Fixed | task-013/DETAIL.md | 14-32 | task-013 deletes `check-skill-counts.mjs` but never named its companion wrapper `tests/canonical/test-skill-counts.sh` (invokes the checker L49; auto-globbed by run-all.sh L112) → deleting the checker breaks the suite. | task-013 scope + AC now remove `test-skill-counts.sh` on full-delete (or keep it pointed at the narrowed checker). Verified: `test-skill-counts.sh:49` runs the checker; `run-all.sh:112` globs `test-*.sh`. |
| 11 | [MEDIUM] | Fixed | task-013/DETAIL.md | 14-32 | 5 KB docs cite the checker by name; full-delete leaves dangling citations (tech-debt.md W1-11 closure evidence becomes false). | task-013 now updates the *current*-claim docs (module-map, test-landscape, tech-debt W1-11), leaves dated historical rows (STATE.md) per F3, and defers `relationships.md` (generated) to task-015's regen. Verified 5 docs via `grep -rln "check-skill-counts\|test-skill-counts" .aid/knowledge`. |
| 12 | [LOW] | Accepted | task-016/DETAIL.md | note | task-016 bundles exit-arithmetic (§5) + C-7 audit (§6) into one DOCUMENT task. | Accepted with justification: both are same-type (DOCUMENT) close-out verifications produced once at the end with separate, independently pass/fail ACs — same-type bundling is permitted; splitting for a LOW would proliferate tasks against the work's minimalism. Note added to task-016. |
