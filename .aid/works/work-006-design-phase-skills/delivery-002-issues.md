# Delivery Issue Log -- delivery-002

> Deferred findings from per-task quick checks. Consumed by the per-delivery
> quality gate as prior context. Not graded -- grade.sh runs only on the
> gate reviewer's own issue list.

| Source task | Severity | Description | Status |
|-------------|----------|-------------|--------|
| task-029,030,031 | [HIGH] | 14 grid design rows sit after delivery-001's planning rows (roadmap/mvp/backlog) rather than immediately after the aid-design row (SPEC feature-005 3b literal); adjudicated presentational -- task-029 DETAIL line 73 states row order has no oracle and requires only that the 14 stay contiguous ahead of task-033's foundation rows (both satisfied), the checkable AC (A less-than row less-than G8) is satisfied, and groups.mjs family order is fixed by the aid-design row (all are verb design) so it is unaffected; reordering would displace feature-003's committed rows. Gate to confirm. GATE: confirmed presentational. Row order has no oracle; contiguity ahead of the foundation rows and the A<row<G8 bound both hold; groups.mjs family order is fixed by the first G3 row and all 19 are verb: design, so rendering is unaffected. Reordering would displace feature-003's committed rows. | Accepted |
| (environment) | [MEDIUM] | tests/canonical/test-domain-doc-matrix.sh (MT01/MT02) and test-doc-set-mapping.sh (T15) fail in THIS VM because it ships mawk 1.3.4 not gawk; the test awk program errors with 'syntax error at or near ,'. Verified pre-existing: identical failure set on committed HEAD before task-034. task-034's rows are conditional and MT01/MT02 compare only required rows, so content is unaffected. Not a delivery-002 defect -- an env portability gap in the test harness. GATE: SPLIT into two distinct causes -- this row's MT01/MT02 half is gawk's 3-arg match($0,/re/,arr), which mawk cannot parse (reproduced in one line; locale-independent). T15 is NOT that: see the corrected row below. Content positively verified: MT01/MT02 recomputed in Python give software-cli=14 and software-web=14 required docs, byte-exact against the 14-doc seed set. | Accepted |
| (environment) | [MEDIUM] | Third suite hit by the same gawk gap: tests/canonical/test-dogfood-byte-identity.sh DBI01 reports 'manifest contains no .claude/ entries' because its awk manifest loader errors under mawk; the manifest genuinely holds 373 .claude/ entries. Verified pre-existing: identical failure at pre-merge commit 3ff30c3b in a clean worktree. Root cause is the VM shipping mawk 1.3.4 instead of gawk -- not a content defect and not caused by the master merge. GATE: confirmed same cause as MT01/MT02 -- 2 occurrences of gawk's 3-arg match(). Locale-independent. Remedy: install gawk in the CI/dev image, or rewrite the four match() calls to the 2-arg form plus substr($0, RSTART, RLENGTH). Not fixed here: tests/ is out of scope (feature-001 AC-3) and task-049 requires git diff origin/master -- tests/ to be 0 lines. | Accepted |
| task-035, task-037 | [HIGH] | canonical/skills/aid-config/SKILL.md:160 enumerates the document-creating create skills that append knowledge.doc_set as only /aid-create-roadmap, /aid-create-backlog and /aid-create-testing-strategy; /aid-create-architecture, /aid-create-stack and /aid-create-cicd also create-and-register a document under feature-004 SPEC 3b, so the list is now factually incomplete. NO TASK OWNS THE FIX: task-035 and task-037 are both scoped to verify-and-add-nothing there (a second write is a defect, not a duplicate), and delivery-001 task-011 authored the line before those three skills existed. Gate to complete the enumeration -- same shape as the delivery-001 gate fixing findings no task owned. GATE FIXED: enumeration completed to all six (added /aid-create-architecture, /aid-create-stack, /aid-create-cicd). Set derived, not guessed -- of seven aid-create-* bodies mentioning knowledge.doc_set, six append and aid-create-mvp's three mentions are all negative (SKILL.md:32-35, CC-5). mvp is now named as the one sibling NOT on the list so it cannot be re-added. | Resolved |
| task-040 | [MEDIUM] | The V23-style oracle wording -- grep '^phase:' returns absent or empty -- is no longer satisfiable by ANY allocated work: the new canonical/aid/templates/work-state-template.yml SEEDS a concrete phase: Describe default, where the previous markdown template carried an un-instantiated placeholder that read as no-committed-value. The property that is actually true and was verified across three allocations is that no skill run WRITES phase (all three equal the template default byte-for-byte). Oracle-wording defect, not a skill defect; same class as the work's three recorded unsatisfiable-criterion instances. Gate to re-word or accept. GATE: accepted as an oracle-wording defect. The DETAILs are immutable definitions and the template's seeded default is correct behavior predating this work. The true property was verified in every allocation across tasks 040-047: no run WRITES phase. | Accepted |
| task-041 | [MEDIUM] | .aid/knowledge/decisions.md has a pre-existing Contents-vs-body gap: section '## D26 -- No line-coverage metric' exists in the body with NO matching '## Contents' entry, so a V21-style comm -3 over that document is non-empty for reasons unrelated to this work. Verified pre-existing at HEAD (body D26=1, contents D26=0 before task-041 ran); task-041's own D27 write IS consistent in both directions. Not fixed here -- repairing another document's table of contents is outside the task's declared write set. Gate to fix or route to KB hygiene. GATE FIXED, and a SECOND gap was found in the same pass that the original finding missed: D13's Contents link text dropped the backticks its body heading carries. Both corrected; comm -3 over headings vs link texts is now empty in both directions, and the D26 anchor was verified to resolve by re-deriving the GitHub slug. | Resolved |
| task-045 | [MEDIUM] | Recurrence of task-040's phase-oracle defect, in a second wording: this task's AC asks for `grep -c '^phase: .'` over the run's work STATE to be **0**, but `canonical/aid/templates/work-state-template.yml:103` ships a literal `phase: Describe`, so allocation makes it 1 by construction and no conforming skill can make it 0. Now confirmed across two independent oracle phrasings (V23/V25-style and this one), which makes it a **class** defect in how "phase is not driven" was specified, not a one-off typo. Satisfied here in its checkable form -- each of the three runs left `phase` byte-identical to the template default, and all four bodies declare `phase` is not driven and none routes to `/aid-execute`. The gate should retire the grep form. GATE: grouped with the task-040 row as ONE class defect, not counted twice. Satisfied in its checkable form in both tasks. | Accepted |
| task-045 | [LOW] | The DETAIL describes F-noGate as "the same shape" as F-noC8, but F-noC8's domain is `methodology-tooling`, where `domain-doc-matrix.md:348` marks `quality-gates.md` **required** -- so a fixture that omitted it under that domain would be broken by construction, which is the exact failure F-noC8's own domain choice was reasoned to avoid. F-noGate was therefore built as `software-cli`, where `:153` marks it `conditional:project enforces merge-blocking quality gates`; owner and concern are identical in both tables, so no assertion changes. Recorded as fixture-construction reasoning, not a spec change. GATE: accepted as fixture-construction reasoning. Owner and concern are identical in both matrix tables, so no assertion changed. | Accepted |
| task-047 | [LOW] | Stale premise in the DETAIL: it asserts the baseline and comparison engines "differ in exactly the two hunks task-026 added and in nothing else". They now differ in **44** blocks -- task-026's two, plus the STATE.md -> STATE.yml migration that landed on this branch after the task was written. The AC's own rationale for preferring `master`'s *rendered* engine ("would differ from the run-2 engine in more than the two hunks under test") therefore now condemns its own prescribed baseline. Not a failed criterion: the row's result is **identity** of the slot set, and identity across a superset of engine differences is a stronger result than identity across two hunks -- 42 extra differing blocks had the chance to move the slot set and none did. Gate should refresh the premise wording. GATE: SUPERSEDED by the stale-local-master root cause below. Against origin/master the engine diff IS exactly +9/-0 in two hunks, so the DETAIL's premise is true and the apparent 44-block drift was the stale ref, not the migration. | Accepted |
| task-049 | [MEDIUM] | Any AC phrased `git diff master -- <path>` is measured against whatever the LOCAL `master` ref points to, and in this worktree it was **100 commits stale** (`master` 75039593 vs `origin/master` a70316c6; `git rev-list HEAD..origin/master --count` = 0, so HEAD already contains all of it). Six of this task's diff rows read as failures against the stale ref and pass exactly against `origin/master` -- most sharply feature-005 V13, which showed the engine at `+130/-81` across 22 hunks instead of its true `+9/-0` across 2, and `git diff master -- tests/` at 33153 lines instead of **0**. The gate MUST re-run every `git diff master` row against `origin/master`; consider respelling the oracles that way so they cannot be read against a stale ref. GATE: accepted with a standing instruction -- this is the root cause of the two oracle-drift rows around it, and every diff row in task-049 was re-run against origin/master, where all pass as specified (tests/ = 0 lines; engine = +9/-0 in 2 hunks). Any future re-run of these oracles must name origin/master explicitly. | Accepted |
| task-049 | [LOW] | feature-004 V25's static half names `canonical/aid/templates/work-state-template.md`, which no longer exists -- it migrated to `work-state-template.yml` upstream, so the oracle's path is stale and a literal run diffs a deleted file. Read against the file that exists, the row passes: `git diff origin/master -- canonical/aid/templates/work-state-template.yml` -> 0 lines. Substance holds (this work did not touch the work-state template); only the path in the oracle needs refreshing. GATE: accepted. Read against the file that exists (work-state-template.yml) the row passes at 0 diff lines; only the path in the oracle is stale. | Accepted |
| task-049 | [MEDIUM] | Recurrence pointer, not a new defect: feature-004 V13's "all five doc-set suites green unmodified" conjunct cannot be satisfied in this VM, because `test-domain-doc-matrix.sh` (MT01/MT02) and `test-doc-set-mapping.sh` (T15) need gawk and the VM ships mawk 1.3.4 with no gawk. Re-confirmed pre-existing by running both in a detached worktree at `master` -- identical failure there -- and both are byte-unmodified by this work (0 diff lines vs origin/master). The other three suites PASS. Same root cause as the two `(environment)` rows above; the gate should adjudicate all of them together. GATE: resolved by the two environment rows above, which between them account for both failures with distinct proven causes. V13's substantive claim holds independently: 14 seed files, and git diff origin/master -- tests/ = 0 lines. Under LC_ALL=C, four of the five pass. | Accepted |
| (gate, split from the MT01/MT02 row) | [MEDIUM] | Accepted | **Corrected attribution.** `tests/canonical/test-doc-set-mapping.sh` T15 was logged under the gawk cause, but that file contains **zero** 3-argument `match()` calls. Its real cause is **locale collation**: T15 compares `$(echo "$filenames" \| sort)` against a hard-coded string in C-locale order (`README.md` first), while this VM runs `LC_ALL=en_US.UTF-8`, where `sort` collates case-insensitively and puts `architecture.md` first. GATE: proven -- under `LC_ALL=C` the suite passes **21/21, 0 failures**, so the doc-set content is correct and only the comparison is locale-sensitive. Byte-unmodified by this work (0 diff lines vs `origin/master`). Not fixed here for the same feature-001 AC-3 / task-049 reason as the gawk rows; the remedy is a one-line `LC_ALL=C` on that comparison, named here so the owner does not re-diagnose it as a gawk problem. |
