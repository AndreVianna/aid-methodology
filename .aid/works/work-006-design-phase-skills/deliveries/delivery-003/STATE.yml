delivery_state: Blocked
gate_tier: "Large"
gate_grade: "D+"
gate_timestamp: "2026-08-15T06:35:00Z"
ticket_ref: "--"

delivery_lifecycle:
  updated: '2026-08-10T17:52:49Z'
  block_reason: --
  block_artifact: --
delivery_gate:
  issue_list:
    - 'Complexity Score: 57 (tier Large). 25 tasks: 23 Done, 2 Blocked, 0 Failed.'
    - 'Cycle 4 (CI read-back). The PR''s checks were read and four failures fixed -- three of them THIS delivery''s own regressions, invisible to every oracle the plan named: six stale counts caught only by the site-side guard, a description sweep that had stripped code spans down to 2 across 111 skills, and all 36 new skills missing from SHORTCUT_KIND_MAP in both dashboard twins. Plus a locale-sensitive INDEX.md build. CI went 5/9 -> **8 of 9 green**; the ninth is coverage-parity, the known blocker. Recorded as ledger row 9 (Fixed). **The grade does not move: still D+.** A FIX cycle only lifts a grade by closing a PENDING finding, and row 7 stays Pending. What the pass bought is measured by simulation rather than asserted: with row 9 left Pending the grade today would be **D** rather than D+, and after the bootstrap it would reach only **D+** rather than **A+**. So the CI read-back protected the ceiling instead of raising the floor -- without it, closing the coverage baseline would NOT have been enough to pass the gate.'
    - 'Cycles: 3. Cycle 3 CLOSED criterion 10: the owner asked whether /aid-summarize could be run here, it was, and it completed end to end -- kb.html regenerated to 23 sections over the resolved 21-doc set (was 21 over 19), graded **A+ / A+** (Machine 70/70 with validate-html-output 21/21; Human 30/30), with V1 -- the mandatory visual gate -- performed by the repository OWNER in a real browser, since Playwright is absent and T1/T2/T3 stayed SKIPPED. No automated visual pass is claimed anywhere. Ledger row 8 moves Pending -> Fixed and the grade with it, **D -> D+**. Still BELOW the floor of A, because row 7 remains Pending: criterion 7 needs a coverage-parity workflow_dispatch run that cannot be triggered from here. That is now the SINGLE remaining blocker on the whole work. Two of my own errors were corrected during the cycle and are recorded rather than quietly fixed: the task-071 sizing (72%% of kb.html is mechanical shell, so the authored load was 157 KB not 671 KB, and staging in gitignored scratch meant an incomplete attempt was always a no-op) and a duplicated export payload in the first assembly. The cycle also caught one more instance of the aid-graph off-by-one -- roadmap.md said 112 skill descriptions -- in a document tasks 065-069 did not scan. tech-debt W1-11''s kb.html half is closed; W1-2''s survivor stands.'
    - 'Cycles: 2. Cycle 2 was run on the owner''s challenge that a D should trigger another FIX/REVIEW pass. It did not move the grade, and the reason is recorded rather than assumed: a FIX cycle can only close a finding by doing the work, and re-reviewing an unchanged tree reproduces the same score. Row 7 was re-tested and is provably immovable here -- pwsh is absent with no route to it (no root, no apt candidate, no static binary, no dotnet), and coverage-parity''s exit-3 guard is deliberate because a baseline captured with a runtime absent MIS-REPORTS runtime-guarded assertions, so forcing it would produce a wrong baseline rather than a late one. Row 8 was re-sized and the earlier sizing in its IMPEDIMENT was found WRONG and corrected: the 671 KB figure is 77%% mechanical shell (492 KB script + 35 KB CSS), and the authored content is 157 KB across 21 sections, median 4.6 KB -- comparable to, not four times, what the last 24m20s run produced. The claim that attempting it risked a degraded artifact was also wrong: GENERATE stages into gitignored scratch and the assembler writes kb.html only at the end, so an incomplete attempt leaves the file untouched. Row 8 is therefore a multi-turn authoring job, not a disproportionate one, and the severities were NOT re-graded to clear the floor -- each still blocks a declared gate criterion.'
    - 'Cycles: 1. Gate grade **D**, BELOW the configured floor of A -- recorded as-is rather than massaged. Two [HIGH] findings are Pending, and each corresponds to a BLUEPRINT criterion that is genuinely NOT closed: criterion 7 (the coverage-baseline re-bootstrap, task-063) and criterion 10 (the kb.html regeneration, task-071). The delivery does NOT pass its gate and the work cannot ship until both land.'
    - '8 findings: 3 Fixed, 3 Accepted, 2 Pending. FIXED: (1) the delivery-wide directory off-by-one -- 112/18 in the plan versus 111/17 on disk, caused by the upstream aid-graph removal, confirmed by four independent derivations and stated correctly on every downstream surface; (2) the folded-versus-raw description oracle, fixed at source and by changing every later neighbour assertion to read the YAML-parsed value; (3) the last arrow sequence in the roster, fixed AT THE GATE because no task owned it -- task-059 was comments-only, task-069 count-only, task-072 writes nothing -- by rewriting aid-create-dashboard''s catalog intent as prose, then rebuilding, re-rendering, resyncing both dogfood trees and regenerating the site pages, after which the roster carries zero arrow sequences and all three count suites are green.'
    - 'ACCEPTED with rationale: the deleted test-graph-skill-registration.sh oracle (its GR01.a3 role discharged by parsing each frontmatter as YAML; authoring a replacement suite is barred by feature-001 AC-3, so it is routed); the Use-when detector note (both phrasings stand as better English, and task-072 matched the directive rather than the conjunction -- 111/111 carry a trigger); and the duplicate G5 comment-block label (presentational -- the group: field is correct on all four rows, so the header was made to state the truth rather than the rows moved).'
    - 'What the two Pending findings block, stated so nothing is quietly counted: criterion 7 needs a coverage-parity workflow_dispatch run with bootstrap: true -- impossible here because gh is read-only and pwsh is absent (collect would exit 3) -- with the predicted delta already verified locally (+144 rows; a/b/c 59->95, d 58->94, e/f/g unchanged at 34). Criterion 10 needs a full authored /aid-summarize GENERATE of a 671 KB / 21-section artifact, roughly 3.9x what the task was sized against; hand-patching its five stale strings is explicitly forbidden and was not done, so kb.html is byte-untouched. Both blockers are cost or capability, not correctness, and each carries a runbook in its task IMPEDIMENT.md.'
    - 'Also carried forward: tech-debt.md W1-11 keeps its kb.html survivor (task-067 deliberately did not close that half), and task-074 reports its kb.html-verdict criterion as UNMET rather than papered over, because task-071 wrote no Summarization History row to cite. No automated visual pass is claimed anywhere -- Playwright is absent from the summarize package, so V1 is an orchestrator step.'
qa:
  - id: 1
    category: 'Requirements / Plan validity'
    impact: 'Required'
    state: 'Answered'
    context: |-
      `origin/master` (the work-004 merge) **retired the repo-wide count guard** --
      `tests/canonical/check-skill-counts.mjs` is deleted (a ~462-line full delete). Its
      replacements are `tests/canonical/test-doc-counts.sh`, scoped to the public-facing docs
      only, plus criterion `G-01` in `authoring-conventions.md`, which is a criterion a reviewer
      applies rather than a guard that runs. delivery-003 was planned against the retired guard
      and still names it in **12 of its 25 task DETAILs** (task-050, 059, 062, 063, 065, 066,
      067, 068, 069, 070, 071, 072), in its own BLUEPRINT, and in four feature SPECs
      (feature-002 x5, feature-003 x3, feature-005 x2, feature-006 x5). **task-069 is the sharp
      case: its entire subject is "The count guard extended, its two ratchets raised, and the
      replay driven to zero" -- it is unexecutable as written, because the artifact it extends
      no longer exists.** Several other tasks use the guard as their verification oracle, so
      those oracles are now dangling rather than merely stale.
    suggested: |-
      Do not silently re-point the oracles. This is a re-planning decision, not a
      gate fix: either (a) re-scope task-069 and the dependent oracles onto
      `test-doc-counts.sh` + `G-01`, accepting that count-bearing statements inside `canonical/`
      and `.aid/knowledge/` are now reviewer-enforced rather than guarded; or (b) reinstate a
      count guard for the skill corpus, which reverses an owner decision another work just took;
      or (c) drop the count-ratchet obligations from this delivery and route them to tech-debt.
    answer: |-
      **Owner decision (2026-08-14): option (a).** Re-scope task-069 and the dependent
      oracles onto `tests/canonical/test-doc-counts.sh` (public-facing docs, a guard that runs and
      **derives** its counts, so there are no ratchets to raise) plus criterion `G-01` in
      `authoring-conventions.md` (count-bearing prose inside `canonical/` and `.aid/knowledge/`,
      reviewer-applied at severity `MINOR`). The accepted trade is that KB/canonical count claims are
      reviewer-governed rather than guarded. A count guard is **not** reinstated.
    applied_to: |-
      `RESCOPE-COUNT-GUARD.md` (the authoritative record, incl. the 14-assertion /
      10-file work list that replaces the ratchet obligations and the mapping for every stale
      citation); task-069 re-scoped end to end (title, Type DOCUMENT, Scope, all criteria);
      guard-running oracles re-pointed in task-065, task-068, task-070 and task-072; the
      count-comment reasoning corrected in this delivery's `BLUEPRINT.md`. Dropped as having no
      successor: the `CLAIMS` extension, `SUPERSEDED`, `MARKER_CAP`, `CLAIM_FLOOR` and the stage-2
      replay. delivery-002 is unaffected.
