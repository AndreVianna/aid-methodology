# task-058 EVIDENCE -- fifteen `design` grid and `/aid-brainstorm` descriptions

REQUIREMENTS **AC-12**. Slice 7 of 7, which closes the hand-authored set.

## 1. The fifteen

Same treatment as slices 5 and 6: these already met AC-12's other three checks, so the trigger is
**inserted** after the first sentence, preserving delivery-002's CC-9 negative routes by
construction.

| Skill | chars (cap 1024) | trigger | banned | arrow-seq | neighbours lost |
|---|---|---|---|---|---|
| `aid-design-api` | 596 | yes | none | no | none |
| `aid-design-ui` | 717 | yes | none | no | none |
| `aid-design-theme` | 582 | yes | none | no | none |
| `aid-design-cli` | 566 | yes | none | no | none |
| `aid-design-data-model` | 627 | yes | none | no | none |
| `aid-design-data-pipeline` | 586 | yes | none | no | none |
| `aid-design-messaging` | 568 | yes | none | no | none |
| `aid-design-integration` | 639 | yes | none | no | none |
| `aid-design-job` | 582 | yes | none | no | none |
| `aid-design-config` | 699 | yes | none | no | none |
| `aid-design-infra` | 718 | yes | none | no | none |
| `aid-design-test` | 689 | yes | none | no | none |
| `aid-design-document` | 679 | yes | none | no | none |
| `aid-design-dashboard` | 567 | yes | none | no | none |
| `aid-brainstorm` | 720 | yes | none | no | none |

**15/15 under the cap, 15/15 carry a trigger, 0 neighbours lost** -- including the three pairs
delivery-002 closed and task-055 re-verified from the other side: `aid-design-ui` <->
`/aid-prototype-ui`, `aid-design-document` naming both `/aid-document` and `/aid-create-document`,
and `aid-brainstorm` <-> `/aid-research`.

Each trigger names the state the work is in, which is what separates a `design` row from its
`create` sibling -- *"when an endpoint's shape is still being worked out and you want the contract
settled before anyone writes the handler"*, and so on for each artifact.

## 2. The sweep, closed: a whole-roster read

With slice 7 done, all **111** skills have been through the sweep -- 34 generated by task-051's
single template edit, 77 hand-authored across slices 1-7. (The DETAILs say 34 + 78 = 112; the
hand-authored figure is **77** and the roster **111**, because `aid-graph` was removed upstream --
the same off-by-one task-050 established and logged for the whole delivery.)

```
=== AC-12 over the whole roster (111 skills) ===
check 1 -- over the 1024 cap : NONE
check 2 -- banned forms      : NONE
check 2 -- arrow sequences   : ['aid-create-dashboard']     <- see below
trigger clause present       : 109/111
hyphen-broken skill names    : NONE
```

**The two "missing" triggers are false negatives of the detector, not gaps.** Both carry a real
trigger in a phrasing the `Use ... when` regex does not match:

- `aid-define` -- *"Use this skill **once** /aid-describe has produced an approved
  REQUIREMENTS.md..."*
- `aid-monitor` -- *"Use this skill **after** a deployment, on a schedule, or **whenever**
  something looks wrong in the field."*

Both are better English than a forced "when", so the phrasing stands and the detector is what
needs widening -- recorded so task-072's whole-roster sweep does not re-report them as defects.

**The one arrow sequence is inherited from the catalog, and is not this task's to fix.**
`aid-create-dashboard` is a **generated** doorway, so its description leads with its catalog row's
`intent`, which reads *"Build a durable dashboard / BI view (source -> visualization ->
publish/refresh)."* That is a **data flow**, not a state machine -- the same ambiguity slice 3 hit
with the ADR and runbook outlines, which were rendered as prose. The fix belongs in the catalog
row's `intent`, which is outside this task's declared writes (this slice owns fifteen `SKILL.md`
descriptions). Logged for the delivery.
