# Delivery Issue Log -- delivery-003

> Deferred findings from per-task quick checks. Consumed by the per-delivery
> quality gate as prior context. Not graded -- grade.sh runs only on the
> gate reviewer's own issue list.

| Source task | Severity | Description | Status |
|-------------|----------|-------------|--------|
| task-050 | [HIGH] | **The delivery's directory count is off by one throughout, and this delivery is the one that states it.** BLUEPRINT criterion 4 and its tasks expect **112** skill directories and "eighteen curated skills that own no row"; the true figures are **111** and **17**. Cause established, not guessed: `aid-graph` was removed upstream (`b8b01b1b`) and it was a **curated, rowless** skill -- `git show b8b01b1b^:...shortcut-catalog.yml \| Resolved | Open |
| task-052 | [MEDIUM] | Stale oracle citation, and it will recur across the description slices: `tests/canonical/test-graph-skill-registration.sh` and its `GR01.a3` (asserts the `description` key's presence) are cited as a guard, but that suite was **deleted upstream** by commit `b8b01b1b` "Completely remove aid-graph skill" -- the same removal behind task-050's directory-count correction -- and is absent from `origin/master`. It has no successor: `tests/canonical/test-frontmatter-lint.sh` does not mention `description` at all (`grep -ci 'description'` -> 0), so nothing in the suite tree now asserts that key. Discharged in task-052 by parsing all nine files as YAML and asserting a non-empty `description` key directly, which is precisely what GR01.a3 asserted. Tasks 053-058 cite the same suite and should use the same substitute rather than report a check as covered by a suite that cannot run. The gate should decide whether the lost assertion deserves a home. | Accepted |
| task-054 | [MEDIUM] | **The obvious oracle for "no neighbour was dropped" is the wrong one.** A folded YAML scalar rejoins its lines with a space, so a skill name split at a wrap point does not survive the fold: `/aid-\n  update-kb` becomes the literal `/aid- update-kb`. A grep over the RAW description block still reports the name present, because the substring is there; only a check against the PARSED (folded) value sees the break. `textwrap.fill` breaks on hyphens by default, which damaged four names in slice 3 -- `/aid-update-kb` and `/aid-create-diagram` in `aid-create-document`, `/aid-create-document` in `aid-create-diagram` and in `aid-document-changelog`. Fixed at source (`break_on_hyphens=False`) and every neighbour assertion from slice 3 onward is made against the YAML-parsed description. A repo-wide scan confirms slices 1-2 were unaffected and 0 remain. Recorded because task-072's whole-roster pair sweep must use the folded value too, or it will report routing that no consumer can actually follow. | Resolved |
| task-058 | [LOW] | One arrow sequence survives the description sweep, inherited rather than authored: `aid-create-dashboard` is a **generated** doorway, so task-051's template leads it with the catalog row's `intent` -- `"Build a durable dashboard / BI view (source -> visualization -> publish/refresh)."` That is a **data flow**, not a state machine, and it is the same ambiguity slice 3 resolved for the ADR/runbook/guideline/standard outlines by rendering them as prose; a mechanical arrow check cannot tell the two apart. The fix is one edit to that row's `intent` in `canonical/aid/templates/shortcut-catalog.yml` followed by a regeneration -- outside task-058's declared writes (fifteen SKILL.md descriptions), and naturally task-059's or task-069's, both of which write that file. Whole-roster state otherwise clean: 0 over the cap, 0 banned forms, 0 hyphen-broken names across all 111. | Resolved |
| task-058 | [LOW] | Detector note for task-072, not a defect: a `Use ... when` regex reports `aid-define` and `aid-monitor` as lacking a trigger clause. Both have one, in better English than a forced "when" -- `aid-define` reads "Use this skill **once** /aid-describe has produced an approved REQUIREMENTS.md", and `aid-monitor` reads "Use this skill **after** a deployment, on a schedule, or **whenever** something looks wrong in the field." The whole-roster sweep should match the imperative *form* (an explicit "Use this skill ..." directive) rather than the single conjunction "when", or it will re-report these two. | Accepted |
| task-059 | [LOW] | Two comment blocks in `canonical/aid/templates/shortcut-catalog.yml` are both labelled **G5** -- "G5: Change + Refactor family" and, below it, "G5: Remove + Deprecate + Migrate" (a v2.1.0 follow-on). delivery-002's four foundation rows `aid-update-{architecture,stack,testing-strategy,cicd}` were placed under the **second**, so the `aid-update*` rows split 15 / 4 / 1 / 1 / 1 across five blocks rather than the 19 a naive `12 + 7` would give. Their `group:` **field** is `G5` on all four, so the site's functional grouping is correct and nothing is broken -- the misplacement is presentational, the same class as delivery-002's adjudicated grid-row finding. task-059's scope is comments-only ("any change to a row -- this task edits comments only"), so the header was made to state the truth rather than the rows moved. The gate should decide whether to relocate the four rows and simplify the parenthetical, or leave both as they stand. | Accepted |
| task-063 | [HIGH] | **BLOCKED, and it gates a CI lane.** The coverage baseline must be re-bootstrapped by a `coverage-parity` **`workflow_dispatch`** run with `bootstrap: true`, then `.tsv` and `.meta` committed together. Neither half is possible here: the `gh` CLI in this environment is read-only so no workflow run can be triggered, and `collect` requires `pwsh`, which is **absent** (node 22 and python 3.12 are present), so it would exit 3 (*required runtime absent*). Hand-editing is not a fallback -- the runbook forbids it and `.meta` is a provenance sidecar that would desynchronise; the committed baseline's own header shows that corner being cut once before, for the same missing-pwsh reason, when aid-graph was removed. Everything verifiable locally WAS verified: the predicted delta is exact -- `a`/`b`/`c` 59 -> 95, `d` 58 -> 94, `e`/`f`/`g` 34 unchanged, **+144** total -- measured by the collector's own `PASS:`/`FAIL:` rule (a first pass counting raw output read 94 `e` and would have reported the prediction wrong; the 60 `repurpose` exemptions are `log` lines the collector never indexes). Until the CI run lands, `coverage-parity` is expected **red** on any `tests/**` push, because the workflow's advisory language applies only while the baseline is absent and it is present. Runbook in `tasks/task-063/IMPEDIMENT.md`. | Open (gate: Pending -- criterion 7 not closed) |
| task-071 | [HIGH] | **BLOCKED, and it leaves BLUEPRINT criterion 10 open.** `kb.html` needs a full authored `/aid-summarize` GENERATE. Re-measured, the artifact is **671 KB / 21 sections / 4199 lines**, roughly **3.9x** the 178 KB / 20 sections the task was sized against -- and the last recorded GENERATE took **24m20s** at that smaller size. Producing it inside this run would yield a degraded file that overwrites a curated one whose only defect is five stale strings, which is a strictly worse trade. **Hand-patching is explicitly forbidden** by the task (the next assembler run silently overwrites a hand edit, and the other twenty sections would still describe a 75-skill roster), so it was not done: `git diff --exit-code -- .aid/knowledge/kb.html` is clean. Staleness measured for the eventual run to assert against: `'75 skills'`=**3** -> must become 0, `'58-row'`=**2** -> must become 0, `'34 verb-first'`=**2** -> must stay >= 1. The blocker is **cost, not feasibility**: GENERATE reads `.aid/knowledge/*.md` directly and writes its own gitignored `.aid/.temp/summarize/` input tree (the false premise in `W1-11` that it could not be regenerated was corrected in task-067), and the KB it reads is final after tasks 065-067 and task-070. Consequences to carry: criterion 10 open, and `tech-debt.md` `W1-11` keeps its `kb.html` survivor. Runbook in `tasks/task-071/IMPEDIMENT.md`. | Open (gate: Pending -- criterion 10 not closed) |
