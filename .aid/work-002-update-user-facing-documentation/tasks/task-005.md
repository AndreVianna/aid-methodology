# task-005: examples/ — brownfield full-path worked example

**Type:** DOCUMENT

**Source:** work-002-update-user-facing-documentation → delivery-001

**Depends on:** task-001

**Scope:**
- Rebuild **from scratch** a brownfield worked example that follows the **full path** (discover → interview → specify → plan → detail → execute), under `examples/`.
- Show a realistic existing-codebase problem applying AID's full path in **tutorial style**: step-by-step, explaining the key aspects of each step (discovery/KB, full interview, feature SPECs, plan, tasks, execution).
- Accurate against the task-001 fact-set.

**Acceptance Criteria:**
- [ ] A brownfield full-path example exists under `examples/`, rebuilt from scratch, reflecting a realistic problem. (SPEC AC: examples rebuilt.)
- [ ] It is tutorial-style: step-by-step with explanation of the key aspects of each step, and it exercises the full path. (SPEC AC: examples — tutorial style.)
- [ ] Commands, phase/skill names, and structure match the current methodology (task-001 fact-set). (SPEC AC: reconciled & accurate.)
- [ ] Linked from the examples index per the IA. (SPEC AC: information architecture.)
- [ ] No methodology or behavior changes. (SPEC AC: docs-only.)
- [ ] All quality gates pass (see SPEC § Quality Gates).
