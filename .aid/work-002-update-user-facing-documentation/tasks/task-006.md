# task-006: examples/ — brownfield lite-path worked example

**Type:** DOCUMENT

**Source:** work-002-update-user-facing-documentation → delivery-001

**Depends on:** task-001

**Scope:**
- Rebuild **from scratch** a brownfield worked example that follows the **lite path** (AID lite: triage → condensed intake → task breakdown → lite review → execute), under `examples/`.
- Show a realistic small existing-codebase change applying AID's lite path in **tutorial style**: step-by-step, explaining the key aspects of each step and *why the lite path was chosen* over the full path.
- Accurate against the task-001 fact-set; this example doubles as the canonical illustration of the newly-documented lite path.

**Acceptance Criteria:**
- [ ] A brownfield lite-path example exists under `examples/`, rebuilt from scratch, reflecting a realistic problem. (SPEC AC: examples rebuilt.)
- [ ] It is tutorial-style: step-by-step with explanation of the key aspects of each step, and it exercises the lite path (incl. lite-vs-full triage rationale). (SPEC AC: examples — tutorial style.)
- [ ] Commands, phase/skill names, and structure match the current methodology and the documented lite path (task-001 fact-set). (SPEC AC: reconciled & accurate.)
- [ ] Linked from the examples index per the IA. (SPEC AC: information architecture.)
- [ ] No methodology or behavior changes. (SPEC AC: docs-only.)
- [ ] All quality gates pass (see SPEC § Quality Gates).
