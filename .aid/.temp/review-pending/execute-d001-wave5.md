| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [HIGH] | Pending | .aid/knowledge/artifact-schemas.md | 381 | KB-09 — "Read but never stored" table declares `kb_baseline.{branch,tip_date}` are never written to settings.yml, directly contradicting the canonical skill instructions that write them | `canonical/skills/aid-discover/references/state-done.md:31` says "Write `.aid/settings.yml` `kb_baseline: {branch, tip_date}`"; `canonical/skills/aid-housekeep/references/state-summary-delta.md:220` says "Re-stamp `.aid/settings.yml kb_baseline.tip_date`" — both consumers WRITE, not merely READ |
| 2 | [HIGH] | Pending | .aid/knowledge/artifact-schemas.md | 362 | KB-09 — Grade modifier description "within a letter, `+` means exactly one finding at that severity" contradicts grade.sh: MINOR×1 → A (not A+); A+ requires TOTAL=0, not one MINOR finding | `bash .cursor/aid/scripts/grade.sh` with one MINOR Pending row outputs `A`, not `A+`; TOTAL=0 outputs `A+` — `modifier_for_count` at grade.sh:96 only applies to B–E bands; A band uses a separate `MINOR -le 5 → A` branch (grade.sh:246). CORRECTION.md correctly shows MINOR×1→A, making artifact-schemas.md internally inconsistent with the CORRECTION.md as well. |
| 3 | [MEDIUM] | Pending | canonical/skills/aid-discover/references/state-generate.md | — | task-013 AC3 — "The generator is re-run in the same commit" not met: the canonical state-generate.md change landed in commit 0e7693f83 but `.cursor/skills/aid-discover/references/state-generate.md` and `.claude/` counterparts were absent from that commit and only appeared in the subsequent task-016 commit 4a932a3f2 | `git show 0e7693f83 --name-only \| grep '^\\.cursor/skills'` returns nothing for skill files; same files appear in `git show 4a932a3f2 --name-only`; render parity is correct in the final state but the same-commit constraint was split across commits. No criterion id (shell artifact per brief) |
| 4 | [LOW] | Pending | canonical/aid/scripts/summarize/kb-html-claims-check.sh | 100 | Task-016 script silently skips claim class 1 (artifact names) when `canonical/aid/templates` is absent from CWD, producing no error and no warning — the TEMPLATE_DIR is hardcoded and not documented as a requirement | Run from /tmp with --kb-dir pointing at the live KB: claims=2 (only classes 2+3), class 1 skipped, exit 0 even though artifact-name checks never ran. Zero-claims guard does not fire because classes 2+3 still extract claims. Usage header lists no CWD requirement. No criterion id (shell artifact per brief) |
