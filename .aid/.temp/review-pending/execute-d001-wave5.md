| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [HIGH] | Fixed | .aid/knowledge/artifact-schemas.md | 381 | KB-09 — "Read but never stored" table declares `kb_baseline.{branch,tip_date}` are never written to settings.yml, directly contradicting the canonical skill instructions that write them | cycle-2 FIX: `kb_baseline` removed from "never stored" table; new paragraph in alias section says baseline IS stored. |
| 2 | [HIGH] | Fixed | .aid/knowledge/artifact-schemas.md | 362 | KB-09 — Grade modifier description "within a letter, `+` means exactly one finding at that severity" contradicts grade.sh: MINOR×1 → A (not A+); A+ requires TOTAL=0, not one MINOR finding | cycle-2 FIX: replaced with explicit table (no findings→A+, 1-5 MINOR→A, 6+MINOR→A-, 1 LOW→B+). Every row exercised: `grade.sh` with MINOR×1..5 all output A; MINOR×6,9 output A-; TOTAL=0 output A+; LOW×1 output B+. ✓ |
| 3 | [MEDIUM] | Fixed | canonical/skills/aid-discover/references/state-generate.md | — | task-013 AC3 — "The generator is re-run in the same commit" not met: the canonical state-generate.md change landed in commit 0e7693f83 but `.cursor/skills/aid-discover/references/state-generate.md` and `.claude/` counterparts were absent from that commit and only appeared in the subsequent task-016 commit 4a932a3f2 | cycle-2 FIX (recorded, not code-fixed): RECORD.md criterion 10 documents cause (rsync absent + stderr redirected to /dev/null = silent no-op) and two rules (never redirect sync stderr; verify by diff not by exit status). `which rsync` → exit 1 confirms rsync absent. `diff -rq .cursor profiles/cursor/.cursor | grep '^Files'` → empty; same for .claude. Parity verified clean; recording is the right disposition for a same-PR process issue with no live defect. |
| 4 | [LOW] | Fixed | canonical/aid/scripts/summarize/kb-html-claims-check.sh | 100 | Task-016 script silently skips claim class 1 (artifact names) when `canonical/aid/templates` is absent from CWD, producing no error and no warning — the TEMPLATE_DIR is hardcoded and not documented as a requirement | cycle-2 FIX: TEMPLATE_DIR now resolved from `BASH_SOURCE[0]`; `--template-dir DIR` flag added; missing dir exits 2 with message. Run from /tmp: `bash canonical/…/kb-html-claims-check.sh .aid/knowledge/kb.html --kb-dir /…/.aid/knowledge` → class 1 fires, `[FAIL] kb.html names STATE.md (16x)`, exit 1. No remaining silent-skip path found: process-substitution invocation emits `template dir not found` and exits 2. |
| 5 | [HIGH] | Fixed | .aid/knowledge/artifact-schemas.md | 394 | KB-09 — New kb_baseline paragraph claims FR35 and FR36 name "the same producer write as `knowledge.{source,last_update}`" — directly contradicted by the authoritative skill references: FR35 (`state-done.md:31`) writes to `kb_baseline: {branch, tip_date}` only, and FR36 (`state-summary-delta.md:220`) re-stamps `kb_baseline.tip_date` only; neither writes to `knowledge.source` or `knowledge.last_update` | cycle-3 FIX: claim withdrawn entirely. Paragraph now cites `dashboard/reader/parsers.py:parse_kb_baseline` as authority (scans `knowledge:` first, falls back to `kb_baseline:` for pre-migration files, calling it legacy explicitly). Counts re-derived: `state-done.md` 7 `kb_baseline` / 0 `knowledge:`; `state-summary-delta.md` 5 / 0. No shared-write-path claim survives. Tech-debt L6 logs the producer/config disagreement. `read-setting.sh` has no `kb_baseline` case. Citation lint: 8 violations (pre-existing lines 102/104); L6 at line 60 adds zero new violations. Render parity clean; baseline 13 suites. ✓ |
| 6 | [LOW] | OOS | tests/review-cost-meter.sh | — | Brief parser drops indented paths not preceded by `- ` bullet; a mixed list (some bulleted, some not) silently undercounts surface total without warning | OOS — not in wave-5 scope; route to task team for a follow-on fix or test coverage. A list with NO bullets warns and records 0 (safe); only a MIXED list fails silently. |
| 7 | [MINOR] | Pending | .aid/knowledge/artifact-schemas.md | 399 | KB-09 — Code block headed `$ sed -n '377,383p' dashboard/reader/parsers.py` shows 5 lines of output, but the actual command produces 7 lines; line 377 ("Tolerant line-scan, reusing the parse_project_name posture (parsers.py:148):") and line 383 ("- Absent/unparseable -> None (skip freshness, stay approved; FF-A2)") are silently omitted from the presented excerpt | `sed -n '377,383p' dashboard/reader/parsers.py \| wc -l` → 7. Doc shows 5. The omitted lines do not contradict the substance (knowledge: first, kb_baseline: fallback), but the block misrepresents the command's verbatim output. |
