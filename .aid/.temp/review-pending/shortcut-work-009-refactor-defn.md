| # | Severity | Status | Doc | Line | Description | Evidence |
|---|---|---|---|---|---|---|
| 1 | [CRITICAL] | Pending | SPEC.md | 149-155 | SP-8 is unsatisfiable and contradicts SP-9/L-3/L-6: it demands all four payloads (legacy-markdown fixture and converted fixture, each read by both twins) be "equal on every rendered field", but the same SPEC deletes the markdown parsers and specifies that a legacy STATE.md is diagnosed, not parsed | SPEC.md:643 "Deleted for state files. Replaced by one legacy *detector*: a sibling STATE.md with no STATE.yml yields `_minimal_work_model` plus a `parse_warning`"; SPEC.md:751-752 "The readers keep exactly one line of legacy awareness: a STATE.md with no sibling STATE.yml is diagnosed, not parsed"; SPEC.md:158-160 (SP-9) restates it; reader.py:1586 "Return a minimal WorkModel for a work folder with no parseable STATE.md" cannot carry per-task state/review/elapsed/notes, lifecycle-history rows, Q&A entries or derived counts, so field equality is impossible. SP-8 also concedes the legacy pair raises a `parse_warning` while demanding full equality, self-contradictory standalone. Restated unchanged in BLUEPRINT.md:174-182 (gate criterion) and narrows REQUIREMENTS.md:420-425 AC-2 ("no parse_warning is raised for either format") to "the converted pair" while SPEC.md:101 claims "Every REQUIREMENTS acceptance criterion is covered" |
| 2 | [CRITICAL] | Pending | SPEC.md | 687-712 | dashboard/scripts/delete-pipeline.sh is an unaccounted state-file consumer whose Running guard fails OPEN after the rename, permitting deletion of a running pipeline's work folder or worktree | delete-pipeline.sh:348 reads `lifecycle` from the candidate work's STATE.md and :349 refuses only when the value equals Running; its own `_frontmatter_value` at :164-167 returns empty on a missing file, so an unretargeted path yields an empty LIFECYCLE and the guard never fires. Its awk also carries the NR==1 fence guard (:168) that SPEC.md:691 removes only for enumerate-works.sh, so even a retargeted path returns empty against a fence-less YAML file. Only one copy exists (no canonical twin, no render). The file appears in NO document: absent from SPEC Layers and Components L-1..L-10, from REQUIREMENTS section 4 In Scope and section 8 Dependencies, from BLUEPRINT Scope, and from all 19 PLAN tasks, yet SPEC.md:850 lists its test test-delete-pipeline.sh as an in-scope suite |
| 3 | [HIGH] | Pending | SPEC.md | 571 | The dashboard server layer is an unaccounted consumer: six AID_STATE_FILE write-path call sites plus the raw-state viewer are named in no component, no scope bullet and no task, and SP-15's search surface excludes dashboard/ so no gate catches it | server.mjs:1242, :1280, :1555 each build AID_STATE_FILE as workDir plus STATE.md for writeback-state.sh (task.set-notes, pipeline Lifecycle=Completed, task.rename); server.py:1503, :1540, :1803 mirror them; unretargeted they make writeback-state.sh die exit 1 on a nonexistent file, silently breaking every dashboard edit surface (test_write_enabled_cross_runtime_parity.py is the existing oracle). home.html:5816 builds the fallback label from workId plus STATE.md, plus 8 UI strings. SPEC Layers and Components enumerates nine components (L-1 templates, L-2 writer, L-3/L-4 readers, L-5 shell readers, L-6 migration, L-7 prose, L-8 review exclusion, L-9 tests); none is the dashboard server. BLUEPRINT.md:49-53 lists only reader.mjs; REQUIREMENTS.md:391 names server.mjs and server.py in the dependency inventory only, with no FR, no AC and no task. SPEC.md:196-197 and BLUEPRINT.md:220-228 scope SP-15's search to "every KB doc, skill, template, agent-context file, profile render and dogfood tree" |
| 4 | [HIGH] | Pending | SPEC.md | 375-482 | D-4's concrete target skeletons, the artifact task-002 implements, put trailing inline comments on writer-owned keys on about 30 lines, the exact shape D-3 and L-1 forbid because the writer destroys them on first write | SPEC.md:358-361 (D-3) "Trailing inline comments are permitted only on a line the writer will never rewrite, and forbidden on any key the writer may write ... WB_SET_FRONTMATTER_AWK replaces the *entire* line for the key it writes"; SPEC.md:582-583 (L-1) "no trailing inline comment on any writer-owned key (D-3)". Yet D-4 emits trailing comments on lifecycle (:390), phase (:391), active_skill (:392), updated (:393), pause_reason (:394), block_reason (:395), block_artifact (:396), ticket_ref (:397), delivery_state (:400), gate_tier (:401), gate_grade (:402), gate_timestamp (:403), started (:387), minimum_grade (:388), user_approved (:389), pipeline.path and pipeline.initiator (:385-386), the five tasks_lifecycle.task-NNN child keys (:430-434) and the whole per-task skeleton (:472-480), every one marked "[W]" (writer-owned). Verified against writeback-state.sh:619, which prints the whole replacement line |
| 5 | [MEDIUM] | Pending | SPEC.md | 323-349 | D-3's reject list contradicts its own permitted shape S5: it declares "a non-comment, non-blank line with no key: separator" malformed, but S5 permits "a sequence of scalars", used by delivery_gate.issue_list whose items are bare dash-prefixed values with no key: | SPEC.md:331 (S5) names "quick_check.findings, delivery_gate.issue_list ... or a sequence of scalars"; SPEC.md:437 comments issue_list as "severity-tagged strings"; SPEC.md:347 gives the reject row "a non-comment, non-blank line with no key: separator". Both twins build their parsers and the SP-1 conformance corpus from this table (SPEC.md:678-683), so a literal implementation drops every gate issue entry and the two twins can diverge on which rule wins |
| 6 | [MEDIUM] | Pending | SPEC.md | 736-741 | L-6 step 2 requires the conversion step to be both "WARN-not-fail per the engine's contract" and "a hard error, not a silent drop, on a non-placeholder DERIVED row", but the chosen engine always returns 0, so SP-3's "exits non-zero naming the file and section" cannot hold | bin/aid:2013-2015 documents _aid_migrate_repo: "Each step is WARN-not-fail: a step failure logs WARN and the next step runs; the function always returns 0 (SEC-4 / NFR12)"; SPEC.md:741 asserts both properties in one sentence; SPEC.md:121-123 (SP-3) and BLUEPRINT.md:151-152 both state the converter "exits non-zero". Consequence: whether the DERIVED data-loss guard actually blocks is undefined, and a WARN leaves that work in markdown while the stamp advances |
| 7 | [MEDIUM] | Pending | SPEC.md | 125-128 | SP-4's absolute "every line other than that key's own line is reproduced byte-for-byte, the resulting git diff is one line" contradicts L-2's create-parent-if-absent behavior, where writing one nested key adds two or more lines | SPEC.md:597 (L-2) "extended to the S3/S4/S5 paths a write can target (tasks_lifecycle.task-NNN.state, ...) -- create-parent-if-absent, insert-at-end-of-parent, exactly as the existing parent_seen logic already does one level up"; verified at writeback-state.sh:585-589, which prints the parent key line and then the indented child line (two new lines). Restated unchanged as a gate criterion in BLUEPRINT.md:153-156, so the gate as written fails on the first new-task write |
| 8 | [MEDIUM] | Pending | BLUEPRINT.md | 30-31 | Component count contradicts SPEC: BLUEPRINT states "The ten components are SPEC.md Layers and Components L-1 .. L-10" while SPEC states nine, and L-10 is an open-items register, not a component | SPEC.md:573 "This is where the restructure lives. Nine components, each with its current shape, its target shape, and what enforces the change."; SPEC.md:883 "L-10. Open items, and what is carried forward" is a carry-forward register (NFR-10 pending, staleness routed onward, A-1..A-6 disposition), not a deliverable. BLUEPRINT.md:70 then also maps "Render fan-out (FR-12, L-1/L-2)", double-counting L-1/L-2 |
| 9 | [MEDIUM] | Pending | PLAN.md | 47 | The dependency graph does not encode task-010 (convert live work trees) after task-017 (render fan-out and dogfood resync), so the SP-18 and C-6 "reader and CLI support before any conversion" invariant rests on an unstated edge | PLAN.md:47 gives task-009 as task-010's only dependency; the live pipeline writes state through the dogfood render of writeback-state.sh, which only task-017 updates (PLAN.md:268). Every wave in PLAN.md:59-69 is exactly the topological minimum of the dependency table (verified task-by-task), so the wave-7-before-wave-8 ordering is a by-product of that computation, not an encoded constraint, and compute-block-radius.sh or any edge-driven scheduler sees no ordering. PLAN.md:288 itself states the hazard ("a repo-local fix does not reach an already-installed aid until it is reinstalled") but no task or edge covers reinstalling the CLI that task-010 depends on |
| 10 | [LOW] | Pending | REQUIREMENTS.md | 72 | Citation is wrong by about 210 lines: list_reviewable is not at doc-set-resolve.md:101 | grep -n list_reviewable canonical/skills/aid-discover/references/doc-set-resolve.md = 288 (section heading), 296, 309, 311 (the function definition), 332, 335. Line 101 is the "Two independent axes -- concern vs. role" blockquote. The substance of the claim (STATE.md filtered out, enforced by test-kb-review-surface.sh RS03 at :68) is correct; only the line pointer is dead. SPEC.md:798-803 repeats the same mechanism without a line cite |
| 11 | [LOW] | Pending | SPEC.md | 593 | "Three awk programs collapse to one" undercounts by one; there are four awk program bodies, as SPEC's own table concedes by listing the section-replace awk "twice" | grep -n "awk -v" canonical/aid/scripts/execute/writeback-state.sh = 719, 725 (both WB_SET_FRONTMATTER_AWK, one program), 915 (positional table), 1056 (findings section-replace), 1259 (delivery-gate section-replace) = 4 distinct programs. SPEC.md:599 "Section-replace awk, twice (:1056-1081 findings, :1259-1284 delivery gate)". The three-count is repeated at REQUIREMENTS.md:49, SPEC.md:52, SPEC.md:68-70, PLAN.md:15-16, BLUEPRINT.md:17-18 and BLUEPRINT.md:42-43 |
| 12 | [LOW] | Pending | SPEC.md | 626 | "eight renders exist" contradicts the enumeration on the same line (five profiles plus two dogfood trees = seven) and the disk | The execute copy has 7 renders: profiles/antigravity, profiles/claude-code, profiles/codex, profiles/copilot-cli, profiles/cursor, plus this repo's .claude/ and .cursor/. The number is inherited from the source banner (writeback-state.sh:4), which is itself stale -- see row 20. Repeated at REQUIREMENTS.md:81 and :388 ("+ 8 renders"), while REQUIREMENTS.md:266-268 (FR-12) correctly enumerates 5 plus 2 |
| 13 | [LOW] | Pending | SPEC.md | 657 | "Nine join(workDir, STATE.md) sites move to STATE.yml" but only seven are enumerated, and seven is the correct in-scope count | grep -n STATE.md dashboard/server/reader.mjs join sites = 879 (kbDir, explicitly out of scope), 3062, 3416, 3986, 4189, 4272, 4321, 4710 = 7 in-scope plus 1 out-of-scope. SPEC lists exactly those 7 line numbers after saying "Nine". The label strings that also need retargeting (:3063, :3982, :4184, :5439) are named for the Python twin (SPEC.md:644 state_path_label) but not for Node |
| 14 | [LOW] | Pending | REQUIREMENTS.md | 273 | FR-13's candidate inventory mis-scopes the 51: that is the count for all of dashboard/, not for dashboard/**/tests/ | grep -rl STATE.md dashboard/reader/tests dashboard/server/tests = 37 files; grep -rl STATE.md dashboard/ = 51 files. REQUIREMENTS.md:26 states the same 51 correctly as "under dashboard/". task-001 triages the FR-13 inventory, so the wrong denominator lands in the change-set. The rest of the inventory verified exact: 47 files and 777 refs under tests/, 627 under dashboard/, and 89 / 73 / 27 / 27 / 15 / 87 per-file |
| 15 | [LOW] | Pending | SPEC.md | 849 | The in-scope suite test-work-state-template.sh is described as carrying "the template shape assertions WS01-WS07"; it carries WS01-WS09, and the two omitted ones are exactly the assertions the conversion breaks | grep -n WS0 tests/canonical/test-work-state-template.sh = WS01..WS09; WS08 "Rendered profile trees all contain the work-state-template Pipeline State header" and WS09 "No Status section/field names remain in any new template" both assert markdown section headings that FR-2b retires |
| 16 | [LOW] | Pending | SPEC.md | 598 | write_task_field_flat is called "85 lines" while the same cell cites the range :877-1022, which is 146 lines | The function spans line 877 (write_task_field_flat opening) to line 1022 (closing brace) = 146 lines; the embedded awk body alone is :915-1008. REQUIREMENTS.md:51 states the same "~85 lines". The cell calls this "the single largest deletion in the work", so the size claim is load-bearing |
| 17 | [LOW] | Pending | SPEC.md | 330 | D-3's shape table assigns interview.sections to S4, but D-4 emits it as S5 (a sequence nested under a mapping), so the SP-1 conformance corpus built from that table will not cover the shape actually on disk | SPEC.md:330 lists S4 (key: then 2-space dash-item then 4-space child) as used by "lifecycle_history, deploy, qa, interview.sections"; SPEC.md:405-412 emits interview: then 2-space sections: then a 4-space dash-item with 6-space children, which is S5's form (SPEC.md:331), and S5's own "Used by" column omits interview.sections. SPEC.md:351-352 then caps nesting at "three levels of mapping (S3) or a sequence at the second level (S5)", which the 6-space form sits at the edge of |
| 18 | [MINOR] | Pending | SPEC.md | 303, 586, 823 | Four citations are off by one line | state_schema.py:64 cited, but def _strip_scalar_quotes is at line 63; state_schema.py:83 cited, but def _looks_like_unfilled_placeholder is at line 82; shortcut-engine.md :720 and :751 cited for the two OUT OF SCOPE bullets, which begin at 721 and 752 (720 and 751 are the last lines of the preceding RUBRIC bullets). Every other line citation checked in these documents was exact |
| 19 | [MINOR] | Pending | REQUIREMENTS.md | 277-278 | The migration fixture is described as carrying state files "at work, delivery-001 and delivery-002 level"; it carries seven, including four per-task files | Under tests/canonical/fixtures/migrate/fixture/work-999-migration-test/ the STATE.md files are: work root, deliveries/delivery-001/, deliveries/delivery-001/tasks/task-001, task-002, task-004, deliveries/delivery-002/, deliveries/delivery-002/tasks/task-003. REQUIREMENTS.md:454-457 (AC-8) repeats the three-file description, which understates what an "every table row preserved" assertion must cover |
| 20 | [LOW] | OOS | canonical/aid/scripts/execute/writeback-state.sh | 4 | Shipped-source defect: the canonical writer's banner claims "EIGHT RENDERS EXIST" but seven do; the definition documents inherited the wrong number from it (see row 12) | Five profile renders of the execute copy plus this repo's .claude/ and .cursor/ dogfood trees = 7. Routing: the shipped script is not an artifact under review in this pass; route to task-007 (Collapse writeback-state.sh onto one YAML single-key write path), which rewrites this banner region anyway, or to .aid/knowledge/tech-debt.md if task-007 declines it |
| 21 | [LOW] | OOS | .aid/works/work-009-refactor/STATE.md | 18-20 | This work's live state file still carries un-instantiated template placeholders in three writer-owned frontmatter keys | Lines 18-20 hold the raw enum hint for gate_tier and the literal placeholder text for gate_grade and gate_timestamp, never replaced by writeback-state.sh. Routing: STATE.md is explicitly out of scope for this pass (process tracking, not a reviewable artifact); route to the shortcut-engine orchestrator's tracking-discipline step. Noted because SPEC.md:512-518 cites this same file as its evidence for the D-5 quoting rules |
