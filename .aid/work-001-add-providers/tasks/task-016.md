# task-016: Extend setup.ps1 — menu + copy blocks for Copilot CLI + Antigravity + AGENTS.md collision (one-to-one parity)

**Type:** IMPLEMENT

**Source:** feature-004-setup-and-nonregression → delivery-004

**Depends on:** task-009, task-010, task-011, task-014, task-004

**Scope:**
- Extend `/home/andre.vianna/projects/AID/setup.ps1` per SPEC §"Setup-Menu Extension (FR4) → `setup.ps1` edits" (steps 1–6) and §"AGENTS.md Multi-Install Collision — Decision (Option A)". This is the PowerShell half of a lockstep-parity pair with task-015 (`setup.sh`) — it is a DIFFERENT file, so it can be authored in parallel, but every behavior must match the bash twin exactly. Concrete edits, grounded in the real file read at spec time:
  - **`$selected` (line 24):** `@{ 1=$false; 2=$false; 3=$false; 4=$false; 5=$false }`.
  - **`Get-ToolName` (lines 26-32):** add `4 { return "GitHub Copilot CLI" }` and `5 { return "Antigravity" }`.
  - **`Show-Menu` (line 38):** `foreach ($i in 1, 2, 3, 4, 5)`; Done line (42) → `[6] Done`.
  - **Read-loop (lines 49-58):** toggle case `{ $_ -in '1','2','3','4','5' }`; change BOTH `'4'`→`'6'` sites (the break/`-eq` checks at lines 54 AND 57 — there are two; missing either breaks the loop); invalid message (55) → `"Enter 1-6."`.
  - **AGENTS.md collision pre-copy block — insert after the "anything selected?" guard (`if (-not $any) { …; exit 0 }`, lines 61-64) and before the first copy block (Claude Code `if`, line 130):** exact parity with bash step 6 — build the AGENTS.md-writer list from `$selected[2]`/`[3]`/`[4]` (Codex/Cursor/Copilot) plus `$selected[5]` (Antigravity) **only if** the committed `profiles/antigravity/` tree writes `AGENTS.md` (not `GEMINI.md`) per Q-H/Q-J in `provider-mapping.md` (read it, do NOT hard-code). When ≥2 are selected, set a script-scope `$script:AgentsCollision = $true` and `Write-Host` the same one-line warning once (identical text to the bash twin, naming the colliding tools and the survivor = highest-numbered selected writer, fixed by per-tool block order).
  - **Non-interactive collision resolution in `Copy-Item-Safe` (the `Read-Host "Overwrite …"` path, lines 87-93):** add a `$script:AgentsCollision`-gated branch — when `$script:AgentsCollision -eq $true` **and** `(Split-Path -Leaf $Dst) -eq 'AGENTS.md'`, `Copy-Item -Force` last-writer-wins WITHOUT prompting and emit an `Updated …` line; otherwise the existing `Read-Host` path is unchanged. The `-Force` and new/identical paths are untouched. ONLY engine change — no merge logic, no new function.
  - **New per-tool blocks (after the Cursor block, line 149),** using `Copy-Dir-Safe`/`Copy-Item-Safe` with `Join-Path` and backslash profile sub-paths (existing convention):
    - **Copilot CLI (`$selected[4]`):** `Copy-Dir-Safe` its committed subtree root (`.github` per feature-002) and `Copy-Item-Safe` its root `AGENTS.md` context file (plus `mcp-config.json` IFF feature-002 ships one). Read the committed `profiles\copilot-cli\` tree for subtree root, context filename, and mcp presence — do NOT fabricate.
    - **Antigravity (`$selected[5]`):** `Copy-Dir-Safe` its committed subtree root (`.agent` per feature-003) and `Copy-Item-Safe` its profile-local context file, whose basename (`AGENTS.md` vs `GEMINI.md`) is read from the committed `profiles\antigravity\` tree per Q-H, NOT hard-coded.
  - Profile-local context files are committed profile-tree data (feature-001 Q-J), copied exactly as codex/cursor's `AGENTS.md` is today — not rendered output.
- Do NOT edit `setup.sh` (that is task-015), any `tests/` suite (task-017), any `canonical/`, renderer, `profiles/*.toml`, or workflow file. Behavior for the existing 3 tools (selections 1/2/3) must be unchanged except for the Done renumber and the shared collision block.

**Acceptance Criteria:**
- [ ] `setup.ps1` menu offers `[4] GitHub Copilot CLI` and `[5] Antigravity`, with `[6] Done`; the toggle case accepts `1-5`, BOTH `'4'`→`'6'` break/`-eq` sites (lines 54 and 57) are updated, and the invalid message reads `Enter 1-6.` (no stale `'4'` break remains in either site).
- [ ] Selecting `4` copies the committed Copilot subtree (`.github`) + root `AGENTS.md` (+ `mcp-config.json` iff shipped) via `Copy-Dir-Safe`/`Copy-Item-Safe`; selecting `5` copies the committed Antigravity subtree (`.agent`) + its context file. Subtree roots, the context filename (`AGENTS.md` vs `GEMINI.md`), and mcp presence are read from the committed `profiles\copilot-cli\` and `profiles\antigravity\` trees (task-009/task-011) and the Q-H/Q-J/Q-B rulings in `provider-mapping.md` (task-004) — no hard-coded basename, no fabricated path.
- [ ] A dedicated pre-copy block (after the "nothing selected" guard, before the first copy `if`) counts the selected AGENTS.md-writing tools (Codex/Cursor/Copilot, +Antigravity iff its committed tree writes `AGENTS.md`); when ≥2, it sets `$script:AgentsCollision = $true` and `Write-Host`s the same one-line warning once, naming the colliding tools and the survivor = highest-numbered selected writer.
- [ ] `Copy-Item-Safe` gains exactly one guarded branch: when `$script:AgentsCollision` is `$true` and `(Split-Path -Leaf $Dst) -eq 'AGENTS.md'`, it `Copy-Item -Force` last-writer-wins WITHOUT a `Read-Host` prompt; every other (unexpected) file difference still hits the existing `Read-Host` prompt, and the `-Force` / new / identical paths are unchanged. No merge logic and no new function added.
- [ ] Lockstep parity with task-015 (`setup.sh`): same numbered tools, same Done=6, same destinations, same diff-aware semantics, the same pre-copy warning text, and the same guarded last-writer-wins `AGENTS.md` branch — any divergence is a defect (parity is asserted by task-017's SU/SPS pairs).
- [ ] No `setup.sh`, `tests/`, `canonical/`, renderer, `profiles/*.toml`, or workflow file is modified; existing-3 install behavior is unchanged except the Done renumber and the shared collision block. All §6 quality gates pass (dependency-free: pure PowerShell, no new pip/npm).
