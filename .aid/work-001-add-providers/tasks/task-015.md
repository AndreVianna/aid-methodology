# task-015: Extend setup.sh — menu + copy blocks for Copilot CLI + Antigravity + AGENTS.md collision (Option A)

**Type:** IMPLEMENT

**Source:** feature-004-setup-and-nonregression → delivery-004

**Depends on:** task-009, task-010, task-011, task-014, task-004

**Scope:**
- Extend `/home/andre.vianna/projects/AID/setup.sh` per SPEC §"Setup-Menu Extension (FR4) → `setup.sh` edits" (steps 1–7) and §"AGENTS.md Multi-Install Collision — Decision (Option A)". This is the bash half of a lockstep-parity pair with task-016 (`setup.ps1`); every change here must have an exact twin there. Concrete edits, grounded in the real file read at spec time:
  - **Menu state (after line 30):** add `selected[4]=0` and `selected[5]=0`.
  - **`tool_name()` (lines 32-38):** add `4) echo "GitHub Copilot CLI" ;;` and `5) echo "Antigravity" ;;`.
  - **`print_menu()` (line 44):** `for i in 1 2 3 4 5; do`; change the Done line (51) to `echo "  [6] Done"`.
  - **Read-loop (lines 58-72):** widen the toggle case `1|2|3)` → `1|2|3|4|5)`; change the break case `4)` → `6)`; update the invalid-choice message (70) to `"Invalid choice. Enter 1-6."`.
  - **"anything selected?" guard (line 77):** `for i in 1 2 3 4 5; do`.
  - **AGENTS.md collision pre-copy block (step 6) — insert after the guard `fi` (lines 81-84) and before the first per-tool copy block (Claude Code `if`, line 134):** build the list of selected AGENTS.md-writing tools from `selected[2]`/`[3]`/`[4]` (Codex/Cursor/Copilot) plus `selected[5]` (Antigravity) **only if** the committed `profiles/antigravity/` tree writes `AGENTS.md` (not `GEMINI.md`) per the FR1 Q-H/Q-J ruling in `provider-mapping.md` — resolve this by reading the committed tree, do NOT hard-code. If ≥2 are selected, set an `AGENTS_COLLISION=1` flag and `echo` the one-line warning once (exact text per SPEC §"Concrete default semantics", naming the colliding tools and the survivor = the highest-numbered selected AGENTS.md-writer, fixed by per-tool `if`-block order, NOT menu-toggle order).
  - **Non-interactive collision resolution in `copy_file` (the `else` of the "files differ, not --force" branch, lines 102-106):** add an `AGENTS_COLLISION`-gated branch — when `AGENTS_COLLISION=1` **and** `$(basename "$dst") == AGENTS.md`, `cp` last-writer-wins WITHOUT firing `read … </dev/tty`, and echo an `Updated: …/AGENTS.md (AGENTS.md last-writer-wins …)`-style line; otherwise the existing `/dev/tty` prompt path is unchanged. The `--force` path and the new/identical paths are untouched. This is the ONLY engine change — no merge logic, no new function.
  - **New per-tool copy blocks (after the Cursor block, line 154),** mirroring the existing `copy_dir`/`copy_file` shape:
    - **Copilot CLI (`selected[4]`):** `copy_dir` its committed subtree root (`.github` per feature-002) and `copy_file` its root context file (`AGENTS.md`) through the same `copy_file` helper (so the Option-A guarded branch applies); plus the separately-copied `mcp-config.json` IFF feature-002's committed tree ships one (Q-B). Read the committed `profiles/copilot-cli/` tree for the exact subtree root, context filename, and mcp presence — do NOT fabricate.
    - **Antigravity (`selected[5]`):** `copy_dir` its committed subtree root (`.agent` per feature-003) and `copy_file` its profile-local context file, whose basename (`AGENTS.md` vs `GEMINI.md`) is read from the committed `profiles/antigravity/` tree per Q-H, NOT hard-coded.
  - Profile-local context files are committed profile-tree data (feature-001 Q-J), copied exactly as codex/cursor's `AGENTS.md` is today — not rendered output.
- Do NOT edit `setup.ps1` (that is task-016), any `tests/` suite (task-017), any `canonical/`, renderer, `profiles/*.toml`, or workflow file. Behavior for the existing 3 tools (selections 1/2/3) must be unchanged except for the Done renumber and the shared collision block.

**Acceptance Criteria:**
- [ ] `setup.sh` menu offers `[4] GitHub Copilot CLI` and `[5] Antigravity`, with `[6] Done`; the toggle case accepts `1-5`, the break case is `6`, the guard loops `1 2 3 4 5`, and the invalid-choice message reads `Invalid choice. Enter 1-6.` (off-by-one renumber complete — no stale `4)` break remains).
- [ ] Selecting `4` copies the committed Copilot subtree (`.github` per feature-002) + root `AGENTS.md` (+ `mcp-config.json` iff feature-002 ships it) via `copy_dir`/`copy_file`; selecting `5` copies the committed Antigravity subtree (`.agent`) + its context file. Subtree roots, the context filename (`AGENTS.md` vs `GEMINI.md`), and mcp presence are read from the committed `profiles/copilot-cli/` and `profiles/antigravity/` trees (task-009/task-011) and the Q-H/Q-J/Q-B rulings in `provider-mapping.md` (task-004) — no hard-coded basename, no fabricated path.
- [ ] A dedicated pre-copy block (after the "nothing selected" guard, before the first per-tool `if`) counts the selected AGENTS.md-writing tools (Codex/Cursor/Copilot, +Antigravity iff its committed tree writes `AGENTS.md`); when ≥2, it sets `AGENTS_COLLISION=1` and echoes the one-line warning once, naming the colliding tools and the survivor = highest-numbered selected writer (fixed install-block order, not toggle order).
- [ ] `copy_file` gains exactly one guarded branch: when `AGENTS_COLLISION=1` and `basename "$dst" == AGENTS.md`, it overwrites last-writer-wins WITHOUT a `/dev/tty` read; every other (unexpected) file difference still hits the existing `/dev/tty` prompt, and the `--force` / new / identical paths are byte-for-byte unchanged. No merge logic and no new helper function added.
- [ ] Lockstep parity with task-016 (`setup.ps1`): same numbered tools, same Done=6, same destinations, same diff-aware semantics, the same pre-copy warning text, and the same guarded last-writer-wins `AGENTS.md` branch — any divergence is a defect (parity is asserted by task-017's SU/SPS pairs).
- [ ] No `setup.ps1`, `tests/`, `canonical/`, renderer, `profiles/*.toml`, or workflow file is modified; existing-3 install behavior is unchanged except the Done renumber and the shared collision block. All §6 quality gates pass (dependency-free: pure bash, no new pip/npm).
