# task-006: E1 — Copilot `.agent.md` emitter + YAML-list serializer in render_agents.py

**Type:** IMPLEMENT

**Source:** feature-002-copilot-cli → delivery-002

**Depends on:** task-004, task-005

**Scope:**
- Implement E1 in `/home/andre.vianna/projects/AID/.claude/skills/aid-generate/scripts/render_agents.py` per SPEC §"Renderer Extension (narrow — E1 only)" / §"E1 — Copilot `.agent.md` frontmatter emitter": a new agent-format branch selected when `profile.agent.format == "copilot-agent"` (the FR1-pinned label from `provider-mapping.md` Q-A, registered in `_KNOWN_AGENT_FORMATS` by task-005) alongside the existing `toml`/`markdown` split in `_render_agent_for_profile` (def at line 219; format-branch region around lines 262-302). **This branch introduces the new-agent-format-branch mechanism that feature-003's `"antigravity-rule"` format reuses** (task-012) — a per-format frontmatter emitter + format-driven routing/suffix selection.
- **Emitted frontmatter field set — exactly `name, description, tools, model` (in that order), per FR1 Q-A.** `name`←canonical `name` (pass-through); `description`←canonical `description` (pass-through); `tools`←canonical `tools:` remapped via `[tool_names]` (`Bash→shell`) and emitted as a **YAML sequence** (computed); `model`←`_resolve_model(tier)` → the `[model_tiers]` string (computed). The optional Copilot fields (`target`, `user-invocable`, `disable-model-invocation`, `mcp-servers`, `metadata`) are **omitted** — AID has no per-agent source for them and the agent defaults are correct (Q-A). Do NOT emit them, and do NOT add an MCP field (E3 dropped, Q-B). Read the field map from `provider-mapping.md` Q-A; do NOT invent it.
- **Reuse, no new transform machinery:** the branch reuses `_parse_frontmatter`, `_resolve_model`, `_remap_tools`, and the SAME `substitute_filenames` THEN `rewrite_install_paths` treatment applied to body AND description (lines 247-260) — matching the determinism/path-rewrite policy every emitter obeys.
- **YAML-list serializer (REQUIRED, not optional):** the existing `_build_frontmatter_md` (lines 104-125) feeds `_remap_tools`'s comma-string through the `str` branch (a scalar `tools: Read, ...`, not a sequence), and its terminal `else` emits a Python `repr` for a list (e.g. `tools: ['Read', 'shell']`) — invalid YAML. Add a Copilot variant/branch that builds a **list** value for `tools` and serializes it as a proper YAML block sequence (flow form acceptable only if it parses back identically), emitting an empty list as `[]` (no dangling key). Reuse the bool/str scalar branches; if Copilot needs a specific quoting rule for a `:`-bearing `description`, override the quoting for that case so it parses back unchanged.
- **Scalar quoting must be Copilot-YAML-safe.** The reused str-branch double-quote-with-escaping rule is asserted-not-verified against Copilot's parser. E1 must produce frontmatter that round-trips through a real YAML load (the round-trip assertion itself is task-010 / SPEC Test #2; this task must produce output that satisfies it).
- **Output path/suffix:** writes to `<output_base>/<output_root>/<agents_dir>/<name>.agent.md`. The `.agent.md` suffix is a property of this format branch (no data-driven `agent_suffix` key — schema confirmed in task-005). Record each file in the manifest with repo-relative `src` and `common_parent`-relative `dst`, exactly as the existing branches do.
- Do NOT change the `markdown` or `toml` branches' emitted bytes. Do NOT author `profiles/copilot-cli.toml` (task-009). There is no E2 (render_skills cross-kind) and no E3 (render_mcp) to build — skills are native `[data]` and MCP is `[omit]`. Round-trip/byte-identical/render-drift integration checks are task-010 (TEST); this task ships code + unit tests only.

**Acceptance Criteria:**
- [ ] A new agent-format branch keyed on `"copilot-agent"` emits `<name>.agent.md` files whose frontmatter is exactly `name, description, tools, model` (in that order); no optional Copilot field and no MCP field is emitted; the `markdown` and `toml` branches are byte-for-byte unchanged.
- [ ] The Copilot frontmatter serializer emits `tools` as a valid YAML sequence (never a comma-string scalar and never a Python `repr`) and emits an empty `tools` list as `[]` (no dangling key).
- [ ] The branch reuses `_parse_frontmatter`, `_resolve_model`, `_remap_tools`, and applies `substitute_filenames` THEN `rewrite_install_paths` to both body and description — no new transform framework is introduced; the branch-dispatch mechanism it adds is reusable by a future agent-format value (feature-003's `antigravity-rule`).
- [ ] Unit tests for the new path: a `copilot-agent`-format render produces `.agent.md` output; `tools` round-trips through a real YAML parse as a sequence (not a repr); a `description` containing `:` is emitted as a quoted scalar that parses back unchanged; an empty `tools` list emits `[]`; `Bash` is remapped to `shell`.
- [ ] All existing tests still pass; build passes; the 3 existing profiles' agent output is unaffected by this change (verified definitively in task-010).
- [ ] All §6 quality gates pass (stdlib-only, no faked primitives — sub-agents become real invocable Copilot custom agents with tool-idiomatic frontmatter per FR1).
