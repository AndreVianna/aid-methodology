# task-011: Author profiles/antigravity.toml (cursor-modeled) + profile-local committed AGENTS.md

**Type:** IMPLEMENT

**Source:** feature-003-antigravity → delivery-003

**Depends on:** task-004, task-005

**Scope:**
- Author `/home/andre.vianna/projects/AID/profiles/antigravity.toml`, modeled on `profiles/cursor.toml`, per SPEC §"Profile Data — `profiles/antigravity.toml` (modeled on cursor.toml)". The file is **pure profile data**; populate every FR1-owned cell from `/home/andre.vianna/projects/AID/.aid/work-001-add-providers/research/provider-mapping.md` (task-004) — do NOT invent any value. Single-root layout: `output_root = "profiles/antigravity/.agent"` (`install_root()` basename `.agent`).
- **Layout knobs:** `agents_dir = "rules"` (sub-agents reshaped → `.agent/rules/*.md`, Q-D); **`skills_dir = "skills"`** (skills → `.agent/skills/<slug>/SKILL.md` folders — the **native** Skills primitive, Q-D/§B.4; drives the existing `render_skills` folder pass, pure `[data]`); `rules_dir = "rules"` (target of `[[extras.rules]]` methodology `.md` files); `templates_dir = "templates"` and `scripts_dir` left unset (schema default `"scripts"`) — bare names so `rewrite_install_paths` link targets agree with emitted locations (SPEC §"`rewrite_install_paths` dir-name caveat"); `recipes_dir = "recipes"` (recipes → `.agent/recipes/` as `[data]`, always emitted — no omit knob). `project_context_file = "AGENTS.md"` (Q-H, token value).
- **Same-dir co-location (resolved, not a risk):** `agents_dir == rules_dir == "rules"` is intended by Q-D (sub-agents live as rules). Reshaped sub-agents emit as persona-stem `<name>.md`; methodology rules emit as `aid-` prefixed `aid-methodology.md`/`aid-review.md` — disjoint stems, no collision. Set both to `"rules"`; do NOT split them.
- **`[agent] format = "antigravity-rule"`** (the new agent-format value reusing feature-002's E1 new-agent-format-branch mechanism; the reshape engine is task-012). `[agent.frontmatter] required = ["trigger", "description"]`, `optional = ["globs"]` — Antigravity rule frontmatter shape (§B.1); these lists are parsed-but-dead-for-emission (the reshape is done by the format branch) and kept structurally valid + documentary only.
- **`[skill] decomposition = "references"`**; `[skill.frontmatter] required = ["name", "description"]`, `optional = ["allowed-tools", "argument-hint"]` — the native SKILL.md shape (Q-D/§B.4). **No `emit_as` knob** — skills are native `.agent/skills/` folders via the existing pass (`[data]`).
- **`[model_tiers]` — detailed form** (Q-E, Gemini-3 lineage): `[model_tiers.large] {model="gemini-3-pro", reasoning_effort="high"}`, `[model_tiers.medium] {model="gemini-3-pro", reasoning_effort="low"}`, `[model_tiers.small] {model="gemini-3-flash", reasoning_effort="low"}` (the small-tier effort is a non-empty placeholder so `validate` passes — `aid_profile.py:426-434` requires both fields; the exact id/effort tokenization is the `docs-only-noted` residual to confirm). **`[tool_names]`** — empty map (Q-F, identity passthrough). **`[capabilities]`** — `hooks=true`, `skill_chaining=true`, `background_execution=true`, `stop_hook_autocontinue=false` (Q-G). **`[filename_map]`** — 3 required keys (`project_context_file = "AGENTS.md"` per Q-H, `reviewer_output_file = "STATE.md"`, `open_questions_file = "additional-info.md"`).
- **`[[extras.rules]]`** — reuse the cursor `_render_cursor_extras` path to place AID's methodology rules into `.agent/rules/`. Per Q-I, Antigravity rules are **`.md`** (NOT `.mdc`), so the OUTPUT name differs from the `canonical/rules/` `.mdc` source name → set `output_filename` (the `RuleEntry.output_filename` field is the small engine touch folded into task-012). Per-rule literals MUST match disk truth (read `canonical/rules/aid-methodology.mdc` and `canonical/rules/aid-review.mdc`):
  - `aid-methodology.mdc`: `output_filename = "aid-methodology.md"`, `description = "AID methodology workflow and Knowledge Base integration"`, `always_apply = true` (disk `alwaysApply: true` → `trigger: always_on`), `globs = []` (no globs on disk).
  - `aid-review.mdc`: `output_filename = "aid-review.md"`, `description = "Code review standards for AID methodology"`, `always_apply = false` (disk `alwaysApply: false` → `trigger: glob`), `globs = ["**/*.{java,py,ts,js,cs,go,rs}"]` (disk glob preserved).
- Author the **profile-local committed context file** `/home/andre.vianna/projects/AID/profiles/antigravity/AGENTS.md` (the filename pinned by Q-H — `AGENTS.md`, the cross-tool canonical pick; `GEMINI.md` is also read by Antigravity but reserved for tool-specific overrides AID does not author) — a committed file alongside the profile (the codex/cursor convention, Q-J). It is authored profile-tree data: **NOT rendered by `run_generator.py`, NOT recorded in any `emission-manifest.jsonl`** (SPEC §"Context-file convention" + §"Emitted Tree"). Author it from the shared ~27-line context template with Antigravity paths/idiom substituted, mirroring `profiles/cursor/AGENTS.md`.
- Do NOT modify any `render_*.py`, `run_generator.py`, `verify_*.py`, `aid_profile.py`, or setup scripts (the sub-agents→rules engine route, the `RuleEntry.output_filename` touch, AND the `"antigravity-rule"` `agent.format` enum registration are all task-012; task-005 registers only the Copilot `"copilot-agent"` value). Do NOT run the generator or commit the emitted `.agent/` tree (that is task-014). This task ships profile data + the committed context file only.

**Acceptance Criteria:**
- [ ] `profiles/antigravity.toml` loads/parses via `python .claude/skills/aid-generate/scripts/aid_profile.py --profile profiles/antigravity.toml`, and every check task-011 controls passes: 3 `filename_map` keys present, valid `[skill].decomposition`, each detailed `[model_tiers.<tier>]` sub-table has non-empty `model` + `reasoning_effort`, `[agent].format = "antigravity-rule"` is present. NOTE: because the `"antigravity-rule"` enum value is registered by **task-012** (not task-005, which registers only `copilot-agent`) and task-011 ∥ task-012 are file-disjoint, a fully clean `validate()` (zero errors, format accepted) is achieved once task-012 lands and is verified at **task-014** — task-011 is not blocked on the enum value it does not own.
- [ ] Every FR1-owned cell is populated from `provider-mapping.md` with no `FR1-*`/`TBD`/`...` placeholders left: detailed `[model_tiers]` (Q-E), empty `[tool_names]` (Q-F), 4 `[capabilities]` flags (Q-G), `project_context_file = "AGENTS.md"` (Q-H), `[[extras.rules]]` `.md` `output_filename`s (Q-I).
- [ ] `skills_dir = "skills"` is set (native `.agent/skills/` home, drives the existing folder pass, `[data]`); **no `[skill].emit_as`** knob is present; `agents_dir == rules_dir == "rules"` (Q-D shared dir, disjoint stems); `recipes_dir = "recipes"`; `scripts_dir` unset / `templates_dir` bare so rewrite targets agree.
- [ ] `[agent].format = "antigravity-rule"`; `[agent.frontmatter]` is the `trigger`/`description`(/`globs`) shape (documentary, dead-for-emission); the reshape engine is task-012.
- [ ] `[[extras.rules]]` per-rule literals match disk truth: aid-methodology = `always_apply: true` / no globs (→ `trigger: always_on`), aid-review = `always_apply: false` / glob `**/*.{java,py,ts,js,cs,go,rs}` (→ `trigger: glob`); both carry the `.md` `output_filename`.
- [ ] `profiles/antigravity/AGENTS.md` exists under `profiles/antigravity/`, is hand-authored from the shared template (NOT generated), and is absent from any emission manifest (no emitter writes it).
- [ ] All §6 quality gates pass (no invented/faked values — every literal sourced to `provider-mapping.md` or disk; data-only, no engine change; existing profiles untouched by this file).
