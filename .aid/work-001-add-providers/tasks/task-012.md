# task-012: `antigravity-rule` agent-format engine (sub-agents→.agent/rules/ reshape) + RuleEntry.output_filename touch

**Type:** IMPLEMENT

**Source:** feature-003-antigravity → delivery-003

**Depends on:** task-004, task-005, task-006

**Scope:**
- Implement the **one engine increment** feature-003 needs, per SPEC §"Renderer Increment (one engine addition — reuses feature-002's E1 mechanism)": a new `[agent].format = "antigravity-rule"` value that reshapes AID's 22 sub-agents into `.agent/rules/*.md` rule files (FR1 Q-D). **This DEPENDS ON task-006: it reuses the new-agent-format-branch mechanism feature-002's E1 introduces** in `_render_agent_for_profile` (the per-format frontmatter emitter + format-driven routing/suffix selection + the widened `_KNOWN_AGENT_FORMATS`). It is **NOT** a reuse of E1's Copilot `.agent.md` *output*, and it is **NOT** the deleted E2 — it adds a *different* (rule-shaped) frontmatter on top of E1's branch-dispatch machinery. The dependency on task-006 is precise: task-006 must land first so the format-branch mechanism exists; this task then adds a second branch keyed on `"antigravity-rule"`.
- **(Engine A) `antigravity-rule` agent-format branch** in `/home/andre.vianna/projects/AID/.claude/skills/aid-generate/scripts/render_agents.py` (def `_render_agent_for_profile` at line 219; format-branch region around lines 262-302; glob at line 358). Selected by `[agent].format = "antigravity-rule"` (validated via task-005's `_KNOWN_AGENT_FORMATS` widening — task-005 must add this value too; if task-005 only registered `"copilot-agent"`, this task adds `"antigravity-rule"` to the enum as the minimal additive widening it requires, default-preserving). The branch emits, from the `canonical/agents/*/AGENT.md` pass, a `.md` rule file under `<output_root>/<agents_dir>/` (= `.agent/rules/`) whose frontmatter is a **reshape**: drop the AGENT `name/description/tools/model` set and emit `trigger:` (+ `description`, and `globs` for the glob trigger) — Antigravity rules carry no `tools`/`model` frontmatter (§B.1). AID's always-loaded sub-agent personas map to `trigger: always_on`. The Markdown body carries over.
- **Reuse, no new transform machinery:** the branch reuses the existing canonical parse (`_parse_frontmatter`), the body treatment (`substitute_filenames` THEN `rewrite_install_paths`), and the manifest/determinism helpers (`_write`/`_record` equivalents, manifest `add`) — no new framework beyond the format branch + a `trigger:`-frontmatter serializer. The default `markdown`/`toml` branches and the `copilot-agent` branch (task-006) stay byte-for-byte unchanged.
- **(Engine B — folds former R3) `RuleEntry.output_filename` touch** (SPEC §"Renderer Increment — Plus the small `[[extras.rules]]` extension touch", Q-I). `_render_cursor_extras` (`/home/andre.vianna/projects/AID/.claude/skills/aid-generate/scripts/render_skills.py:261-293`) copies rules verbatim with `out_path = rules_out_dir / rule.filename` (source name == output name). Antigravity needs `.md` while the canonical source is `.mdc`, so the output name must differ. Add an optional `output_filename` field to `RuleEntry` (`/home/andre.vianna/projects/AID/.claude/skills/aid-generate/scripts/aid_profile.py` `class RuleEntry`, lines 145-150 + `_parse_extras` lines 258-269) defaulting to `None`, and have `_render_cursor_extras` read the source by `rule.filename` but write to `rule.output_filename or rule.filename`. Default-preserving: with no `output_filename` set, cursor's verbatim copy is byte-identical. The `trigger:`-shape injection for the methodology `.md` rule bodies (translating disk `alwaysApply:`/`globs:` into Antigravity `trigger:` frontmatter) is sized here too — flag whether the touch injects the `trigger:` shape or a separate Antigravity `.md` rule source is committed (SPEC §"`[[extras.rules]]` extension touch"); resolve per the disk truth task-011 declares.
- Ship the determinism self-tests the SPEC §"Test Plan" #2/#3 require (byte-identical re-render + zero blast radius on the other profiles) for both engine pieces, within `render_agents.py`'s and `render_skills.py`'s existing `--self-test` harnesses. Integration/render-drift/byte-identical-across-profiles gating is **task-014** (TEST); this task ships code + its own targeted self-tests only.
- Do NOT author `profiles/antigravity.toml` or the context file (task-011) — but the `"antigravity-rule"` route value and the `output_filename` values must match the data task-011 declares. Do NOT touch `run_generator.py`/`verify_deterministic` pass-lists: both engine pieces reuse existing EMITTING passes (`render_agents`, `render_skills`/`_render_cursor_extras`), so no new pass is added and the dual-list wiring concern does NOT arise. Do NOT touch setup scripts (delivery-004).

**Acceptance Criteria:**
- [ ] `render_agents` emits the 22 sub-agents as `<name>.md` rule files under `.agent/rules/` when `[agent].format = "antigravity-rule"`, with `trigger:`/`description`(/`globs`) frontmatter (NOT `name/description/tools/model`); always-loaded personas map to `trigger: always_on`; the body carries over. The branch reuses task-006's format-branch mechanism (a second branch on the same dispatch), not E1's Copilot output and not the deleted E2.
- [ ] `"antigravity-rule"` is accepted by `_KNOWN_AGENT_FORMATS` (added here if not already by task-005); an unknown `[agent].format` is rejected with a clear message; the default `markdown`/`toml` and the `copilot-agent` branches are byte-for-byte unchanged.
- [ ] `RuleEntry.output_filename` defaults to `None`; `_render_cursor_extras` reads source by `filename` and writes to `output_filename or filename`; with no `output_filename` set, cursor's verbatim `.mdc` copy is byte-identical. With it set, the methodology rules emit as `aid-methodology.md`/`aid-review.md`.
- [ ] Both engine pieces are profile-gated and default-preserving — re-rendering the existing profiles (claude-code, codex, cursor; +copilot-cli once delivery-002 merged) produces byte-identical output (verified by the pieces' own self-tests); no new EMITTING pass added.
- [ ] **IMPLEMENT defaults:** unit/self-tests for both new paths within `render_agents.py --self-test` and `render_skills.py --self-test` (byte-identical re-render + zero blast radius); all existing tests still pass; build passes.
- [ ] All §6 quality gates pass (stdlib-only; the reshape is documented not faked; backward compatible by gating; convention-over-infrastructure: a single named branch reusing E1's mechanism + one optional `RuleEntry` field, not a new engine).
