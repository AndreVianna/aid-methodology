# task-014: Render Antigravity tree + self-tests, render-drift, existing-profiles byte-identical gate

**Type:** TEST

**Source:** feature-003-antigravity → delivery-003

**Depends on:** task-004, task-011, task-012

**Scope:**
- Run the full generator and the SPEC §"Test Plan" checks to prove the Antigravity tree is complete and render-drift-clean while all prior profiles stay byte-identical. This is the integration/gate task — no production code; it runs the renderers, commits the emitted `profiles/antigravity/.agent/` tree + `emission-manifest.jsonl`, and asserts every gate. Defects route back to task-011/task-012 — do NOT edit renderer/profile code to make a test pass. Test cases (SPEC Test Plan #1–#7):
  - **#1 validator self-test:** `profiles/antigravity.toml` loads + `validate()` returns no errors (3 `filename_map` keys; `[agent].format = "antigravity-rule"` accepted by the widened `_KNOWN_AGENT_FORMATS`; an unknown `[agent].format` rejected with a clear message; `[skill].decomposition = "references"` valid; each detailed `[model_tiers.<tier>]` validates with both `model` and `reasoning_effort` non-empty). The 3 existing profiles (claude-code, codex, cursor; +copilot-cli once delivery-002 merged) still validate clean.
  - **#2 render_agents self-test (sub-agents→rules reshape):** `python .claude/skills/aid-generate/scripts/render_agents.py --self-test --canonical-root .` green for all profiles incl. `antigravity` (auto-discovered): two renders byte-identical; for `antigravity` the 22 sub-agents emit as `<name>.md` rule files under `.agent/rules/` whose frontmatter is `trigger:`/`description`(/`globs`) — NOT `name/description/tools/model`; each emitted frontmatter block round-trips through a YAML parse. The 3 existing profiles' agent output (+copilot-cli once delivery-002 merged) is byte-identical across the run (the `antigravity-rule` branch is selected only by `antigravity`).
  - **#3 render_skills self-test (skills as `[data]` folders + the `output_filename` rule touch):** `python .claude/skills/aid-generate/scripts/render_skills.py --self-test --canonical-root .` green for all profiles incl. `antigravity`: the 10 skills emit as **10 `SKILL.md` folders** under `.agent/skills/` (the same folder shape cursor emits — `[data]`), NOT as workflows; a multi-reference skill emits `SKILL.md` + `references/*.md`; the zero-reference skill `aid-config` emits `SKILL.md` only. The `[[extras.rules]]` methodology rules land under `.agent/rules/` as `aid-methodology.md`/`aid-review.md` (`.md`, via `output_filename`). The pre-existing profiles' skill output (cursor's `.mdc` rules included — `output_filename` unset → source name preserved) is byte-identical to before.
  - **#4 disjoint-stem assertion:** no reshaped sub-agent rule output name (`<name>.md`) equals a methodology rule output name (`aid-methodology.md`/`aid-review.md`) in the shared `.agent/rules/` dir (the renderer has no collision guard; assert the invariant Q-D relies on).
  - **#5 render-drift clean (new profile):** `python run_generator.py` emits `antigravity` with no errors; `verify_deterministic` PASS — byte-identical re-render + presence audit with no MISSING/EXTRA for `antigravity` (incl. confirming the committed `profiles/antigravity/AGENTS.md` is NOT in the manifest and not flagged EXTRA — it lives at the profile root, outside the walked `.agent/` output_root). Confirm `.agent/recipes/` is emitted with every `canonical/recipes/` file (no omit knob — SPEC §"Recipes are always emitted").
  - **#6 prior-profiles byte-identical gate:** after running the generator, `git diff --exit-code -- profiles/claude-code/ profiles/codex/ profiles/cursor/` shows ZERO changes (trees + their `emission-manifest.jsonl`). **If delivery-002 has merged, also `git diff --exit-code -- profiles/copilot-cli/` is zero.** This is the continuous backward-compat gate, not a final-only check.
  - **#7 canonical suites green:** `render_lib --self-test`, `render_templates`, `render_canonical_scripts`, `render_recipes` self-tests, `verify_deterministic --self-test`, and `verify_advisory` continue to pass unchanged.
- Commit the emitted `profiles/antigravity/.agent/` tree + `emission-manifest.jsonl`; record results.

**Acceptance Criteria:**
- [ ] `profiles/antigravity.toml` validates clean (`[agent].format = "antigravity-rule"` accepted, unknown rejected); all prior profiles still validate clean (Test #1).
- [ ] `render_agents --self-test` green incl. antigravity (two renders byte-identical); the 22 sub-agents emit under `.agent/rules/` as `<name>.md` with `trigger:`/`description`(/`globs`) reshape, every frontmatter block YAML-parses; prior profiles' agent output (+copilot-cli iff merged) byte-identical (Test #2).
- [ ] `render_skills --self-test` green incl. antigravity: 10 `SKILL.md` folders under `.agent/skills/` (NOT workflows, like cursor); multi-reference skill emits `SKILL.md`+`references/`; `aid-config` emits `SKILL.md` only; `[[extras.rules]]` land as `aid-methodology.md`/`aid-review.md` (`.md` via `output_filename`); prior profiles' skill output byte-identical (Test #3).
- [ ] No reshaped sub-agent rule name collides with a methodology rule name in `.agent/rules/` (Test #4).
- [ ] `python run_generator.py` succeeds and `verify_deterministic` PASS for antigravity (no MISSING/EXTRA; the committed `AGENTS.md` un-manifested and not EXTRA; `.agent/recipes/` present with every canonical recipe) (Test #5).
- [ ] `git diff --exit-code -- profiles/claude-code/ profiles/codex/ profiles/cursor/` reports zero changes; if delivery-002 merged, `profiles/copilot-cli/` is likewise zero (Test #6).
- [ ] All canonical suites + `verify_advisory` green (Test #7).
- [ ] Tests are deterministic; clean setup/teardown (temp dirs, no leftover state); all acceptance criteria from feature-003 SPEC are covered. All §6 quality gates pass.
