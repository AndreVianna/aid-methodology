# Line-ending policy.
#
# AID ships POSIX shell scripts that adopters run via bash, and run_generator.py
# always writes LF. Forcing LF for shell/JS/Python text prevents a contributor
# whose git core.autocrlf=true (this repo is authored on Windows) from committing
# CRLF that would break `#!/usr/bin/env bash` on Linux/macOS adopters — and would
# break the byte-exact render-drift CI gate.
*.sh   text eol=lf
*.mjs  text eol=lf
*.py   text eol=lf
# Markdown must be LF too: build-kb-index.sh's awk frontmatter parser chokes on
# CRLF on Linux (intent: blocks parse as "no intent declared"), which broke the
# kb-hygiene CI check.
*.md   text eol=lf
# The dashboard file-set manifest is extensionless (no rule above covers it) and is
# line-parsed by install.sh / install.ps1 / vendor.js / vendor.py / release.sh. Force LF
# so a Windows autocrlf checkout can't inject CRLF that a consumer's parser mishandles
# (a CRLF checkout made vendor.js leak comment lines as bogus file paths).
dashboard/MANIFEST text eol=lf
# The knowledge-graph work (work-005) adds four canonical roots whose file sets include
# extensions no rule above names — `.yml` under the graph template root, and `.js`, `.css`,
# `.html` under the knowledge-graph view templates. A `.yml` is VERBATIM class in the render
# (render.py's _TEXT_EXTENSIONS excludes it), so a CRLF commit would ship CRLF to every
# profile AND to every adopter with a perfectly clean render-drift diff — the one place that
# gate is structurally blind. The rules are PATH-scoped rather than extension-wide on purpose:
# `*.yml text eol=lf` at repo root would renormalise every tracked .yml in the repository,
# burying an unrelated repo-wide change in this diff.
canonical/aid/templates/graph/**             text eol=lf
canonical/aid/templates/knowledge-graph/**   text eol=lf
canonical/aid/scripts/graph/**               text eol=lf
canonical/skills/aid-graph/**                text eol=lf
# Two already-registered files the same work amends that sit outside every root above and
# that no extension rule reaches: the settings template (verbatim class, seeds a `graph:`
# section) and the generated-file registry (transformed class, gains a header-comment line).
canonical/aid/templates/settings.yml         text eol=lf
canonical/aid/templates/generated-files.txt  text eol=lf
# The install manifest is LF-only in history but had no rule, so a Windows tool that
# rewrote it (Python's default newline translation) silently committed CRLF —
# functionally harmless, since every consumer JSON-parses it, but it turned a 72-line
# path deletion into an 875-line whole-file re-encode and made the diff of the very
# artifact under review unreadable. Scoped to this one file deliberately rather than a
# blanket *.json rule: .claude/settings.json has been CRLF since before this policy and
# renormalising it is a separate change.
.aid/.aid-manifest.json text eol=lf
# The vendored third-party bundles (work-005 task-023). Two rules, one path, and
# both matter for a different reason. Last-match-wins, so this must stay AFTER the
# `canonical/aid/templates/knowledge-graph/**` rule above, which otherwise reaches
# these files.
#
# `linguist-vendored=true` keeps them OUT OF THE KNOWLEDGE GRAPH.
# `scan-source.sh` already excludes any path git reports as linguist-vendored or
# linguist-generated (it runs `git check-attr --stdin` over the enumerated set), so
# this is the sanctioned mechanism rather than a new exclusion list. Without it the
# ten bundles enumerate as source artifacts, and since nobody documents
# `pixi.min.js` in the Knowledge Base they would sit as permanent
# "undocumented artifact" coverage gaps that no one can ever legitimately close --
# owner's decision, 2026-08-06.
#
# `-text` stops git normalising their line endings. These are third-party artifacts
# whose byte counts are VERIFIED against upstream by the packaging gate (815,219
# bytes combined, recorded in the decision record), and the rule above currently
# gives them `text` + `eol=lf`. Today that is harmless because every bundle happens
# to ship LF-only -- measured, zero CR bytes. But a future re-download that arrives
# with CRLF would be silently rewritten on commit, and the gate would then compare a
# git-altered file against an upstream byte count and disagree, with nothing in the
# diff to explain why. Marking them binary removes that class of failure instead of
# relying on upstream's choice of newline.
canonical/aid/templates/knowledge-graph/vendor/** -text linguist-vendored=true
