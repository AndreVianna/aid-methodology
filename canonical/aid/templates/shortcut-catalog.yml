# canonical/aid/templates/shortcut-catalog.yml
#
# Single-source manifest for the direct-entry shortcut engine (feature-003,
# work-001-lite-aid-skills). One row per capability, one directory per row.
# `create` and `update` are the two canonical grid verbs. There is no alias
# form, no synonym data, and no redirect -- a name that is not a row is not
# invocable at all. The generator provides no runtime redirect mechanism: `render.py`
# sets `skill_slug = skill_dir.name` and carries the frontmatter `name:` through
# untouched (see the `name` field contract below for the full consequence).
#
# Renders as VERBATIM BYTES to `<root>/aid/templates/shortcut-catalog.yml` in all 5
# profiles -- a `.yml` is not in render.py's `_TEXT_EXTENSIONS`, matching the shipped
# `templates/settings.yml` precedent (render.py grep `_TEXT_EXTENSIONS`).
#
# Two consumers:
#   1. The maintainer build helper
#      (`.claude/skills/generate-profile/scripts/build-shortcut-skills.py`) -- reads this
#      file and emits/refreshes the thin-doorway `canonical/skills/aid-<name>/SKILL.md`
#      directories, one per non-`repurpose` row. After any edit to this file the
#      maintainer runs that helper, then the FULL `run_generator.py` (never a partial
#      render -- architecture.md § Gotchas).
#   2. `/aid-triage` (feature-014) at runtime -- resolves a typed invocation to its
#      `{verb, artifact, default_type, group}` binding for an unsure user.
#
# ---------------------------------------------------------------------------
# Field contract (every row; documented once here, not repeated per row)
# ---------------------------------------------------------------------------
#
#   name          (required, string) -- the invocation name AND the skill directory name
#                 AND the rendered `/command` name. `render.py` sets
#                 `skill_slug = skill_dir.name` and carries the frontmatter `name:`
#                 through untouched -- there is no command-alias mechanism and no
#                 runtime redirect, so every row name is its own
#                 `canonical/skills/<name>/SKILL.md` directory and nothing else is
#                 invocable. MUST be `aid-` prefixed and MUST equal the directory it
#                 lives in.
#
#   verb          (required, string) -- the verb the engine dispatches on (e.g. `create`,
#                 `update`, `fix`, `experiment`). `create` and `update` are the two
#                 canonical grid verbs (FR-10 / Q3). The engine's family-scaffolding
#                 consult keys on this field to resolve per-artifact CAPTURE slots and
#                 SPEC-section activation (shortcut-engine.md § Family Scaffolding
#                 Consult; feature-005 owns the `update`-family groupings-table binding).
#
#   artifact      (required, string, may be `""`) -- the artifact-type suffix (e.g.
#                 `api`, `ui`, `data-model`). `""` for bare verbs that take no artifact
#                 suffix (`aid-fix`, `aid-refactor`, `aid-experiment`, `aid-report`, ...).
#
#   alias_of      (required, string or `null`) -- **always `null`**; vestigial but
#                 deliberately retained as a required field. Removal is a scheduled
#                 follow-on after features 004, 005, and 006 land, and is blocked today by
#                 three CLASSES of consumer. Individual call sites rot -- the site-side
#                 check in (b) has already moved once -- so re-derive the live set before
#                 attempting removal, via
#                 `git grep -nE 'alias_of' -- '*.py' '*.mjs' '*.sh'`, keeping only the
#                 hits that read this field's PRESENCE or its `null` VALUE:
#                   a. Builder validation -- `_REQUIRED_FIELDS` in
#                      `build-shortcut-skills.py` reports every row as invalid if the
#                      field is absent. Assertion `SBI04a` in
#                      `tests/canonical/test-shortcut-builder-invariants.sh` is the oracle
#                      pinning that requirement, so the two must move together.
#                   b. Site count derivation -- the canonical/alias split
#                      `rows.filter((r) => r.alias_of === 'null')` in
#                      `site/scripts/skills/skill-counts.mjs`, whose row shape is declared
#                      in `site/scripts/skills/catalog.mjs`. This consumer fails SILENTLY
#                      rather than loudly: the catalog parser keeps whatever keys a row
#                      carries, so a missing field reads back `undefined`, the canonical
#                      count drops to zero and every row is reclassified as an alias.
#                      This check formerly lived in `site/scripts/gen-reference.mjs`; that
#                      file no longer reads the field at all, so do not look for it there.
#                   c. Canonical shell assertions on the field's presence or `null` value
#                      -- `DC01a` in `tests/canonical/test-doc-counts.sh` (asserts
#                      `CANON + ALIAS == ROWS`, both sides derived by `grep -c` on the
#                      anchored `^    alias_of:` prefix), `DMR31`/`DMR32` in
#                      `test-deploy-monitor-repurpose.sh`, `TR21` in
#                      `test-triage-routing.sh`, and the per-family row checks `CRF15`,
#                      `DFS12`, `PFS13` and `TEF13`.
#                 Deliberately NOT blockers, recorded so a future author does not chase
#                 them: the `alias_of` column in `test-catalog-dirs-parity.sh` is
#                 parsed-then-never-read dead input by that file's own note, and the
#                 `alias_of: aid-` zero-greps in the create / fix / change-refactor family
#                 suites hold at zero whether or not the field exists.
#                 Every row now reads `alias_of: null` -- no exception.
#
#   default_type  (required, string) -- the default task Type this shortcut's DETAIL
#                 emits, one of the closed 8-enum (byte-stable; extending it is a
#                 lockstep break across `grade.sh`, both dashboard reader twins, and
#                 every grading skill -- `artifact-schemas.md § Contracts`):
#                   RESEARCH | DESIGN | IMPLEMENT | TEST | DOCUMENT | MIGRATE | REFACTOR | CONFIGURE
#                 Multi-task shortcuts (e.g. create-data-model -> MIGRATE + IMPLEMENT +
#                 TEST) still declare exactly ONE `default_type` here -- the row's
#                 PRIMARY/first task type. The full per-family task breakdown lives in
#                 the family's scaffolding reference
#                 (`shortcut-scaffolding/<family>.md`), not in this field. See the
#                 default-type mapping table in `shortcut-engine.md` (A-6; no enum
#                 change) for the verb -> default_type rationale.
#
#   group         (required, string) -- the activity group this shortcut belongs to,
#                 per REQUIREMENTS.md §5.1 (e.g. `G4` for the Create family, `G6` for
#                 Fix). Organizational metadata; NOT a task Type.
#
#   intent        (required, string) -- a one-line human-readable summary of what this
#                 shortcut does.
#
#   repurpose     (optional, boolean, default `false`) -- `true` for rows whose
#                 `SKILL.md` is hand-authored rather than generated. The maintainer
#                 build helper (`build-shortcut-skills.py`) SKIPS `repurpose: true`
#                 rows -- it never generates or overwrites their `SKILL.md`. 24 rows
#                 carry `repurpose: true`.
#
# ---------------------------------------------------------------------------
# Row shape (documentation only -- illustrative; NOT a live row. Family features
# 005-013 append their own real rows to the `shortcuts:` list below; the first
# family row lands via a later task, not this one).
# ---------------------------------------------------------------------------
#
#   - name: aid-create-api      # == skill directory == /command (render.py: name == dir)
#     verb: create               # the verb the engine dispatches on
#     artifact: api               # artifact suffix ("" for bare verbs: fix, refactor, ...)
#     alias_of: null              # always null; vestigial-but-required (see alias_of contract)
#     default_type: IMPLEMENT     # default task Type (closed 8-enum; see shortcut-engine.md § A-6)
#     group: G4                   # activity group (REQUIREMENTS.md §5.1)
#     intent: "Create an API endpoint / middleware (contract, handler, validation)."
#   - name: aid-update-api       # canonical update-family row
#     verb: update                # update is the second canonical grid verb
#     artifact: api
#     alias_of: null              # always null; vestigial-but-required (see alias_of contract)
#     default_type: IMPLEMENT
#     group: G5                   # G5: Change + Refactor family
#     intent: "Update an existing API endpoint / middleware's contract or behavior."
#     # repurpose: false           # illustrative -- normally omitted (default false)
#
# ---------------------------------------------------------------------------

version: 1
shortcuts:
  - name: aid-fix
    verb: fix
    artifact: ""
    alias_of: null
    default_type: IMPLEMENT
    group: G6
    intent: "Diagnose and correct a defect, regression, incident, or vulnerability."

  # --- G4: Create family (feature-006, task-015) -- 16 canonical rows (12 in this
  # section; aid-create-test in G7, aid-create-document and aid-create-diagram in G8,
  # aid-create-dashboard in G11) ---
  - name: aid-create
    verb: create
    artifact: ""
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Create a new internal code artifact (module, interface, type) from scratch."
  - name: aid-create-api
    verb: create
    artifact: api
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Create an API endpoint / middleware (contract, handler, validation)."
  - name: aid-create-ui
    verb: create
    artifact: ui
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Create a UI component or page."
  - name: aid-create-theme
    verb: create
    artifact: theme
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Create a visual theme or style-token set."
  - name: aid-create-cli
    verb: create
    artifact: cli
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Create a CLI command."
  - name: aid-create-data-model
    verb: create
    artifact: data-model
    alias_of: null
    default_type: MIGRATE
    group: G4
    intent: "Create a new data model/entity with its schema migration."
  - name: aid-create-data-pipeline
    verb: create
    artifact: data-pipeline
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Create a data pipeline (source, transform, sink, schedule)."
  - name: aid-create-messaging
    verb: create
    artifact: messaging
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Create a message/event schema and its emission."
  - name: aid-create-integration
    verb: create
    artifact: integration
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Create an external-service integration (client/adapter)."
  - name: aid-create-job
    verb: create
    artifact: job
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Create a scheduled or background job."
  - name: aid-create-config
    verb: create
    artifact: config
    alias_of: null
    default_type: CONFIGURE
    group: G4
    intent: "Create a new configuration option or feature flag."
  - name: aid-create-infra
    verb: create
    artifact: infra
    alias_of: null
    default_type: IMPLEMENT
    group: G4
    intent: "Provision a new infrastructure resource."
  - name: aid-create-roadmap
    verb: create
    artifact: roadmap
    alias_of: null
    default_type: DOCUMENT
    group: G4
    intent: "Realize a roadmap seed into roadmap.md, creating the document on first use; leaves the MVP section to /aid-create-mvp."
    repurpose: true
  - name: aid-create-backlog
    verb: create
    artifact: backlog
    alias_of: null
    default_type: DOCUMENT
    group: G4
    intent: "Realize a backlog seed into backlog.md, creating the document on first use and moving accepted items out of tech-debt.md."
    repurpose: true
  - name: aid-create-mvp
    verb: create
    artifact: mvp
    alias_of: null
    default_type: DOCUMENT
    group: G4
    intent: "Realize an MVP seed into roadmap.md's MVP section only; the roadmap document itself is /aid-create-roadmap's to create."
    repurpose: true
  - name: aid-create-architecture
    verb: create
    artifact: architecture
    alias_of: null
    default_type: DOCUMENT
    group: G4
    intent: "Realize an architecture seed into the project's build-and-shape (C1) Knowledge Base document; rejected alternatives go to the decisions document."
    repurpose: true
  - name: aid-create-stack
    verb: create
    artifact: stack
    alias_of: null
    default_type: DOCUMENT
    group: G4
    intent: "Realize a stack seed into the project's technology (C0) Knowledge Base document, versions included; rejected alternatives go to the decisions document."
    repurpose: true

  # --- G5: Change + Refactor family (feature-007, task-017) --
  # -- 15 canonical aid-update* rows (12 in this section; aid-update-test in G7,
  # -- aid-update-document in G8, aid-update-dashboard in G11) + aid-refactor.
  # -- aid-refactor is bare with no artifact suffixes.
  - name: aid-update-roadmap
    verb: update
    artifact: roadmap
    alias_of: null
    default_type: DOCUMENT
    group: G5
    intent: "Revise roadmap.md's direction entries outside the MVP section, plus any previously created outputs."
    repurpose: true
  - name: aid-update-mvp
    verb: update
    artifact: mvp
    alias_of: null
    default_type: DOCUMENT
    group: G5
    intent: "Revise roadmap.md's MVP section only, plus any previously created outputs."
    repurpose: true
  - name: aid-update-backlog
    verb: update
    artifact: backlog
    alias_of: null
    default_type: DOCUMENT
    group: G5
    intent: "Revise backlog.md -- re-prioritize, add items, and promote accepted tech-debt.md rows -- plus any previously created outputs."
    repurpose: true
  - name: aid-update
    verb: update
    artifact: ""
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing internal code artifact's behavior under new acceptance criteria."
  - name: aid-update-api
    verb: update
    artifact: api
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing API endpoint / middleware's contract or behavior."
  - name: aid-update-ui
    verb: update
    artifact: ui
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing UI component or page."
  - name: aid-update-theme
    verb: update
    artifact: theme
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing visual theme or style-token set."
  - name: aid-update-cli
    verb: update
    artifact: cli
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing CLI command."
  - name: aid-update-data-model
    verb: update
    artifact: data-model
    alias_of: null
    default_type: MIGRATE
    group: G5
    intent: "Update an existing data model/entity's schema, with forward+rollback migration."
  - name: aid-update-data-pipeline
    verb: update
    artifact: data-pipeline
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing data pipeline's source, transform, sink, or schedule."
  - name: aid-update-messaging
    verb: update
    artifact: messaging
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing message/event schema or its emission."
  - name: aid-update-integration
    verb: update
    artifact: integration
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing external-service integration."
  - name: aid-update-job
    verb: update
    artifact: job
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing scheduled or background job."
  - name: aid-update-config
    verb: update
    artifact: config
    alias_of: null
    default_type: CONFIGURE
    group: G5
    intent: "Update an existing configuration option or feature flag."
  - name: aid-update-infra
    verb: update
    artifact: infra
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Update an existing infrastructure resource."
  - name: aid-refactor
    verb: refactor
    artifact: ""
    alias_of: null
    default_type: REFACTOR
    group: G5
    intent: "Restructure or optimize code without changing behavior (rename, restructure, or improve performance)."

  # --- G5: Remove + Deprecate + Migrate (v2.1.0 coverage-gap follow-on,
  # work-001-lite-aid-skills) -- 3 rows: aid-remove, aid-deprecate, aid-migrate.
  # Bare verbs, no artifact suffixes. Non-schema data/dependency/platform migration;
  # schema migrations stay create/update-data-model. Family scaffolding:
  # shortcut-scaffolding/change-refactor.md.
  - name: aid-remove
    verb: remove
    artifact: ""
    alias_of: null
    default_type: REFACTOR
    group: G5
    intent: "Remove or delete a code artifact, endpoint, dependency, feature, or dead code; update dependents, tests, and docs."
  - name: aid-deprecate
    verb: deprecate
    artifact: ""
    alias_of: null
    default_type: IMPLEMENT
    group: G5
    intent: "Deprecate an existing artifact/API: mark deprecated, add warnings and a migration path, without deleting yet."
  - name: aid-migrate
    verb: migrate
    artifact: ""
    alias_of: null
    default_type: MIGRATE
    group: G5
    intent: "Migrate data, a dependency, framework, or platform, with a rollback plan (non-schema; schema migrations use create/update-data-model)."

  # --- G7: Test + Experiment family -- work-005 restructure of feature-009 --
  # Test AUTHORING is a create/update ARTIFACT: aid-create-test / aid-update-test --
  # ENGINE-DRIVEN, keep-cycle, executed by aid-developer (test code is code);
  # artifact=test, group G7 for discovery; they consult create.md / change-refactor.md.
  # Test RUNNING is aid-test -- a HAND-AUTHORED collapse (run any suite/verification +
  # consolidate findings; aid-reviewer, review-shaped; resolves nothing; fixes -> /aid-fix).
  # aid-test-security/-performance/-data-quality are kind-siblings (repurpose; body delegates
  # to aid-test). Removal via bare aid-remove. See canonical/skills/aid-test/SKILL.md.
  - name: aid-create-test
    verb: create
    artifact: test
    alias_of: null
    default_type: TEST
    group: G7
    intent: "Author new tests (unit/integration/e2e); each test traces to an acceptance criterion; framework inferred from the KB."
  - name: aid-update-test
    verb: update
    artifact: test
    alias_of: null
    default_type: TEST
    group: G7
    intent: "Update or extend existing tests."
  - name: aid-test
    verb: test
    artifact: ""
    alias_of: null
    default_type: TEST
    group: G7
    intent: "Run any requested test suite/verification (unit/integration/e2e, security, performance, data-quality, model-eval) and consolidate findings; resolves nothing; hand fixes off to /aid-fix."
    repurpose: true
  - name: aid-test-security
    verb: test
    artifact: security
    alias_of: null
    default_type: TEST
    group: G7
    intent: "Run a security verification (SAST/DAST/fuzz/dependency-audit) and report -- security kind of aid-test; body delegates."
    repurpose: true
  - name: aid-test-performance
    verb: test
    artifact: performance
    alias_of: null
    default_type: TEST
    group: G7
    intent: "Run a performance benchmark/load/stress test against a threshold and report -- performance kind of aid-test; body delegates."
    repurpose: true
  - name: aid-test-data-quality
    verb: test
    artifact: data-quality
    alias_of: null
    default_type: TEST
    group: G7
    intent: "Run data-quality checks (schema/freshness/completeness/uniqueness) and report -- data-quality kind of aid-test; body delegates."
    repurpose: true
  - name: aid-experiment
    verb: experiment
    artifact: ""
    alias_of: null
    default_type: RESEARCH
    group: G7
    intent: "Design, run, and analyze a controlled experiment or A/B test."

  # --- G3: Prototype + Design (work-005 reframe of feature-005) --
  # HAND-AUTHORED collapse skills. aid-prototype: build a THROWAWAY model to validate a
  # direction NOW (LIGHT verify; resolves nothing; hand off the real build to /aid-create*).
  # aid-prototype-ui -> backward-compat hint-alias (ui target). aid-design: NEW -- produce a
  # KEPT design artifact (UX/flow/component/interface/a11y) to inform the build (FULL verify).
  # Both producer=aid-architect. prototype = throwaway/validate; design = kept/inform-the-build.
  # See canonical/skills/aid-prototype/SKILL.md and canonical/skills/aid-design/SKILL.md.
  - name: aid-prototype
    verb: prototype
    artifact: ""
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Build a low-fidelity model to validate a direction before committing to a full build."
    repurpose: true
  - name: aid-prototype-ui
    verb: prototype
    artifact: ui
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Wireframe/mock a UI to validate UX direction (ui kind of aid-prototype; body delegates)."
    repurpose: true
  - name: aid-design
    verb: design
    artifact: ""
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Produce a kept design artifact (UX/flow/component/interface, with a11y notes) to inform the build; resolves nothing."
    repurpose: true
  - name: aid-design-roadmap
    verb: design
    artifact: roadmap
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop the project's direction as a design seed -- committed vs. wanted, and the sequencing rationale; writes no KB document."
    repurpose: true
  - name: aid-design-mvp
    verb: design
    artifact: mvp
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Draw the MVP line as a design seed -- what is in the first shippable slice, what defers, and why each cut was made."
    repurpose: true
  - name: aid-design-backlog
    verb: design
    artifact: backlog
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop the defined-and-prioritized item set as a design seed, including which tech-debt.md rows to accept into the plan."
    repurpose: true
  - name: aid-design-api
    verb: design
    artifact: api
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop an API design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-ui
    verb: design
    artifact: ui
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a UI design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-theme
    verb: design
    artifact: theme
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a theme design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-cli
    verb: design
    artifact: cli
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a CLI design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-data-model
    verb: design
    artifact: data-model
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a data-model design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-data-pipeline
    verb: design
    artifact: data-pipeline
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a data-pipeline design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-messaging
    verb: design
    artifact: messaging
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a messaging design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-integration
    verb: design
    artifact: integration
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop an integration design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-job
    verb: design
    artifact: job
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a job design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-config
    verb: design
    artifact: config
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a config design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-infra
    verb: design
    artifact: infra
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop an infrastructure design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-test
    verb: design
    artifact: test
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a test design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-document
    verb: design
    artifact: document
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a document design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-design-dashboard
    verb: design
    artifact: dashboard
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop a dashboard design in .aid/design/ before building it; writes a seed, builds nothing."
    repurpose: true
  - name: aid-brainstorm
    verb: brainstorm
    artifact: ""
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Diverge on an unformed problem, then converge to a .aid/design/ seed; resolves nothing."
    repurpose: true
  - name: aid-design-architecture
    verb: design
    artifact: architecture
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop the system's shape as a design seed (components, boundaries, interactions, invariants); writes no KB document -- /aid-create-architecture realizes it."
    repurpose: true
  - name: aid-design-stack
    verb: design
    artifact: stack
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop the technology choice as a design seed (languages, runtimes, frameworks, build and test tooling with versions) plus the alternatives rejected and why."
    repurpose: true
  - name: aid-design-testing-strategy
    verb: design
    artifact: testing-strategy
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop the testing policy as a design seed (levels, coverage expectations, which gates block a merge, who may waive one); not test code."
    repurpose: true
  - name: aid-design-cicd
    verb: design
    artifact: cicd
    alias_of: null
    default_type: DESIGN
    group: G3
    intent: "Develop the delivery pipeline as a design seed (stages, triggers, environments, promotion); touches no workflow file."
    repurpose: true

  # --- G8: Document family -- work-005 reframe of feature-010 --
  # `document` is a create/update ARTIFACT, not a verb. HAND-AUTHORED collapse skills.
  # Canonical: aid-create-document / aid-update-document (generic; format+genre resolved by
  # intelligence). The 8 legacy aid-document* and aid-create-diagram are KIND-SIBLINGS
  # (repurpose, alias_of null, own {verb,artifact}; their hand-authored body delegates to
  # aid-create-document, binding a genre/format hint).
  # Producer aid-tech-writer, verifier aid-reviewer; present-before-place; NEVER writes
  # .aid/knowledge/ (that is aid-update-kb's territory).
  # See canonical/skills/aid-create-document/SKILL.md.
  - name: aid-create-document
    verb: create
    artifact: document
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Create a document NOW (format/genre resolved by intelligence -- markdown, diagram, ADR, runbook, ...); resolves nothing; present before placing."
    repurpose: true
  - name: aid-update-document
    verb: update
    artifact: document
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Update an existing document NOW; resolves nothing; present before placing."
    repurpose: true
  - name: aid-create-diagram
    verb: create
    artifact: diagram
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Create a diagram (mermaid/graphviz) -- diagram kind of aid-create-document; body delegates."
    repurpose: true
  - name: aid-document
    verb: document
    artifact: ""
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Write a general how-to/reference/explanation document or status/progress report (general kind; body delegates to aid-create-document)."
    repurpose: true
  - name: aid-document-decision
    verb: document
    artifact: decision
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Write an ADR: context, decision, alternatives, consequences."
    repurpose: true
  - name: aid-document-architecture
    verb: document
    artifact: architecture
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Document a system's components, boundaries, interactions, and diagrams."
    repurpose: true
  - name: aid-document-guideline
    verb: document
    artifact: guideline
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Write an advisory guideline: recommended practice with rationale and examples."
    repurpose: true
  - name: aid-document-standard
    verb: document
    artifact: standard
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Write a mandatory standard: rule, scope, compliance/enforcement, exceptions."
    repurpose: true
  - name: aid-document-runbook
    verb: document
    artifact: runbook
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Write an operational runbook: trigger, diagnostic, remediation, escalation."
    repurpose: true
  - name: aid-document-tutorial
    verb: document
    artifact: tutorial
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Write a learning-oriented tutorial: prerequisites, worked steps, outcome."
    repurpose: true
  - name: aid-document-changelog
    verb: document
    artifact: changelog
    alias_of: null
    default_type: DOCUMENT
    group: G8
    intent: "Write changelog/release notes (Added/Changed/Fixed/Removed/Security)."
    repurpose: true

  # --- G11: Analyze & Report family (feature-011, task-025) -- 2 rows, no aliases ---
  # aid-report: HAND-AUTHORED collapse skill (work-005). Analyzes data/usage NOW and
  # presents an insight report (findings, conclusions +/-, caveats, conflicts, gaps);
  # resolves nothing. aid-researcher produces, aid-reviewer verifies. Durable dashboards
  # are aid-create-dashboard's job, not this. See canonical/skills/aid-report/SKILL.md.
  - name: aid-report
    verb: report
    artifact: ""
    alias_of: null
    default_type: RESEARCH
    group: G11
    intent: "Analyze data or usage and communicate insight (EDA, metrics, A/B analysis)."
    repurpose: true
  # dashboard: work-005 reframe of show-dashboard into a create/update ARTIFACT --
  # ENGINE-DRIVEN, keep-cycle (a durable/published BI view is a real build).
  # group G11 for discovery. Boundary:
  # aid-create-dashboard = durable/refreshable BI view; aid-report = one-time analysis.
  # See canonical/skills/aid-create-dashboard/SKILL.md.
  - name: aid-create-dashboard
    verb: create
    artifact: dashboard
    alias_of: null
    default_type: IMPLEMENT
    group: G11
    intent: "Build a durable dashboard / BI view (source -> visualization -> publish/refresh)."
  - name: aid-update-dashboard
    verb: update
    artifact: dashboard
    alias_of: null
    default_type: IMPLEMENT
    group: G11
    intent: "Update an existing dashboard / BI view (source, visualization, or refresh cadence)."

  # --- G11: Review + Research (v2.1.0 coverage-gap follow-on,
  # work-001-lite-aid-skills) -- 2 rows: aid-review, aid-research. Bare verbs,
  # no artifact suffixes. Family scaffolding: shortcut-scaffolding/analyze-report.md
  # (same RESEARCH-ends-in-a-recommendation shape as aid-report).
  # aid-review: HAND-AUTHORED collapse skill (work-005). repurpose: true so
  # build-shortcut-skills.py never generates/overwrites it. A review runs NOW and returns
  # findings + a grade (single-shot: INTAKE -> REVIEW -> VERIFY -> PRESENT -> PUBLISH -> DONE);
  # it no longer plans-and-halts through the shortcut engine. See
  # canonical/skills/aid-review/SKILL.md.
  - name: aid-review
    verb: review
    artifact: ""
    alias_of: null
    default_type: RESEARCH
    group: G11
    intent: "Review/assess an existing artifact -- code, a change/diff, or a design -- against criteria; produce findings + recommendations."
    repurpose: true
  # aid-research: HAND-AUTHORED collapse skill (work-005).
  # A research investigation runs NOW and returns a curated, verified answer -- it RESOLVES
  # NOTHING (presents conclusions +/-, conflicts with their reasons, and gaps for the user
  # to resolve). aid-researcher produces, aid-reviewer verifies. See
  # canonical/skills/aid-research/SKILL.md.
  - name: aid-research
    verb: research
    artifact: ""
    alias_of: null
    default_type: RESEARCH
    group: G11
    intent: "Investigate an open technical question (evaluate options or run a feasibility spike); present curated, verified conclusions, conflicts, and gaps for the user to resolve."
    repurpose: true

  # --- G9/G10: Deploy + Monitor re-purpose (feature-012, task-034) -- 2 rows, no
  # aliases. repurpose: true -- these are the 2 pre-existing FAT pipeline skills this
  # catalog also registers so the parity check and /aid-triage recognise them; the
  # build helper SKIPS these rows (never generates/overwrites aid-deploy/aid-monitor's
  # hand-authored SKILL.md).
  - name: aid-deploy
    verb: deploy
    artifact: ""
    alias_of: null
    default_type: CONFIGURE
    group: G9
    intent: "Ship an artifact to its target environment/audience (promote, verify, rollback)."
    repurpose: true
  - name: aid-monitor
    verb: monitor
    artifact: ""
    alias_of: null
    default_type: CONFIGURE
    group: G10
    intent: "Run, observe, and sustain a live asset (SLOs, observability, toil, capacity)."
    repurpose: true

  # --- Q&A re-purpose (v2.1.0 coverage-gap follow-on, work-001-lite-aid-skills) -- 1 row,
  # repurpose: true. aid-ask: hand-authored single-shot Q&A skill (NOT generated by
  # build-shortcut-skills.py). Group G11 (Analyze & Report): answering a project question
  # by citing KB/codebase/in-flight-work evidence, one-shot, without a work folder.
  - name: aid-ask
    verb: query
    artifact: ""
    alias_of: null
    default_type: RESEARCH
    group: G11
    intent: "Answer a free-form question about the project from the KB, codebase, and in-flight works -- read-only, cited, gap-capturing."
    repurpose: true
