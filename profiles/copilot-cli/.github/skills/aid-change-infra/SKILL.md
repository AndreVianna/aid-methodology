---
name: aid-change-infra
description: >
  Direct-entry Lite-path shortcut (Change an existing infrastructure resource.) -- skips the aid-describe
  interview/triage. Binds VERB=`change` ARTIFACT=`infra` and runs the shared
  shortcut engine, producing a fully-graded flattened Lite work that halts for
  approval.
  State machine: delegated to .github/aid/templates/shortcut-engine.md
  (INTAKE -> CAPTURE -> SPEC -> PLAN -> DETAIL -> GATE -> APPROVAL-HALT).
allowed-tools: Read, Glob, Grep, shell, Write, Edit, Agent
argument-hint: "[description]  -- what to change; runs straight to a graded flattened Lite work"
---

# Shortcut: change infra

<!-- GENERATED by .claude/skills/generate-profile/scripts/build-shortcut-skills.py from .github/aid/templates/shortcut-catalog.yml -- DO NOT EDIT BY HAND; edit the catalog row and re-run the helper, then the full run_generator.py. -->

Bind **VERB=`change`**, **ARTIFACT=`infra`**, then run the shared engine at `.github/aid/templates/shortcut-engine.md`. The engine scaffolds the flattened Lite work (feature-001 structure), authors REQUIREMENTS -> SPEC -> PLAN + BLUEPRINT -> DETAIL tasks with reduced capture, runs the per-document Grading Gates (feature-004), and halts at the FR-10 approval gate. It never executes. This shortcut's `default_type`/`group`/`alias_of` are its row in `.github/aid/templates/shortcut-catalog.yml`.
