---
name: aid-test-data-quality
description: >
  Direct-entry Lite-path shortcut (Check a dataset or pipeline's schema, freshness, completeness, or uniqueness.) -- skips the aid-describe
  interview/triage. Binds VERB=`test` ARTIFACT=`data-quality` and runs the shared
  shortcut engine, producing a fully-graded flattened Lite work that halts for
  approval.
  State machine: delegated to .cursor/aid/templates/shortcut-engine.md
  (INTAKE -> CAPTURE -> SPEC -> PLAN -> DETAIL -> GATE -> APPROVAL-HALT).
allowed-tools: Read, Glob, Grep, Terminal, Write, Edit, Agent
argument-hint: "[description]  -- what to test; runs straight to a graded flattened Lite work"
---

# Shortcut: test data-quality

<!-- GENERATED by .claude/skills/generate-profile/scripts/build-shortcut-skills.py from .cursor/aid/templates/shortcut-catalog.yml -- DO NOT EDIT BY HAND; edit the catalog row and re-run the helper, then the full run_generator.py. -->

Bind **VERB=`test`**, **ARTIFACT=`data-quality`**, then run the shared engine at `.cursor/aid/templates/shortcut-engine.md`. The engine scaffolds the flattened Lite work (feature-001 structure), authors REQUIREMENTS -> SPEC -> PLAN + BLUEPRINT -> DETAIL tasks with reduced capture, runs the per-document Grading Gates (feature-004), and halts at the FR-10 approval gate. It never executes. This shortcut's `default_type`/`group`/`alias_of` are its row in `.cursor/aid/templates/shortcut-catalog.yml`.
