---
name: aid-refactor
description: >
  Restructure or optimize code without changing behavior (rename, restructure, or improve
  performance). Use this skill when you already know what to refactor and want it scoped,
  specified, and broken into reviewable tasks in a single pass, with no requirements
  interview. You approve the resulting plan before anything is built: this skill plans and
  stops, so run `/aid-execute` to carry the plan out.
allowed-tools: Read, Glob, Grep, Bash, Write, Edit, Agent
argument-hint: "[description]  -- what to refactor; runs straight to a graded flattened Lite work"
---

# Shortcut: refactor

<!-- GENERATED by .claude/skills/generate-profile/scripts/build-shortcut-skills.py from .agent/aid/templates/shortcut-catalog.yml -- DO NOT EDIT BY HAND; edit the catalog row and re-run the helper, then the full run_generator.py. -->

Bind **VERB=`refactor`**, **ARTIFACT="" (bare verb)**, then run the shared engine at `.agent/aid/templates/shortcut-engine.md`. The engine scaffolds the flattened Lite work (feature-001 structure), authors `REQUIREMENTS.md` -- whose `§ 11` feature section carries the technical specification -- then the task `DETAIL.md` files, with reduced capture. It writes no SPEC.md and no PLAN.md: one feature and one delivery means nothing to sequence, and the execution graph is derived from each task's `**Depends on:**` field. It runs the per-document Grading Gates (feature-004) and halts at the FR-10 approval gate. It never executes. This shortcut's `default_type`/`group` are its row in `.agent/aid/templates/shortcut-catalog.yml`.
