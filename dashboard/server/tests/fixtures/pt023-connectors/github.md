---
name: "GitHub"
connection_type: mcp
endpoint: ""
auth_method: none
preset: custom
objective: "Connect to GitHub via its host tool's own MCP/plugin; AID stores no credential."
summary: "GitHub (mcp) -- request via the host tool's MCP/plugin."
tags: [connector, mcp]
audience: [developer, architect]
---

# GitHub

> Connection: mcp · Mode: tool-managed · Auth: handled by the host tool (no AID credential)

Request this connection from the host tool's own MCP/plugin at use-time; AID stores no credential for it.
