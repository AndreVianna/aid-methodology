---
title: Report a Bug or Issue in the AID CLI, Skills or Scripts
description: Report a bug in AID itself — the aid CLI, a skill, an agent, or a script. Opens a prefilled GitHub issue, no backend.
reportIssue: false
---

import ReportForm from '../../../components/ReportForm.astro';

Found a bug in AID itself — the `aid` CLI, a skill, an agent, or one of the scripts behaving
incorrectly? Describe what happened below. The more specific you are (command, version, environment),
the faster it can be triaged.

<ReportForm kind="bug" />
